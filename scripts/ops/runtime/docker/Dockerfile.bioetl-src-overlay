# Overlay-only rebuild: refresh /app/src without reinstalling third-party deps.
# Use when host Docker Desktop already has bioetl-main-bioetl:local and only
# interfaces/application code changed (e.g. report_root readiness).
FROM bioetl-main-bioetl:local
USER root
COPY --chown=root:root src/ ./src/
USER bioetl
