______________________________________________________________________

Version: 1.0.0
Status: active
Class: internal-published
Owner: BioETL Team
Reviewers:

- BioETL Team
  Last verified: '2026-03-31'

______________________________________________________________________

# Практический Индекс Skills

*Статус: internal-working-reference*
*Обновлено: 2026-04-02*

Этот документ собирает **уже описанные в рабочей сессии skills** в удобный практический индекс:

- `SKILLS-CATALOG.md` остаётся каноническим реестром локальных BioETL-skills
- этот файл нужен как **операционная шпаргалка**
- сюда включены как BioETL-skills, так и полезные global/system/knowledge skills, которые реально могут использоваться в работе
- runtime source-of-truth остаётся в `.codex/skills/`, а этот индекс не должен
  использоваться как authoring surface для изменения skill behavior
- **Live project skills (14):** `agent-debugging`, `py-audit-bot`, `py-config-bot`,
  `py-debug-bot`, `py-doc-bot`, `py-plan-bot`, `py-test-bot`, `research-workflow`,
  `new-pipeline`, `observability-dashboard`, `observability-prometheus`,
  `technical-designer-mermaid`, `vcr-record`, `verify-architecture`.
  Names such as `py-review-orchestrator`, `py-test-swarm`,
  `grafana-dashboard-*`, and split `prometheus-*` skills are historical
  references only.

Текущее правило orchestration: production-код в обычном Codex workflow пишет orchestrator напрямую. Если встречается `py-code-bot`, считай это legacy/compatibility reference, а не preferred active step.

## 15 Skills, Которые Стоит Использовать Чаще Всего Именно В BioETL

Ниже не “самые мощные вообще”, а **самые практичные для повседневного workflow BioETL**: код, архитектура, конфиги, тесты, docs и безопасная работа с ветками.

| Приоритет | Skill                   | Зачем нужен в BioETL                                                                                                                                                       | Когда доставать первым                                                             |
| --------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 1         | `capability-discovery`  | Быстро показывает, какие project-specific команды, skills и quality gates уже есть в репозитории. Сильно снижает лишнюю ручную работу.                                     | В начале новой задачи, в незнакомой зоне проекта, перед запуском сложного workflow |
| 2         | `py-plan-bot`           | Хорошо раскладывает большие изменения на шаги по слоям BioETL: код, конфиги, тесты, docs, architecture checks.                                                             | Когда задача не умещается в “исправь один файл”                                    |
| 3         | `using-git-worktrees`   | Даёт безопасную изоляцию для реализации, если дерево уже грязное или работа рискованная. Для BioETL это особенно полезно из-за длинных рефакторингов и параллельных веток. | Перед крупным feature/fix/refactor или branch consolidation                        |
| 4         | `py-debug-bot`          | Хорош для root-cause analysis в многослойных поломках, где symptom и причина живут в разных местах.                                                                        | Когда failure не локализуется одной быстрой правкой                                |
| 5         | `py-test-bot`           | Самый частый спутник кодовой работы: написать, адаптировать, стабилизировать или починить тесты.                                                                           | После изменений в логике, при локальных падениях suite                             |
| 6         | `verify-unit-tests`     | Проверяет, что unit tests остались unit tests: детерминированные, изолированные, без лишнего I/O.                                                                          | После добавления или переписывания тестов                                          |
| 7         | `verify-architecture`   | Быстрый operational guardrail после структурных изменений. Позволяет подтвердить, что import matrix и layer boundaries не поехали.                                         | Перед commit/PR после рефакторинга или DI/wiring changes                           |
| 8         | `architecture-guardian` | Более строгий архитектурный взгляд: ADR, anti-patterns, naming, ответственность модулей.                                                                                   | Когда изменение затрагивает `domain/application/infrastructure/composition`        |
| 9         | `py-config-bot`         | Для BioETL это must-have: конфиги здесь управляют значительной частью поведения pipelines.                                                                                 | При работе с `configs/**`, schema rules, provider/entity YAML                      |
| 10        | `documentation-audit`   | Документация в BioETL легко дрейфует после рефакторинга. Этот skill помогает быстро поймать рассинхрон.                                                                    | После заметных изменений в архитектуре, tooling, workflow, onboarding              |
| 11        | `py-doc-bot`            | Исполнительский docs-skill для точечных и массовых обновлений документации.                                                                                                | Когда уже известно, какие docs нужно синхронизировать                              |
| 12        | `vcr-record`            | Практически обязательный skill для HTTP/VCR интеграций с провайдерами. Держит кассеты и secret-safety в порядке.                                                           | При записи, обновлении и проверке cassette-based tests                             |
| 13        | `collecting-evidence`   | Полезен, когда нужно не спорить об архитектуре на интуиции, а собирать traceable evidence pack.                                                                            | Перед repo-wide выводами, redesign и roadmap-калибровкой                           |
| 14        | `synthesizing-pillars`  | Превращает собранный evidence в patterns, tensions и actionable conclusions.                                                                                               | После research-wave, до roadmap или решения                                        |
| 15        | `making-decisions`      | Закрепляет явные решения с рисками и trade-offs вместо плавающих выводов в чате.                                                                                           | Когда evidence уже собран и нужен decision layer                                   |

## Быстрая Карта По Сценариям

| Сценарий                             | Лучший стартовый набор                                                                                                   |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Новый feature/fix                    | `capability-discovery` → `py-plan-bot` → `using-git-worktrees` → orchestrator (direct implementation)                    |
| Непонятное падение                   | `py-debug-bot` → `py-test-bot` → `verify-unit-tests`                                                                     |
| Архитектурный рефакторинг            | `py-plan-bot` → `architecture-guardian` → `verify-architecture`                                                          |
| Pipeline/config работа               | `py-config-bot` → `data-engineer` → `schema-parity`                                                                      |
| Provider/VCR                         | `provider-health` → `vcr-record` → `py-test-bot`                                                                         |
| Grafana dashboard / observability UX | `observability-dashboard` → `py-doc-bot` |
| Prometheus query / alerting          | `observability-prometheus` |
| Docs drift                           | `py-doc-bot` |
| Большой review                       | `py-audit-bot` (`review`) |
| Branch cleanup / consolidation       | `using-git-worktrees` → `git-workflow-manager`                                                                           |
| Иерархическая evidence-wave          | `hierarchical-evidence-orchestration` → `collecting-evidence` → `synthesizing-pillars`                                   |

## Полный Индекс Уже Описанных Skills

Ниже собраны **все skills, которые уже были подробно описаны в рабочей сессии**.

### BioETL / Delivery

| Skill                                 | Назначение                                                                                                                                                                                         | Когда вызывать                                                                      | Обычно сочетать                                                    |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `agent-orchestration`                 | Координация multi-agent или multi-role workflow по одной сложной задаче. Помогает разложить работу по профилям и сохранить целостность результата.                                                 | Когда задача затрагивает код, тесты, docs, конфиги и review одновременно.           | `py-plan-bot`, `py-audit-bot` (`review`), `py-test-bot`           |
| `hierarchical-evidence-orchestration` | Запускает topic-level evidence программу по shard-пакетам: сначала collection, затем synthesis, затем parent cross-synthesis. Полезен, когда один `collecting-evidence` уже слишком узок для темы. | Когда нужно исследовать repo-wide тему по слоям, package families или doc domains.  | `collecting-evidence`, `synthesizing-pillars`, `making-decisions`  |
| `architecture-guardian`               | Проверяет архитектурные границы, ADR compliance, naming и anti-patterns. Смотрит на форму системы, а не только на зелёные тесты.                                                                   | После рефакторинга, перед PR, при review изменений по слоям.                        | `verify-architecture`, `py-audit-bot`                              |
| `capability-discovery`                | Находит доступные skills, agents, commands и quality gates проекта. Помогает не изобретать workflow вручную.                                                                                       | В начале работы в насыщенном или незнакомом repo.                                   | `repo-config`, `py-plan-bot`                                       |
| `create-pr`                           | Упаковывает изменения в понятный PR с хорошим scope, verification context и reviewer-friendly структурой.                                                                                          | Когда реализация завершена и её нужно передать в review.                            | `suggest-users`, `verify-implementation`                           |
| `deep-research`                       | Делает не быстрый ответ, а глубокое исследование с источниками, сравнением и синтезом. Полезен там, где важна надёжность вывода.                                                                   | Для сложных технических, продуктовых или domain-вопросов.                           | `collecting-evidence`, `making-decisions`                          |
| `repo-config`                         | Читает реальную конфигурацию репозитория: default branch, labels, repo identity, settings. Убирает риск неверных предположений.                                                                    | Перед GitHub automation, branch cleanup, PR workflows.                              | `create-pr`, `git-workflow-manager`                                |
| `suggest-users`                       | Подбирает reviewers и assignees по ownership, file expertise и активности. Делает review loop быстрее и точнее.                                                                                    | При создании PR, issue и re-request review.                                         | `create-pr`, `repo-config`                                         |
| `vcr-record`                          | Управляет VCR-кассетами: запись, валидация, обновление, очистка. Держит HTTP tests воспроизводимыми и безопасными.                                                                                 | При работе с integration tests против внешних API.                                  | `py-test-bot`, `verify-unit-tests`                                 |
| `new-pipeline`                        | Помогает добавить новый pipeline так, чтобы он сразу жил по правилам BioETL: configs, wiring, baseline verification.                                                                               | При добавлении нового provider/entity pipeline.                                     | `py-config-bot`, `data-engineer`, `verify-architecture`            |
| `grafana-dashboard-extension`         | Специализированно ведёт правки shipped Grafana dashboards: панели, drilldowns, navigation, datasource semantics и docs cascade.                                                                    | Когда меняются `grafana/dashboards/*.json`, monitoring UX или поведение drilldowns. | `documentation-audit`, `py-doc-bot`                                |
| `prometheus-metric-discovery`         | Быстро находит реальные metrics, label names и label values до того, как кто-то начнёт гадать про PromQL по памяти. Снижает риск ложных query fixes.                                               | Когда работа начинается с вопроса “как вообще называется нужная метрика?”.          | `prometheus-query-debugger`, `grafana-dashboard-extension`         |
| `prometheus-query-debugger`           | Разбирает PromQL по смыслу: aggregation, `rate/increase`, empty-state, histogram и label matching. Помогает объяснить проблему, а не только переписать выражение.                                  | Когда Prometheus-backed panel или alert expression ведут себя странно.              | `prometheus-metric-discovery`, `prometheus-alert-rule-editor`      |
| `prometheus-alert-rule-editor`        | Держит alert rules в инженерной форме: intent, threshold, `for`, severity, labels, annotations и distinction между repo-backed и Grafana-managed rules.                                            | Когда создаются или меняются Prometheus-backed alerts.                              | `prometheus-query-debugger`, `prometheus-rule-testing`             |
| `prometheus-rule-testing`             | Даёт детерминированную проверку repo-backed Prometheus rules через `promtool`, чтобы поведение алертов не оставалось на уровне интуиции.                                                           | После изменений в rule files и перед merge/release.                                 | `prometheus-alert-rule-editor`, `py-test-bot`                      |
| `verify-architecture`                 | Операционная архитектурная верификация через quick/full/category checks. Быстро подтверждает, что границы проекта не нарушены.                                                                     | Перед commit/PR после структурных изменений.                                        | `architecture-guardian`, `verify-implementation`                   |
| `py-test-bot` (broad campaign; formerly `py-test-swarm`) | Массовый test workflow для failures, flakiness, coverage и telemetry. Превращает большой тестовый хаос в управляемые кластеры. | Когда проблема уже шире одного локального теста. | `py-audit-bot` (`review`) |
| `py-audit-bot` (`review`; formerly `py-review-orchestrator`) | Иерархический код-ревью workflow по секторам проекта. Собирает findings, severity и remediation в один обзор. | Для широкого project audit, release review, post-refactor review. | `py-audit-bot`, `architecture-guardian` |
| `py-doc-bot`                          | Исполнительский docs-skill для sync и remediation задач. Хорошо доводит документацию до соответствия коду.                                                                                         | После code/config changes и docs drift.                                             | `documentation-audit`, `py-plan-bot`                               |
| `py-audit-bot`                        | Строгий аудит кода, рисков, инвариантов и regressions. Полезен, когда нужно оценить состояние, а не только сразу фиксить.                                                                          | Перед релизом, после крупных изменений, при audit-режиме.                           | `architecture-guardian`, `verify-architecture`                  |
| `py-config-bot`                       | Профиль для YAML/schema/topology/config consistency. Относится к конфигам как к коду, а не как к второстепенным файлам.                                                                            | При изменении `configs/**`, schema rules, provider/entity settings.                 | `schema-parity`, `new-pipeline`                                    |
| `py-debug-bot`                        | Методичный root-cause analysis для сложных поломок. Держит reasoning чистым и не даёт застрять в поверхностном patching.                                                                           | Когда symptom и причина разбросаны по нескольким слоям.                             | `debugger`, `py-test-bot`                                          |
| `py-test-bot`                         | Локальная работа с тестами: писать, обновлять, стабилизировать, чинить. Помогает держать tests как инженерный актив.                                                                               | При конкретной test-задаче или падении suite.                                       | `verify-unit-tests`, `vcr-record`                                  |
| `py-plan-bot`                         | Декомпозирует сложную работу на безопасные и логичные шаги. Делает многослойные задачи обозримыми.                                                                                                 | Когда задача затрагивает код, тесты, docs и конфиги сразу.                          | `using-git-worktrees`, orchestrator                                |
| `py-code-bot`                         | Historical compatibility profile. Может встречаться в старых notes и mirrors, но не является обычным активным skill entry в текущем Codex orchestration.                                           | Только при разборе legacy references или исторических workflow artifacts.           | `py-plan-bot`, `docs/00-project/ai/agents/agents/ORCHESTRATION.md` |
| `refactoring-specialist`              | Специализируется на структурном улучшении кода без потери управляемости. Убирает дублирование, legacy seams и смешанные ответственности.                                                           | Когда система работает, но её форма мешает развитию.                                | `architecture-guardian`, `verify-architecture`                     |
| `test-automator`                      | Автоматизирует тестовые workflows и harness, уменьшая ручную рутину. Хорош для повторяемых QA-процессов.                                                                                           | Когда тестовая работа стала слишком процедурной или хрупкой.                        | `py-test-bot`, `verify-unit-tests`                                 |
| `data-engineer`                       | Смотрит на data flow, schema, lineage и transforms как на first-class concern. Помогает видеть поведение данных, а не только код вокруг них.                                                       | При pipeline logic, schema evolution, normalization, medallion transitions.         | `schema-parity`, `provider-health`                                 |
| `scientific-literature-researcher`    | Работает с научными источниками и scholarly domain context. Полезен там, где нужно понять не только API, но и предметную среду.                                                                    | Для publication/provider/domain research и сравнений scholarly sources.             | `deep-research`, `collecting-evidence`                             |
| `debugger`                            | Спокойный и последовательный debug workflow. Локализует проблему и помогает не перепутать symptom с cause.                                                                                         | Когда ошибка комплексная и не чинится одной очевидной правкой.                      | `py-debug-bot`, `using-git-worktrees`                              |
| `database-optimizer`                  | Фокусируется на storage behavior, data performance и эффективности операций чтения/записи.                                                                                                         | При bottlenecks в Delta/Arrow/export/storage logic.                                 | `data-engineer`, `py-audit-bot`                                    |
| `dependency-manager`                  | Управляет библиотеками, extras и совместимостью среды. Делает dependency changes безопасными и воспроизводимыми.                                                                                   | При обновлении пакетов, version conflicts, setup drift.                             | `verify-implementation`, `git-workflow-manager`                    |
| `git-workflow-manager`                | Помогает с ветками, cherry-pick, rebase/merge, cleanup и history hygiene. Делает движение изменений по repo чище.                                                                                  | При branch cleanup, consolidation, PR prep, remote confusion.                       | `repo-config`, `using-git-worktrees`                               |
| `prompt-engineer`                     | Проектирует хорошие prompts и instructions для AI workflow. Делает агентные задачи яснее, стабильнее и проверяемее.                                                                                | Когда AI должен выполнять нетривиальный workflow предсказуемо.                      | `agent-orchestration`, `skill-creator`                             |
| `mermaid-design`                      | Проектирует Mermaid-диаграммы как инженерные артефакты, а не просто синтаксис. Помогает выбрать правильную форму для содержания.                                                                   | Для architecture, data flow и process diagrams.                                     | `technical-designer-mermaid`, `docs-styles`                        |
| `provider-health`                     | Проверяет состояние внешних API и отличает внутренний баг от провайдерского сбоя.                                                                                                                  | При flaky integrations, provider regressions, suspicious API behavior.              | `vcr-record`, `data-engineer`                                      |
| `schema-parity`                       | Проверяет согласованность между code, contracts, configs, docs и tests. Особенно полезен против тихого schema drift.                                                                               | После rename migrations, contract changes, provider field updates.                  | `py-config-bot`, `data-engineer`                                   |
| `verify-implementation`               | Комплексная финальная проверка результата через несколько quality layers. Отвечает на вопрос “мы правда готовы?”.                                                                                  | Перед PR, после значимых fix/refactor циклов.                                       | `verify-architecture`, `verify-unit-tests`                         |
| `verify-unit-tests`                   | Проверяет качество unit-тестов: изоляцию, детерминизм, отсутствие лишнего I/O. Держит unit-layer честным и быстрым.                                                                                | После добавления или переписывания unit tests.                                      | `py-test-bot`, `test-automator`                                    |
| `security-best-practices`             | Security-focused review для Python/JS/Go. Ищет небезопасные defaults, утечки и слабые trust boundaries.                                                                                            | Когда явно нужен security review или secure-by-default guidance.                    | `py-audit-bot`, `verify-implementation`                            |

### Skills / Meta

| Skill                 | Назначение                                                                                                                                | Когда вызывать                                                           | Обычно сочетать                           |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------- |
| `skill-creator`       | Создаёт новые skills как переиспользуемые workflow-инструменты. Превращает личный опыт в общую operational practice.                      | Когда recurring task уже стабильно повторяется и пора его формализовать. | `prompt-engineer`, `manage-skills`        |
| `skill-installer`     | Подключает skills из curated sources или GitHub. Упрощает расширение среды без ручного хаоса.                                             | Когда нужно установить полезный workflow в текущую среду.                | `manage-skills`, `skill-creator`          |
| `manage-skills`       | Поддерживает экосистему skills в здоровом состоянии: без дублей, устаревших инструкций и слепых зон.                                      | Когда skills уже используются как часть реальной рабочей среды.          | `skill-creator`, `skill-installer`        |
| `openai-docs`         | Работает с официальной документацией OpenAI как с первоисточником. Полезен там, где память недостаточно надёжна.                          | Для API, моделей, limits, SDK, migration и точных цитат по OpenAI.       | `deep-research`, `prompt-engineer`        |
| `playwright`          | Даёт реальный браузерный проход через UI и web-flow. Убирает гадание там, где важен runtime behavior.                                     | Для UI debugging, form automation, screenshots, web extraction.          | `pdf`, `verify-implementation`            |
| `pdf`                 | Работает с PDF как с визуальным артефактом, а не просто текстом. Проверяет layout, рендеринг и пригодность файла к чтению.                | Когда важны страницы, таблицы, экспорт и визуальная корректность PDF.    | `playwright`, `docs-styles`               |
| `using-git-worktrees` | Создаёт изолированные рабочие деревья для безопасной параллельной работы. Сильно снижает риск задеть грязный workspace.                   | Перед крупной реализацией, экспериментом или branch consolidation.       | `git-workflow-manager`, `py-plan-bot`     |
| `nci-analysis`        | Разбирает тексты на манипуляцию, пропагандистские приёмы и эмоциональное давление. Возвращает более трезвое чтение контента.              | Когда нужно понять, не пытается ли материал влиять вместо объяснения.    | `deep-research`, `collecting-evidence`    |
| `learn`               | Вынимает из разговора или работы устойчивые learnings и сохраняет их как переносимое знание. Делает опыт накапливаемым.                   | После сложного fix/debug/research цикла.                                 | `adding-notes`, `manage-skills`           |
| `demo-2-1-features`   | Демонстрирует новые возможности среды в прикладном формате. Помогает команде быстрее понять, что реально стоит использовать.              | При онбординге, обновлении инструмента, внутреннем показе возможностей.  | `capability-discovery`, `prompt-engineer` |
| `docs-styles`         | Поддерживает визуальный язык документации: типографика, блоки, диаграммы и согласованность. Делает docs легче для чтения.                 | Когда нужно улучшить docs UX и визуальную консистентность.               | `documentation-audit`, `mermaid-diagrams` |
| `mermaid-diagrams`    | Универсальный skill для Mermaid flowchart/sequence/class/state/ER/C4-подобных диаграмм. Быстро делает структуру видимой и поддерживаемой. | Когда текст уже хуже объясняет систему, чем диаграмма.                   | `mermaid-design`, `docs-styles`           |

### Knowledge Base / Notes

| Skill                         | Назначение                                                                                                                     | Когда вызывать                                                              | Обычно сочетать                        |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- | -------------------------------------- |
| `adding-notes`                | Добавляет новые ресурсы в knowledge base так, чтобы они были понятны и полезны потом. Хороший входной фильтр для заметок.      | Когда нужно сохранить статью, видео, книгу, репозиторий или идею.           | `writing-style`, `linking-notes`       |
| `adding-tweets`               | Превращает короткие посты из X/Twitter в нормальные knowledge objects с контекстом. Не даёт базе засоряться “просто ссылками”. | Когда твит содержит идею, источник или полезную формулировку.               | `adding-notes`, `linking-notes`        |
| `creating-obsidian-templates` | Проектирует шаблоны заметок, которые реально снижают трение и повышают согласованность базы.                                   | Когда один и тот же формат заметок создаётся снова и снова.                 | `writing-style`, `manage-skills`       |
| `daily-note`                  | Ежедневная точка сборки мыслей, событий и контекста дня. Помогает не терять маленькие идеи и закрывать день спокойнее.         | Утром, вечером или в течение дня для личной рабочей памяти.                 | `weekly-review`, `adding-notes`        |
| `weekly-review`               | Синтезирует неделю из daily notes и задач. Помогает увидеть паттерны, small wins и recurring blockers.                         | В конце недели или перед планированием следующей.                           | `daily-note`, `monthly-review`         |
| `monthly-review`              | Даёт более крупную ретроспективу месяца: направления, энергия, паттерны, прогресс. Возвращает ощущение траектории.             | В конце месяца или перед новым месячным циклом.                             | `weekly-review`, `update-system-map`   |
| `weekly-newsletter`           | Собирает weekly digest по тому, что добавлено в knowledge base. Делает входящий поток знаний обозримым.                        | Когда нужно быстро вспомнить или показать, что нового накопилось за неделю. | `adding-notes`, `summarizing-topic`    |
| `writing-style`               | Делает текст яснее, короче и полезнее для будущего чтения. Это quality-layer для всей базы знаний.                             | При создании или улучшении любых notes, summaries и docs.                   | `adding-notes`, `enhancing-notes`      |
| `asking-notes`                | Позволяет спрашивать свою базу знаний как вторую память. Быстро собирает уже существующий контекст по теме.                    | Когда хочется узнать “что у меня уже есть по X”.                            | `summarizing-topic`, `linking-notes`   |
| `linking-notes`               | Находит и добавляет meaningful связи между заметками. Превращает архив заметок в сеть знаний.                                  | Когда заметки уже есть, но плохо связаны между собой.                       | `adding-notes`, `moc-curator`          |
| `summarizing-topic`           | Синтезирует знания по теме из нескольких notes. Делает из распределённого материала usable overview.                           | Когда по теме уже накопилось достаточно заметок, но нет общей картины.      | `asking-notes`, `weekly-newsletter`    |
| `reviewing-notes`             | Аудитирует качество заметок: ясность, полноту, связность, полезность. Помогает базе не деградировать.                          | Когда заметок много и чувствуется uneven quality.                           | `enhancing-notes`, `managing-tags`     |
| `moc-curator`                 | Поддерживает Maps of Content как навигационные узлы больших тем. Делает крупные области базы более обозримыми.                 | Когда заметок уже много и нужны хорошие точки входа.                        | `linking-notes`, `exploring-graph`     |
| `exploring-graph`             | Анализирует knowledge graph: hubs, clusters, orphans и structural blind spots.                                                 | Когда база выросла и важна уже не только содержательность, но и структура.  | `moc-curator`, `update-system-map`     |
| `update-system-map`           | Поддерживает верхнеуровневую карту всей knowledge system. Возвращает обзорность и навигацию на meta-уровне.                    | После роста базы, reorganize, cleanup или graph-analysis.                   | `exploring-graph`, `moc-curator`       |
| `enhancing-authors`           | Усиливает author pages: bio, темы, ссылки, роль автора в системе знаний. Делает персональный слой базы более полезным.         | Когда один автор начинает встречаться во многих notes.                      | `linking-notes`, `enhancing-notes`     |
| `enhancing-notes`             | Дорабатывает сырые или короткие заметки до более сильных knowledge artifacts.                                                  | Когда заметка есть, но она ещё слишком слаба для повторного использования.  | `writing-style`, `reviewing-notes`     |
| `enhancing-talks`             | Улучшает notes по лекциям, подкастам и интервью: структура, timestamps, key insights.                                          | Когда есть transcript или сырой конспект разговора/выступления.             | `enhancing-notes`, `summarizing-topic` |
| `managing-blog-ideas`         | Ведёт backlog идей для текстов и развивает сырые замыслы в более ясные outlines.                                               | Когда регулярно появляются мысли “об этом стоит написать”.                  | `adding-notes`, `writing-style`        |
| `managing-tags`               | Консолидирует и чистит теги, чтобы они оставались полезным навигационным слоем, а не шумом.                                    | Когда tag-system разрастается и появляются дубли или хаос.                  | `reviewing-notes`, `moc-curator`       |

### Research / Decision / Spec

| Skill                                 | Назначение                                                                                                                        | Когда вызывать                                                                       | Обычно сочетать                                                   |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| `collecting-evidence`                 | Собирает traceable evidence-объекты с источниками, уверенностью и допущениями. Делает исследование проверяемым и сравнимым.       | Когда нужно строить решение на доказательной базе, а не на интуиции.                 | `deep-research`, `synthesizing-pillars`                           |
| `hierarchical-evidence-orchestration` | Оркестрирует несколько evidence packs как одну wave: shard decomposition, gate checks, synthesis handoff, parent cross-synthesis. | Когда одна тема требует не одного pillar pack, а нескольких coordinated shard packs. | `collecting-evidence`, `synthesizing-pillars`, `making-decisions` |
| `synthesizing-pillars`                | Превращает набор evidence в структурированное понимание по pillar-областям. Находит паттерны, противоречия и сильные сигналы.     | Когда evidence уже собрано, но ясной картины ещё нет.                                | `collecting-evidence`, `making-decisions`                         |
| `making-decisions`                    | Помогает зафиксировать явное решение с trade-offs, рисками и альтернативами. Делает выбор объяснимым и переносимым во времени.    | Когда исследование завершено и пора выбирать путь.                                   | `synthesizing-pillars`, `generating-constrained-specs`            |
| `generating-constrained-specs`        | Пишет спецификации только на основе подтверждённых решений и ссылок. Не даёт документу расползтись в догадки и scope creep.       | Когда нужен PRD или arch doc, опирающийся на уже принятые решения.                   | `making-decisions`, `collecting-evidence`                         |
| `initializing-ledger`                 | Создаёт структуру для нового исследовательского или продуктового пространства: evidence, decisions, synthesis, artifacts.         | В начале большого проекта, ресерча или discovery-потока.                             | `collecting-evidence`, `making-decisions`                         |

## Примечания

- Этот файл — **практический индекс**, а не канонический trigger-registry.
- Для runtime-правил и локального published catalog см. `SKILLS-CATALOG.md`.
- Топ-15 выше — это рекомендация **именно под повседневный BioETL workflow**, а не универсальный рейтинг “лучших skills вообще”.
