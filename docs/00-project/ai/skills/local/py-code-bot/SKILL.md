> **DEPRECATED TOMBSTONE (DOC-GOV-03 / #6879)**
> Mirror under `docs/00-project/ai/**` only. Not an active runtime skill.

# py-code-bot (deprecated local mirror)

Do **not** use for new implementation. Route through the active orchestrator
and the relevant `py-*` wrappers.

Canonical guidance: root `AGENTS.md` and `.codex/**` runtime profiles.
