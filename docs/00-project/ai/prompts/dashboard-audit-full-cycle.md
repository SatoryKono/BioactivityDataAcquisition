  Что прочитать (не пересказывать)

  1. AGENTS.md (приоритет источников, зеркала, запрет .env, бюджеты
     техдолга)
  2. docs/00-project/NORMATIVE_SOURCES.md
  3. Только релевантные принятые ADR — по мере необходимости для SCOPE
  4. docs/00-project/ai/agents/guides/MEMORY_USAGE.md, когда в SCOPE
     попадают AI/memory-поверхности

  Git / безопасность

  • Не редактировать и не удалять чужую незакоммиченную работу
  • Никаких reset --hard, никакого force-push
  • Никогда не коммитить в main; использовать fix/<slug> (или worktree,
    если main грязный)
  • Пушить только feature-ветку; открывать PR в main
  • Предпочитать закрытие только по evidence, когда корневая причина
    продукта уже исправлена в origin/main

  Бюджеты техдолга

  • ЗАПРЕЩЕНО УВЕЛИЧИВАТЬ бюджеты техдолга / качества, исключения
    (exemptions), пороги hotspot или family caps.
  • Долг может только уменьшаться или оставаться неизменным. Не глушить
    гейты повышением лимитов.

  Ограждение .env

  • НЕ создавать, редактировать, переименовывать, перемещать,
    перезаписывать или удалять любой файл .env / .env.* без явного
    одобрения пользователя под конкретную задачу.
  • Чтение .env разрешено. Токены и секреты не должны появляться в
    коммитах, отчётах, логах или комментариях к issue.

  Контракт доказательности

  • Каждое утверждение требует доказательства на уровне файла: путь,
    символ или диапазон строк и вывод команды/сниппета, когда применимо.
  • Помечать NOT_PROVEN, когда доказательств нет; не выдумывать находки.
  • Предпочитать текущий checkout + origin/main памяти или устаревшим
    отчётам.

  Язык

  • По умолчанию отвечать оператору на русском, когда сессия на русском.
  • Код, команды, пути, идентификаторы и имена полей API сохранять в
    исходной корректной форме.

  Схема находки (finding)

  Каждая находка обязана содержать:

  ┌─────────────────┬─────────────────────────────────────────────────────┐
  │ Поле            │ Правило                                             │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ id              │ Стабильный короткий id (например, DOCS-012)         │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ path            │ Существующий файл; предпочтительно path:line или    │
  │                 │ диапазон строк                                      │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ observation     │ Одно фактическое утверждение                        │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ method          │ Команда, тест или способ инспекции                  │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ expected        │ Ожидаемое состояние                                 │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ actual          │ Наблюдаемое состояние                               │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ impact          │ Влияние на пользователя/runtime/безопасность/ops    │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ confidence      │ Диапазон high | medium | low; опционально float     │
  │                 │ confidence_score в 0..1                             │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ status          │ PROVEN | NOT_PROVEN                                 │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ priority        │ P0 | P1 | P2 | P3                                   │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ severity        │ Critical / High / Medium / Low (маппинг из          │
  │                 │ priority)                                           │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ remediation     │ Конкретный следующий шаг                            │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ effort          │ S | M | L | XL, когда известно                      │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ automation      │ Предотвращение (CI/hook/тест) или n/a               │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ automated_fix_p │ boolean; не авторизует применение фикса             │
  │ ossible         │                                                     │
  └─────────────────┴─────────────────────────────────────────────────────┘

  Правила:

  • Нет доказательства на уровне файла → NOT_PROVEN (не открывать GitHub
    issue).
  • Не выдумывать стек, SLA, цели покрытия или модели угроз; помечать как
    unknown.
  • Предпочитать текущий checkout + origin/main памяти или устаревшим
    отчётам.
  • Никогда не помещать секретные значения в находки, issues, тела PR или
    логи.

  findings.json (машиночитаемо)

  Записать UTF-8 JSON-массив (или {"findings":[...]}) в каталог отчёта
  домена.
  Рекомендуемая форма объекта:

  {
    "id": "AREA-001",
    "priority": "P1",
    "severity": "High",
    "confidence": "high",
    "confidence_score": 0.9,
    "status": "PROVEN",
    "category": "string",
    "evidence": [
      {
        "path": "path/to/file",
        "line": 42,
        "command": "safe diagnostic command",
        "observation": "what was observed"
      }
    ],
    "expected": "desired or documented state",
    "actual": "observed state",
    "impact": "specific impact",
    "root_cause": "known root cause or unspecified",
    "remediation": "smallest safe remediation",
    "effort": "S",
    "dependencies": [],
    "validation": ["exact command or assertion"],
    "automated_fix_possible": false
  }

  Сопутствующий человекочитаемый отчёт: report.md (executive summary,
  surface_score, топ-пробелы).

  Шкала аудита

  Surface score (выше = выше зрелость контроля)

  ┌─────┬─────────┬───────────────────────────────────────────────────────┐
  │ Sco │ Качеств │ Значение                                              │
  │ re  │ о       │                                                       │
  ├─────┼─────────┼───────────────────────────────────────────────────────┤
  │ 3   │ хорошо  │ Проверки воспроизводимы; существенные риски закрыты;  │
  │     │         │ есть автоматизация                                    │
  ├─────┼─────────┼───────────────────────────────────────────────────────┤
  │ 2   │ приемле │ Основной механизм корректен; локальные некритичные    │
  │     │ мо      │ пробелы                                               │
  ├─────┼─────────┼───────────────────────────────────────────────────────┤
  │ 1   │ слабо   │ Существенные пробелы, ручные этапы, дрейф или слабое  │
  │     │         │ принуждение                                           │
  ├─────┼─────────┼───────────────────────────────────────────────────────┤
  │ 0   │ неприем │ Механизм отсутствует, системно сломан или прямой риск │
  │     │ лемо    │                                                       │
  └─────┴─────────┴───────────────────────────────────────────────────────┘

  Использовать один surface_score (0–3) на аудируемую поверхность/домен в
  резюме и closeout. Не ставить то же число 0–3 на отдельные находки без
  пометки control_maturity и повторения этой легенды.

  Опциональная карта измерений (0–5)

  Некоторые campaign-kit оценивают измерения (полнота, свежесть, …) по
  шкале 0–5.
  Если используете такую карту, также выдавайте surface_score через:

  ┌─────────────────────────────┬───────────────┐
  │ Среднее по измерениям (0–5) │ surface_score │
  ├─────────────────────────────┼───────────────┤
  │ ≥ 4.5                       │ 3             │
  ├─────────────────────────────┼───────────────┤
  │ ≥ 3.0                       │ 2             │
  ├─────────────────────────────┼───────────────┤
  │ ≥ 1.5                       │ 1             │
  ├─────────────────────────────┼───────────────┤
  │ < 1.5                       │ 0             │
  └─────────────────────────────┴───────────────┘

  Или маппинг одного измерения: surface_score = min(3, floor(dim * 3 / 5)).
  Всегда указывайте, какой маппинг использован.

  BI-проверка score_1_5 (1–5, выше = лучше)

  Используется в acceptance-проверках BI-дашбордов
  (fragments/bi-check-schema.md):

  ┌───────────┬─────────────────────────┐
  │ score_1_5 │ surface_score (типично) │
  ├───────────┼─────────────────────────┤
  │ 5         │ 3                       │
  ├───────────┼─────────────────────────┤
  │ 4         │ 3 или 2                 │
  ├───────────┼─────────────────────────┤
  │ 3         │ 2                       │
  ├───────────┼─────────────────────────┤
  │ 2         │ 1                       │
  ├───────────┼─────────────────────────┤
  │ 1         │ 0                       │
  └───────────┴─────────────────────────┘

  Приоритеты kit high|medium|low маппятся на P0–P3 по bi-check-schema (не
  1:1 со score). Неверный KPI может быть score 1 + priority high, даже если
  раскладка выглядит нормально.

  Priority (меньше число = хуже)

  ┌──────┬───────┬────────────────────────────────────────────────────────┐
  │ Prio │ Значе │ Типовые критерии                                       │
  │ rity │ ние   │                                                        │
  ├──────┼───────┼────────────────────────────────────────────────────────┤
  │ P0   │ блоки │ Компрометация, потеря данных, RCE, утечка секрета,     │
  │      │ рующи │ опасный деплой, критически неверная инструкция         │
  │      │ й     │                                                        │
  ├──────┼───────┼────────────────────────────────────────────────────────┤
  │ P1   │ высок │ Высокая вероятность дефекта/инцидента, нарушение       │
  │      │ ий    │ целостности релиза, неконтролируемый критический путь  │
  ├──────┼───────┼────────────────────────────────────────────────────────┤
  │ P2   │ средн │ Существенная стоимость поддержки, нестабильность,      │
  │      │ ий    │ дрейф архитектуры/документации                         │
  ├──────┼───────┼────────────────────────────────────────────────────────┤
  │ P3   │ низки │ Локальная гигиена, удобство, форматирование,           │
  │      │ й     │ низкорисковая оптимизация                              │
  └──────┴───────┴────────────────────────────────────────────────────────┘

  Маппинг severity (BioETL closeout / issues)

  ┌──────────┬─────────────────┐
  │ Priority │ BioETL severity │
  ├──────────┼─────────────────┤
  │ P0       │ Critical        │
  ├──────────┼─────────────────┤
  │ P1       │ High            │
  ├──────────┼─────────────────┤
  │ P2       │ Medium          │
  ├──────────┼─────────────────┤
  │ P3       │ Low             │
  └──────────┴─────────────────┘

  В JSON-находках предпочитать имя поля priority для P0–P3. Если kit
  использует "severity": "P0", трактовать это как priority и всё равно
  проставлять BioETL severity.

  Вывод отчётов

  Доменные аудиты

  • Писать в reports/audit/<domain>/ (создавать по необходимости).
  • Каноничная пара: report.md + findings.json.
  • Примеры:
    • reports/audit/docs-content/
    • reports/audit/tests/
    • reports/audit/tech-debt/
    • reports/audit/repo-tree/
    • reports/audit/gha/
    • reports/audit/agents/
    • reports/audit/diagrams/
    • reports/audit/docs-pipeline/
    • reports/audit/architecture/ — разовое или последнее зеркало цикла
      архитектуры
    • reports/audit-runs/<run_id>/ — циклический аудит архитектуры
      (prompt.architecture.cycle)
    • reports/audit/bi-dashboard/ — acceptance: report.md, checks.json,
      findings.json (опц. подкаталоги visual/, layout/, data/)
    • reports/audit/grafana-panels/ — выводы инженерного цикла панелей,
      когда используется
    • reports/audit/dashboard-cycle/<run_id>/ — циклический аудит
      дашбордов (prompt.observability.dashboard-audit-cycle,
      render/density/fill + BI)
    • reports/audit/test-cycle/<run_id>/ — циклическое тестирование
      (prompt.tests.cycle)
    • reports/audit/project-domain/<run_id>/ — сводка девятидоменного
      аудита проекта (workflow project-domain-audit)

  Оркестрируемые многоитерационные прогоны

  • Использовать reports/audit-runs/<run_id>/ (не .audit-runs/ в корне
    репо).
  • Рекомендуемая раскладка:
    • run.json
    • iteration-<i>/audit.md, findings.json, plan.json, issues.jsonl,
      execution.jsonl, summary.md
    • final-summary.md

  Запрещено

  • В корне репо audit/, .audit-runs/ или разрозненные *-audit.md /
    findings.json
  • В корне _tmp_*.py, /_cr_*.py, имена устройств Windows (nul / NUL)
  • Отслеживаемые файлы в корне вне .github/root-allowlist.txt (RH5/RH6)

  Для любых вспомогательных временных файлов предпочитать scripts/** или
  reports/**.

  BioETL — последовательный запуск library/observability (v1.1)

  Не runtime SSOT. Precedence: .codex/skills/observability-dashboard/ →
  AGENTS.md → docs/01-requirements/DASHBOARD_REQUIREMENTS.md → карточки
  папки.
  Язык ответа: ru. Технические литералы не переводить.

  Params

  ┌─────────────────────┬───────────────────────────────────────────────┐
  │ Param               │ Default                                       │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ REPO                │ SatoryKono/BioactivityDataAcquisition         │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ BASE                │ origin/main                                   │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ WORK_BRANCH         │ fix/observability-seq-<shortsha> (never main) │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ SCOPE               │ grafana/dashboards                            │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ LANGUAGE            │ ru                                            │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ MONITORING          │ true                                         │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ ALLOW_ISSUE_WRITE   │ true                                          │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ ALLOW_PUSH          │ true                                          │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ ALLOW_MERGE         │ false                                         │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ ALLOW_CLOSE         │ true                                          │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ MAX_ISSUES_PER_STEP │ 8                                             │
  ├─────────────────────┼───────────────────────────────────────────────┤
  │ REQUIRE_GH_TRACKING │ true                                          │
  └─────────────────────┴───────────────────────────────────────────────┘

  TASK / MODE

  TASK: исполнить уникальные карточки папки по порядку; после карточки без
  нативного create/close — ISSUE GATE + CLOSEOUT GATE; в конце sweep.
  MODE: implement. Не commit/push в main. Чужой dirty WIP — worktree.

  Жёсткие запреты

  • Не запускать grafana-six/*.
  • Не выдумывать UID/panel/metric/SLA/live values. Shipped JSON =
    structure SSOT.
  • Не поднимать бюджеты техдолга. Не трогать .env.
  • Не стартовать docker-compose.monitoring.yml при MONITORING=false.
  • Скриншот не доказывает данные.
  • Различать: valid zero · Expected Empty · selector error · query error
    ·

    backend error · stale telemetry.
  • FACT / INFERENCE / GAP / CONTRADICTION. Issues только из PROVEN.
  • Один issue на root cause / panel-cluster.
  • Windows: только .\.venv-win\Scripts\python.exe.

  Пропуск полных дубликатов

  ┌──────────────────────────────────────────┬────────────────────────────┐
  │ Deprecated                               │ Преемник                   │
  ├──────────────────────────────────────────┼────────────────────────────┤
  │ grafana-six.evidence / .consolidate /    │ master + этот оркестратор  │
  │ .pack                                    │                            │
  ├──────────────────────────────────────────┼────────────────────────────┤
  │ grafana-six.visual                       │ grafana-audit.visual       │
  ├──────────────────────────────────────────┼────────────────────────────┤
  │ grafana-six.layout                       │ grafana-audit.layout       │
  ├──────────────────────────────────────────┼────────────────────────────┤
  │ grafana-six.data                         │ grafana-audit.data-integri │
  │                                          │ ty                         │
  ├──────────────────────────────────────────┼────────────────────────────┤
  │ grafana-six.reverify                     │ grafana-audit.regression   │
  └──────────────────────────────────────────┴────────────────────────────┘

  Пересечения (исполнять, дедупить dashboard+panel_id+root_cause):
  master vs specialists; BI-V/L/D vs grafana-audit; cycle не повторяет
  render,visual,layout,data.

  Уроки прогона 2026-08-14 (обязательны)

  1. Dedupe шире open issues. Искать closed issues + open PRs за 14 дней

     (allValue, color-background, inventory, CB mappings). Не плодить
     второй тикет и третий PR.
  2. Inventory CONTRADICTION. dashboard-inventory.yaml считает leaf;

     report-dashboard-inventory --check считает leaf+row. Не «чинить» оба
     числа в разные стороны. Один formula на прогон.
  3. check-dashboard-visual-semantics PASS ≠ нет visual дефектов.

     Table-wide color-background и allValue=$__all на main проходили
     этот гейт. Смотри JSON напрямую + DASH-AUTO-* ниже.
  4. Closeout vs origin/main. Issue закрывать как DONE только если

     acceptance виден на origin/main или оператор явно принял PR-head
     как evidence. Иначе BLOCKED + ссылка на PR. Уже закрытые не reopen.
  5. r2 без уникальных коммитов не пушить. Candidate для regression —

     существующие fix PR, не пустая ветка.

  Каркас шага (1–8)

  A. Прочитать карточку + includes. Read-only карточка не правит JSON во
  время аудита.
  B. Dedupe по ledger + gh issue list --state all + open PRs.
  C. ISSUE GATE только если нет native create
     (dashboard-panel-audit при REQUIRE_GH_TRACKING=true,
     dashboard-audit-cycle при ALLOW_ISSUE_WRITE=true).
     Иначе ≤ MAX_ISSUES_PER_STEP на PROVEN P0–P2 (P3 — только если
  блокирует роль).
  D. CLOSEOUT GATE только если нет native close. Fix на WORK_BRANCH,
     не дублируя чужой open PR. ALLOW_MERGE=false по умолчанию.
  E. NO_ACTIONABLE_FINDINGS → не выдумывать работу.
  F. В ledger обязателен исход шага, даже issues=0.

  Последовательность

  ┌─────┬─────────────────────────┬───────────────────────────────────────┐
  │ Ste │ Card                    │ Notes                                 │
  │ p   │                         │                                       │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 0   │ Shared inventory        │ 7 UID; leaf и leaf+row; не создавать  │
  │     │                         │ issues                                │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 1   │ grafana-audit.master    │ затем wrapper issues/close            │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 2   │ grafana-audit.visual    │ wrapper                               │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 3   │ grafana-audit.layout    │ wrapper                               │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 4   │ grafana-audit.data-inte │ live query только при MONITORING=true │
  │     │ grity                   │                                       │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 5   │ bi-dashboard-acceptance │ DEPTH=detailed если MONITORING=false  │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 6   │ dashboard-panel-audit   │ native 3–5; CYCLE_COUNT=1             │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 7   │ dashboard-audit-cycle   │ N=1, CONTOURS=density,fill,pipeline   │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 8   │ grafana-audit.regressio │ только если есть candidate ≠ BASE     │
  │     │ n                       │                                       │
  ├─────┼─────────────────────────┼───────────────────────────────────────┤
  │ 9   │ Final sweep             │ все issue# из ledger                  │
  └─────┴─────────────────────────┴───────────────────────────────────────┘

  Статические гейты (step 0/7, не вместо карточек):

  python -m scripts.engineering.qa report-dashboard-inventory --check
  --json
  python -m scripts.engineering.qa check-dashboard-visual-semantics
  python -m scripts.engineering.qa check-dashboard-performance-budgets

  Артефакты

  reports/audit/observability-seq/<UTC>-obs-seq-<shortsha>/
  00-inventory.md … 09-final-sweep.md, ledger.jsonl, findings.json,
  report.md.

  Success

  • grafana-six не запускался
  • 8 карточек executed или SKIP с причиной
  • у шагов 1–8 есть issue/close исход в ledger
  • нет открытых issues этого прогона без BLOCKED
  • нет commit в main, нет правок .env

  Appendix — DASH-AUTO (предложения, ещё не SSOT)

  Не выдавать за действующий DASH-*. На шагах 2/3/4/7 отметить
  PASS/FAIL/GAP, если проверка дешёвая из JSON.

  ┌──────────┬────────────────────────────────────────────────────────────┐
  │ ID       │ Check (JSON)                                               │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ includeAll PromQL var → allValue is .*, not $__all/null    │
  │ O-001    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ table defaults.cellOptions ≠ color-background              │
  │ O-002    │ (override-only)                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ data-bearing title starts with                             │
  │ O-003    │ Monitor/Inspect/Track/Compare/Review/Navigate/Understand/S │
  │          │ tart                                                       │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ data-bearing panel has non-empty description               │
  │ O-004    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ first-window current-status expr must not contain $__range │
  │ O-005    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ range/track panel has $__range or range wording            │
  │ O-006    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ panel id unique per dashboard                              │
  │ O-007    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ gridPos: w>0, h>0, x>=0, x+w<=24, no top-level overlap     │
  │ O-008    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ no unexplained top-level y gap > 1                         │
  │ O-009    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ no placeholder titles (Panel Title, TODO, empty)           │
  │ O-010    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ datasource type ∈ prometheus / infinity / grafana          │
  │ O-011    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ domain-enum stats (CB, raw health) have text mappings      │
  │ O-012    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ fail-closed status stats set noValue + null→UNKNOWN        │
  │ O-013    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ dashboard links include ${__url_time_range} or from/to     │
  │ O-014    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ first-window text panel body ≤ 800 chars                   │
  │ O-015    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ First Action / CTA ≤ 4 rows or ≤ 4 data links              │
  │ O-016    │                                                            │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ inventory panel_count formula is one (leaf XOR leaf+row)   │
  │ O-017    │ and matches                                                │
  ├──────────┼────────────────────────────────────────────────────────────┤
  │ DASH-AUT │ timeseries tooltip mode matches role (multi/desc vs        │
  │ O-018    │ single)                                                    │
  └──────────┴────────────────────────────────────────────────────────────┘