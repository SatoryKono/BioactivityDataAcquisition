# Agent Scripts — Archived (2026-09-04)

This directory formerly held legacy generators (`py-config-bot-*.py`, `architecture-techdebt-automation.py`, `diagrams/`). They were first moved to `docs/99-archive/agents-scripts-2026-09/`; archived Python executables were later removed to keep executable code out of `docs/99-archive/` (Wave 7 follow-up, RF-005).

Use canonical generators in `scripts/ai/` and `scripts/engineering/` instead.

See `docs/99-archive/agents-scripts-2026-09/README.md` for the retained archive inventory and retirement history.
