# Aliases (Compatibility Layer)

This folder documents compatibility aliases for relocated files under `docs/00-project/ai/agents/`.

Current policy:

1. Canonical files live in `guides/`, `runtime/`, `policy/`, `audit/`, and `snapshots/`.
2. Legacy paths remain as deprecated stubs for compatibility.
3. Deprecated stubs should be removed after one release window once inbound links are migrated.
