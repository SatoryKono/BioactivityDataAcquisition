# План рефакторинга BioETL

**Версия:** 2.0
**Дата:** 2025-12-16

> Краткая версия плана. Полный анализ см. в [ARCHITECTURE_REVIEW.md](./ARCHITECTURE_REVIEW.md)

---

## Текущее состояние (Обновлено v2.0)

| Метрика | v1.0 | v2.0 | Изменение |
|---------|------|------|-----------|
| **Интегральный балл** | 7.34 / 10 | **7.61 / 10** | ⬆️ +0.27 |
| **Уровень** | 🟡 Удовлетворительный | 🟡 Удовлетворительный | → |
| **Целевой балл** | 8.52 / 10 | 8.52 / 10 | → |
| **Целевой уровень** | 🟢 Отличный | 🟢 Отличный | → |
| **Выполнено задач** | 0/10 | **5/10** | ⬆️ +50% |

---

## Приоритизированный план (Обновлено v2.0)

### Фаза 1: Критические исправления (P0) — ⏳ В ПРОЦЕССЕ

| ID | Задача | Файлы | Сложность | Эффект | Статус |
|----|--------|-------|-----------|--------|--------|
| **R1** | Выделить Composition Root (`bootstrap.py`) | +`bootstrap.py`, `cli.py`, `chembl_activity.py` | Средняя | +0.3 | ❌ TODO |
| **R2** | Извлечь `StorageAdapter` в модуль | +`unified_storage.py`, `chembl_activity.py` | Низкая | +0.1 | ❌ TODO |

**Результат:** Устранение нарушений DI, чистые границы слоёв.

---

### Фаза 2: Архитектурные улучшения (P1) — ✅ ВЫПОЛНЕНО

| ID | Задача | Файлы | Сложность | Эффект | Статус |
|----|--------|-------|-----------|--------|--------|
| **R3** | Декомпозиция `BasePipeline` | +`lock_manager.py`, +`checkpoint_manager.py`, +`record_processor.py`, `base.py` | Высокая | +0.4 | ⚠️ Частично (base.py 314 LOC) |
| **R4** | Убрать ignore из import-linter | `.importlinter` | Средняя | +0.2 | ✅ ГОТОВО |

**Результат:** ~~BasePipeline < 100 LOC~~ Компоненты выделены, import-linter без исключений.

---

### Фаза 3: Устранение дублирования (P2) — ✅ ЧАСТИЧНО

| ID | Задача | Файлы | Сложность | Эффект | Статус |
|----|--------|-------|-----------|--------|--------|
| **R5** | DRY для конфигурации AWS в CLI | `cli.py`, `bootstrap.py` | Низкая | +0.1 | ❌ TODO (зависит от R1) |
| **R6** | Консолидировать observability | `observability/`, `infrastructure/observability/` | Низкая | +0.05 | ✅ ГОТОВО |

**Результат:** ~~Единая точка конфигурации,~~ Один модуль observability.

---

### Фаза 4: Структурные улучшения (P3) — ❌ НЕ НАЧАТО

| ID | Задача | Файлы | Сложность | Эффект | Статус |
|----|--------|-------|-----------|--------|--------|
| **R7** | Добавить слой `interfaces/` | +`interfaces/cli/`, `cli.py` → `interfaces/` | Средняя | +0.15 | ❌ TODO |
| **R8** | Улучшить error classification | +`exceptions.py`, `base.py`, adapters | Низкая | +0.1 | ❌ TODO |

**Результат:** Явный слой интерфейсов, типобезопасные исключения.

---

### Фаза 5: Чистка (P4) — ✅ ВЫПОЛНЕНО

| ID | Задача | Файлы | Сложность | Эффект | Статус |
|----|--------|-------|-----------|--------|--------|
| **R9** | Удалить `services/` | `services/` | Минимальная | +0.02 | ✅ ГОТОВО |
| **R10** | Исправить deprecated `datetime.utcnow()` | `chembl_activity.py` | Минимальная | +0.02 | ✅ ГОТОВО |

**Результат:** Чистая кодовая база без мёртвого кода.

---

## Диаграмма зависимостей задач

```
R1 ─────┬──► R4 ──► R5
        │
R2 ─────┤
        │
        └──► R3

R6 (независимая)

R7 (независимая, после R1)

R8 (независимая)

R9, R10 (независимые)
```

---

## Критерии готовности (Обновлено v2.0)

### Фаза 1 завершена когда:
- [ ] `bootstrap.py` создан с `AppConfig` и `Container`
- [ ] CLI использует `bootstrap()` вместо inline создания адаптеров
- [ ] `unified_storage.py` создан
- [ ] Вложенный `StorageAdapter` удалён из `chembl_activity.py`
- [ ] Все тесты проходят

### Фаза 2 завершена когда:
- [x] `LockManager` выделен в отдельный класс ✅
- [x] `CheckpointManager` выделен в отдельный класс ✅
- [x] `RecordProcessor` выделен в отдельный класс ✅
- [ ] `BasePipeline` < 100 строк (текущий: 314 LOC)
- [x] `.importlinter` не содержит `ignore_imports` ✅
- [x] `lint-imports` проходит без ошибок ✅

### Фаза 3 завершена когда:
- [ ] Код конфигурации AWS встречается 1 раз
- [x] Все CLI команды используют общий config модуль (частично через `bioetl.config`)
- [x] Одна директория observability ✅

### Фаза 5 завершена когда:
- [x] `services/` удалена ✅
- [x] `datetime.utcnow()` заменено на `datetime.now(UTC)` ✅

### Весь рефакторинг завершён когда:
- [ ] Интегральный балл ≥ 8.5 (текущий: 7.61)
- [ ] Все R1-R10 выполнены (5/10 выполнено)
- [ ] Покрытие тестами ≥ 85%
- [ ] Все CI checks проходят

---

## Рекомендуемый порядок выполнения (Обновлено v2.0)

```
✅ ВЫПОЛНЕНО:
├── [R3] Декомпозиция BasePipeline - компоненты созданы
├── [R4] Import-linter - исключения удалены
├── [R6] Консолидация observability
├── [R9] Удаление services/
└── [R10] Исправление datetime.utcnow()

⏳ ОСТАВШИЕСЯ ЗАДАЧИ:
1. [R2] StorageAdapter - быстрая победа
   │
2. [R1] Composition Root - ключевое изменение
   │
3. [R5] DRY в CLI - зависит от R1
   │
4. [R3] Продолжить: base.py < 100 LOC
   │
5. [R7, R8] Параллельно:
   ├── interfaces/ слой
   └── exceptions.py
```

---

## Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Ломаются тесты после R1 | Высокая | Среднее | Добавить e2e тест до начала |
| R3 слишком большой | Средняя | Высокое | Разбить на 3 подзадачи |
| Регрессия в production | Низкая | Высокое | Feature flags, canary deployment |

---

## Метрики для мониторинга

```bash
# Проверка размера модулей
find src -name "*.py" -exec wc -l {} \; | sort -rn | head -10

# Проверка сложности
radon cc src -a -s

# Проверка дублирования
jscpd src --min-lines 5 --min-tokens 50

# Проверка архитектурных контрактов
lint-imports --config .importlinter
```

---

**Ссылки:**
- [Полный архитектурный обзор](./ARCHITECTURE_REVIEW.md)
- [RULES.md](../../RULES.md)
- [REQUIREMENTS.md](../../REQUIREMENTS.md)
