version: 1
source: grafana/dashboards/*.json
epic: "#6570"
phase: "#6574"
mode: error
calibration_note: >-
  2026-08-07 Phase 2 (#6574): first-screen mega-expr diet — DQ reasons and
  Overview domain tracks moved under collapsed progressive-disclosure rows;
  timeseries maxDataPoints/interval capped; budget tightened toward ≤200 char
  first-screen expressions and ≤6 first-load PromQL (worst board).
  first_screen_y_max is the PromQL/HTTP selection window (FIRST_LOAD_Y_MAX),
  not the visual fold (see layout-budgets.yaml:first_window_y).
# PromQL/HTTP first-load selection window (FIRST_LOAD_Y_MAX). Not the visual
# fold — that is first_window_y=18 in layout-budgets.yaml.
first_screen_y_max: 28
budgets:
  worst_first_load_promql: 6
  overview_first_load_promql: 6
  first_paint_ops_http_panels: 5
  max_first_screen_expr_chars: 200
  first_screen_range_refs: 0
  refresh_seconds: 60
  dual_status_identical_pairs: 0
  max_primary_dashboard_count: 7
  max_nav_targets_per_dashboard: 6
baseline_2026_07_27_pre_diet:
  note: Offline JSON walk before
  worst_first_load_promql: 8
  overview_first_load_promql: 4
  first_paint_ops_http_panels: 12
  max_first_screen_expr_chars: 2960
  first_screen_range_refs: 12
  refresh_seconds: 30
  dual_status_identical_pairs: 2
  primary_dashboard_count: 7
primary_uids:
  - bioetl-control-plane-v1
  - bioetl-overview-v2
  - bioetl-runtime
  - bioetl-provider-health-v2
  - bioetl-dq-v2
retired_uids: []
expr_length_exceptions: []
