# 00 Duplication Reduction Roadmap

## Статус текущих мер

### 📋 Задокументировано (код отсутствует)

| Мера | Документация | Код | Компонент |
|------|--------------|-----|-----------|
| Базовый пайплайн с оркестрацией ETL | ✅ | ❌ | `PipelineBase` |
| ChEMBL-специфичный базовый пайплайн | ✅ | ❌ | `ChemblBasePipeline` |
| Унифицированный API клиент | ✅ | ❌ | `UnifiedAPIClient` |
| Унифицированный writer | ✅ | ❌ | `UnifiedOutputWriter` |
| Базовый нормализатор для ChEMBL | ✅ | ❌ | `BaseChemblNormalizer` |
| 28 ABC для стандартизации | ✅ | ❌ | `docs/reference/abc/` |

### ✅ Реализовано

| Мера | Статус | Компонент |
|------|--------|-----------|
| Профили конфигураций | ✅ Реализовано | `configs/profiles/` |
| Иерархия конфигураций | ✅ Реализовано | `extends` в YAML |

### ⏳ Требует реализации в коде

| Задача | Приоритет | Оценка |
|--------|-----------|--------|
| Реализация `PipelineBase` | Критичный | 3 дня |
| Реализация `ChemblBasePipeline` | Критичный | 2 дня |
| Реализация `UnifiedAPIClient` | Критичный | 3 дня |
| Реализация `BaseChemblNormalizer` | Высокий | 2 дня |
| Реализация `ConfigResolver` с поддержкой `extends` | Критичный | 2 дня |
| Default/Impl для ABC | Средний | 5 дней |

## План снижения дублирования по этапам

### Этап 1: Базовая инфраструктура (критичный)

**Цель:** Общие компоненты для всех пайплайнов

**Задачи:**
1. Реализовать `ConfigResolver` с поддержкой профилей
2. Реализовать `PipelineBase` с оркестрацией стадий
3. Реализовать `UnifiedAPIClient` с retry/rate limiting
4. Реализовать `UnifiedOutputWriter` с атомарной записью
5. Реализовать `SchemaRegistry` для централизованного управления схемами

**Метрики успеха:**
- ✅ Все пайплайны используют `PipelineBase` → 0 дублирования оркестрации
- ✅ Все API-вызовы через `UnifiedAPIClient` → 0 дублирования HTTP-логики
- ✅ Все записи через `UnifiedOutputWriter` → 0 дублирования write-логики

**Срок:** 2 недели

### Этап 2: ChEMBL-специфичная унификация (высокий)

**Цель:** Общие компоненты для всех ChEMBL-пайплайнов

**Задачи:**
1. Реализовать `ChemblBasePipeline` с общей инициализацией
2. Реализовать `BaseChemblNormalizer` с типовой нормализацией
3. Реализовать `ChemblExtractionService` с версионированием релизов
4. Реализовать `ChemblWriteService` с QC-сайдкарами
5. Создать базовую `ChemblSchema` для общих полей

**Метрики успеха:**
- ✅ Все ChEMBL-пайплайны наследуются от `ChemblBasePipeline` → сокращение LOC на 40%
- ✅ Все ChEMBL-нормализаторы используют `BaseChemblNormalizer` → сокращение дублирования на 60%
- ✅ Все ChEMBL-схемы наследуются от `ChemblSchema` → единообразие метаданных

**Срок:** 2 недели

### Этап 3: ABC Default/Impl (средний)

**Цель:** Готовые реализации для типовых задач

**Задачи:**
1. Реализовать Default для 28 ABC (может быть stub с NotImplementedError)
2. Реализовать типовые Impl:
   - `OffsetPaginatorImpl`
   - `ExponentialBackoffRetryImpl`
   - `TokenBucketRateLimiterImpl`
   - `PanderaValidatorImpl`
3. Зарегистрировать в `abc_impls.yaml`
4. Создать фабрики: `default_<domain>_<entity>()`

**Метрики успеха:**
- ✅ 100% ABC имеют Default
- ✅ 80% типовых задач покрыты готовыми Impl
- ✅ Новые пайплайны переиспользуют Impl без копипаста

**Срок:** 1 неделя

### Этап 4: Композиция схем (средний)

**Цель:** Переиспользование базовых схем для специфичных

**Задачи:**
1. Создать `BaseChemblSchema` с общими полями (chembl_release, extracted_at, hash_row)
2. Рефакторить существующие схемы на наследование от базовой
3. Создать `BaseDocumentSchema` для документов (title, year, doi)
4. Создать `BaseMoleculeSchema` для молекул (chembl_id, smiles, inchi)

**Метрики успеха:**
- ✅ Все ChEMBL-схемы наследуются от `BaseChemblSchema`
- ✅ Общие поля описаны в одном месте
- ✅ Добавление нового поля в базовую схему → автоматически во всех специфичных

**Срок:** 3 дня

### Этап 5: Аудит и рефакторинг дублирования (непрерывный)

**Цель:** Постоянный мониторинг и устранение дублирования

**Инструменты:**
- `grep -r "def <function_name>" src/` — поиск дублированных функций
- `diff` между конфигами — поиск дублированных параметров
- Code review checklist с вопросом "Есть ли готовый компонент?"

**Метрики отслеживания:**
- **LOC в пайплайнах:** должен снижаться при добавлении новых сущностей
- **Количество строк в пайплайновых YAML:** должно быть минимальным (только специфика entity)
- **Coverage общих компонентов:** должен быть > 90%
- **Отношение страниц docs/common/ к docs/entity/:** должно расти

**Срок:** Постоянно

## Чеклист для новых пайплайнов

Перед созданием нового пайплайна **обязательно** проверить:

- [ ] Наследуется от `PipelineBase` или `ChemblBasePipeline`?
- [ ] Использует `UnifiedAPIClient` для HTTP-запросов?
- [ ] Использует `UnifiedOutputWriter` для записи?
- [ ] Использует `BaseChemblNormalizer` для нормализации?
- [ ] Конфиг наследуется от `chembl_default` профиля?
- [ ] Не дублирует `pagination`, `client`, `storage` параметры?
- [ ] Схема наследуется от `BaseChemblSchema`?
- [ ] Нет прямого использования `requests` в коде?
- [ ] Нет дублирования функций нормализации из других пайплайнов?
- [ ] Все повторяющиеся трансформации вынесены в `TransformerABC`?

Если хотя бы один пункт не выполнен — **рефакторинг обязателен**.

## Антипаттерны (запрещено)

### ❌ Прямое использование requests

```python
# Плохо
import requests
response = requests.get(url, timeout=30)
```

```python
# Хорошо
from bioetl.clients.unified import UnifiedAPIClient
client = UnifiedAPIClient(...)
response = client.fetch_one(request)
```

### ❌ Дублирование параметров в конфигах

```yaml
# Плохо: activity.yaml, assay.yaml, target.yaml — везде одинаково
pagination:
  limit: 1000
  offset: 0
```

```yaml
# Хорошо: вынесено в chembl_default.yaml
extends: chembl_default
# pagination наследуется автоматически
```

### ❌ Копипаста нормализации

```python
# Плохо: ActivityNormalizer, AssayNormalizer — одинаковый код
def normalize_date(value):
    # ... 10 строк кода ...
```

```python
# Хорошо: вынесено в BaseChemblNormalizer
class ActivityNormalizer:
    def __init__(self):
        self.base = BaseChemblNormalizer(...)
    
    def normalize(self, df):
        return self.base.normalize(df)  # Переиспользование
```

### ❌ Создание нового слоя без необходимости

```python
# Плохо: новый слой для метрик
class MetricsCollectorLayer:
    pass
```

```python
# Хорошо: использование PipelineHookABC
class MetricsHook(PipelineHookABC):
    def on_stage_complete(self, stage, result):
        self.collect_metrics(stage, result)
```

## Метрики прогресса (KPI)

Отслеживать ежемесячно:

| Метрика | Базовая линия | Цель через 3 месяца |
|---------|---------------|---------------------|
| Средний LOC на пайплайн | 500 | 200 |
| Строк в пайплайновом YAML | 300 | 100 |
| Coverage общих компонентов | 60% | 90% |
| Количество ABC с реализацией | 5 | 28 |
| Время добавления нового пайплайна | 5 дней | 1 день |

## Related Documents

- `docs/project/04-architecture-and-duplication-reduction.md` — детальные принципы
- `configs/README.md` — архитектура конфигураций
- `docs/project/00-rules-summary.md` — общие правила
- `docs/project/00-rules-summary.md` (раздел 2) — политика ABC/Default/Impl

