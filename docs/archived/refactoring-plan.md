# Refactoring Plan (Archived)

This file is intentionally minimal. The original refactoring plan was removed from the repo,
but it is still referenced by `RULES.md`, `CLAUDE.md`, and `AGENT.md` for historical checks.

## Status

- **State**: Archived / not actively maintained
- **Purpose**: Preserve link integrity for legacy references

## Guidance

If you need an up-to-date refactoring backlog, use the current issue tracker or create a
new plan document and update the references in `RULES.md`, `CLAUDE.md`, and `AGENT.md`.
