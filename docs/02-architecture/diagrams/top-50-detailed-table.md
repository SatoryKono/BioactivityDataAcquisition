# TOP-50 Diagram Detailed Table

*Generated: 2026-02-17 | Scoring: RULES.md v5.19 | Codebase: 534 Python files*

## Scoring Formula

```
Priority = (Arch×2 + Doc×1.5 + Freq×1.5 + Complex×2 + Coverage×1) / 8
```

---

| # | Название | Тип | Категория | Priority | Arch | Doc | Freq | Complex | Coverage | Обоснование важности | Классы/компоненты на диаграмме |
|---|----------|-----|-----------|----------|------|-----|------|---------|----------|----------------------|-------------------------------|
| 1 | Hexagonal Architecture — Ports and Adapters Overview | flowchart | Architecture | 9.38 | 10 | 10 | 8 | 9 | 10 | Ключевая диаграмма для понимания всей архитектуры BioETL. Показывает все 24 доменных порта (Protocol) и их инфраструктурные реализации. Без неё невозможно быстро понять, какой адаптер реализует какой контракт. Наиболее полезна новым разработчикам и архитекторам при code review. | `DataSourcePort`, `StoragePort`, `LockPort`, `CheckpointPort`, `QuarantinePort`, `MetricsPort`, `TracingPort`, `LoggerPort`, `ChemblAdapter`, `PubChemAdapter`, `UniProtAdapter`, `MemoryLock`, `BronzeWriter`, `SilverWriter`, `GoldWriter` |
| 2 | Five-Layer Import Matrix Enforcement | flowchart | Architecture | 9.00 | 10 | 9 | 9 | 8 | 9 | Визуализирует правило ARCH-001 — самое критичное архитектурное ограничение. Показывает, какие импорты разрешены между 5 слоями, а какие запрещены. Нарушение этого правила блокирует PR через import-linter. Необходима каждому разработчику для повседневной работы. | `domain`, `application`, `infrastructure`, `composition`, `interfaces`, `import-linter`, `BaseHttpAdapter`, `PipelineRunner`, `GenericPipelineFactory` |
| 3 | Composition Root Wiring — Full DI Graph | flowchart | Architecture | 9.00 | 9 | 9 | 7 | 10 | 10 | Самая сложная концепция проекта — полный граф Dependency Injection. Показывает, как composition layer соединяет десятки фабрик и портов в работающее приложение. Без визуализации практически невозможно отследить, какой адаптер куда инжектируется. Критична для техлидов и при добавлении новых провайдеров. | `GenericPipelineFactory`, `RunnerFactory`, `ServicesBuilder`, `StorageFactory`, `HttpClientFactory`, `DataSourceFactory`, `DQServicesFactory`, `ProviderRegistry`, `PipelineRegistry`, `ObservabilityBundle`, `PipelineServices` |
| 4 | Composite Pipeline Full Workflow — Seed to Gold | flowchart | Composite | 8.88 | 9 | 9 | 7 | 10 | 9 | Наиболее сложный архитектурный паттерн проекта (ADR-026). Многофазный workflow: Seed → Dependencies → Fan-Out Enrichers → Merge → Gold. Без диаграммы понять порядок и взаимосвязи фаз крайне затруднительно. Необходима при работе с publication enrichment pipeline. | `CompositePipelineRunner`, `EnrichmentCoordinator`, `DependencyCoordinator`, `MergeService`, `KeyExtractorService`, `CompositeConfig`, `SeedConfig`, `EnricherConfig`, `ColumnOrderer`, `EnricherDeduplicator` |
| 5 | Port-to-Adapter Mapping Table Diagram | flowchart | Architecture | 8.63 | 9 | 10 | 8 | 7 | 10 | Справочная диаграмма №1 — "где реализован интерфейс X?". Маппинг всех 24 портов на конкретные адаптеры с указанием модулей. Самая часто используемая при трассировке кода. Полезна всем участникам проекта — от джуниоров до архитекторов. | `DataSourcePort`, `FilterableDataSourcePort`, `StoragePort`, `LockPort`, `CheckpointPort`, `QuarantinePort`, `MetricsPort`, `LoggerPort`, `TracingPort`, `MemoryLock`, `LocalCheckpoint`, `UnifiedQuarantine`, `PrometheusMetrics`, `StructlogLogger`, `NoOpTracing` |
| 6 | Pipeline Run Lifecycle — From Config to Completion | stateDiagram | Lifecycle | 8.56 | 9 | 9 | 8 | 9 | 7 | Полная state machine выполнения pipeline с 9+ состояниями и переходами по ошибкам. Расширяет существующую диаграмму 05-pipeline-lifecycle-states деталями PreflightService и PostrunService. Критична для дебага зависших pipeline и понимания lifecycle hooks. | `PipelineRunner`, `PipelineRunState`, `StageStatus`, `PreflightService`, `BatchExecutor`, `PostrunService`, `LockManager`, `CheckpointManager`, `PipelineObserver` |
| 7 | Record Processing Pipeline — Single Record Journey | flowchart | DataFlow | 8.56 | 7 | 10 | 9 | 9 | 8 | Лучшая onboarding-диаграмма — "путь одной записи через систему". От REST API ответа до Gold таблицы с маршрутизацией в Quarantine. Формирует ментальную модель, которую разработчики используют ежедневно. Решает проблему понимания трансформационного конвейера. | `RecordProcessor`, `BaseTransformer`, `BatchTransformer`, `BronzeWriter`, `SilverWriter`, `GoldWriter`, `UnifiedQuarantine`, `QuarantineManager`, `ETLRecordSchema` |
| 8 | CLI Run Command → PipelineRunner Full Interaction | sequenceDiagram | Interaction | 8.50 | 7 | 10 | 10 | 8 | 8 | Отвечает на главный вопрос: "что происходит, когда я запускаю `bioetl run chembl_activity`?". Полная цепочка от CLI до финализации. Самая частая операция в системе — каждый запуск pipeline проходит этот путь. Полезна для всех разработчиков. | `cli/commands/run.py`, `bootstrap_pipeline()`, `GenericPipelineFactory`, `RunnerFactory`, `PipelineRunner`, `PreflightService`, `BatchExecutor`, `PostrunService`, `PipelineObserver` |
| 9 | Batch Processing Flow — Extract to Write | sequenceDiagram | DataFlow | 8.44 | 8 | 9 | 10 | 8 | 7 | Ядро обработки данных — внутренний цикл, выполняемый для каждого batch. Показывает взаимодействие BatchExecutor с трансформером, валидатором и writer'ами. Самый часто исполняемый код в runtime. Критична для оптимизации производительности. | `BatchExecutor`, `BatchTransformer`, `BatchWriter`, `BatchMetricsRecorder`, `BatchTracingManager`, `TransformResult`, `BatchResult` |
| 10 | Composition Layer Bootstrap Sequence | flowchart | Architecture | 8.44 | 8 | 9 | 8 | 9 | 8 | Показывает пошаговую сборку системы от CLI до готового PipelineRunner. Многошаговая ассемблировка: logger → storage → HTTP → adapter → services → runner. Необходима при модификации bootstrap или добавлении провайдеров. | `bootstrap/runtime/assembly.py`, `BootstrapLogger`, `StorageFactory`, `HttpClientFactory`, `DataSourceFactory`, `ServicesBuilder`, `RunnerFactory`, `ObservabilityBundle` |
| 11 | Architecture Principles Mind Map | mindmap | Architecture | 8.38 | 9 | 10 | 6 | 8 | 9 | Единственная mindmap в проекте — обзор всех архитектурных принципов. Hexagonal Architecture, DI, Medallion, DDD, Composition Root, Local-Only — всё в одном месте. Идеальна для презентаций и быстрого онбординга. Решает проблему фрагментарности архитектурной документации. | `DataSourcePort`, `StoragePort`, `PipelineRunner`, `PipelineRun`, `Batch`, `QuarantineEntry`, `GenericPipelineFactory`, `MemoryLock`, `BronzeWriter`, `SilverWriter`, `GoldWriter` |
| 12 | CLI Entry Point to Pipeline Execution Full Chain | flowchart | Architecture | 8.31 | 7 | 10 | 9 | 8 | 8 | Полная цепочка от click-команды до exit code. Включает парсинг аргументов, bootstrap, assembly, runner.run(), finalize. Документирует все точки входа и выхода. Наиболее полезна DevOps-инженерам и разработчикам CLI. | `cli/commands/run.py`, `ExitCode`, `PipelineRunnerService`, `bootstrap_pipeline()`, `PipelineRunner`, `PipelineRunResult`, `ShutdownService` |
| 13 | Runtime Assembly Sequence — bootstrap/runtime/assembly.py | sequenceDiagram | Architecture | 8.25 | 8 | 9 | 7 | 9 | 8 | Пошаговая sequence assembly.py — от создания logger до полностью собранного runner. Каждый шаг — вызов конкретной фабрики. Показывает порядок зависимостей при инициализации. Решает проблему "что от чего зависит при bootstrap". | `assembly.py`, `BootstrapLogger`, `StorageAdapter`, `StorageFactory`, `HttpClientFactory`, `DataSourceFactory`, `BaseServicesFactory`, `RunnerFactory`, `PipelineServices` |
| 14 | Medallion Architecture Invariants | flowchart | Architecture | 8.19 | 10 | 8 | 7 | 9 | 5 | Критическое правило ARCH-007: REBUILD/BACKFILL → clear Silver+Gold, INCREMENTAL → clear neither. Нарушение приводит к потере данных или дубликатам. Показывает enforcement через MedallionLifecycleService. Полезна для понимания data safety guarantees. | `MedallionLifecycleService`, `RunType`, `ClearResult`, `SilverWriter`, `GoldWriter`, `PipelineRunner` |
| 15 | Application Core Component Collaboration | flowchart | Architecture | 8.19 | 8 | 9 | 8 | 8 | 8 | Показывает внутреннюю организацию application/core/ — как PipelineRunner оркестрирует все сервисы. Ключевые связи: Runner → LockManager, PreflightService, BatchExecutor, PostrunService, CheckpointManager. Решает проблему понимания orchestration layer. | `PipelineRunner`, `LockManager`, `PreflightService`, `BatchExecutor`, `PostrunService`, `CheckpointManager`, `HeartbeatTask`, `PipelineObserver`, `PipelineServices` |
| 16 | Error Classification Decision Tree — Full Logic | flowchart | ErrorHandling | 8.06 | 8 | 9 | 8 | 9 | 5 | Полное дерево решений классификации ошибок. HTTP status → Critical/Recoverable/DataQuality с конкретными кодами (401→CRITICAL, 429→RECOVERABLE, 500→RECOVERABLE). Необходима при добавлении обработки новых типов ошибок. Полезна SRE и backend-разработчикам. | `ErrorCategory`, `ErrorService`, `BioETLError`, `CriticalError`, `RecoverableError`, `DataQualityError`, `NetworkError`, `CircuitBreakerOpenError`, `RetryExhaustedError` |
| 17 | PipelineRunner Internal Component Diagram | classDiagram | Component | 8.06 | 8 | 9 | 8 | 8 | 7 | Class diagram PipelineRunner с полным списком инжектируемых зависимостей и методов. Показывает _lock_manager, _preflight, _batch_executor, _postrun, _checkpoint, _observer — все DI-связи. Референс для рефакторинга и расширения runner. | `PipelineRunner`, `LockManager`, `PreflightService`, `BatchExecutor`, `PostrunService`, `CheckpointManager`, `HeartbeatTask`, `PipelineObserver`, `ShutdownService`, `PipelineServices` |
| 18 | Fan-Out/Fan-In Pattern — Composite Pipeline Enrichment | flowchart | Pattern | 8.00 | 9 | 8 | 6 | 9 | 7 | Паттерн параллельного обогащения данных: Seed → extract keys → asyncio.gather(enricher1, enricher2, ...) → MergeService. Критический для publication composite pipeline. Показывает, как EnrichmentCoordinator управляет параллельными задачами. | `EnrichmentCoordinator`, `KeyExtractorService`, `MergeService`, `EnricherConfig`, `CompositePipelineRunner`, `asyncio.gather`, `EnricherDeduplicator` |
| 19 | Cross-Provider Data Enrichment Flow — Publication | flowchart | DataFlow | 8.00 | 8 | 8 | 6 | 9 | 9 | Конкретный data flow обогащения publication: ChEMBL document → DOIs/PMIDs → CrossRef + PubMed + OpenAlex + SemanticScholar → Merge → Gold. Охватывает 5 провайдеров. Решает проблему понимания cross-provider integration. | `CrossRefAdapter`, `PubMedAdapter`, `OpenAlexAdapter`, `SemanticScholarAdapter`, `MergeService`, `ColumnOrderer`, `CompositePublicationGoldSchema` |
| 20 | Template Method Pattern — BaseTransformer._transform_impl() | classDiagram | Pattern | 7.94 | 8 | 9 | 8 | 8 | 6 | Показывает core design pattern проекта: BaseTransformer определяет skeleton (transform()), подклассы реализуют _transform_impl(). 23+ конкретных трансформера. Необходима при создании нового pipeline — разработчик должен понимать контракт наследования. | `BaseTransformer`, `BaseChemblTransformer`, `ActivityTransformer`, `MoleculeTransformer`, `AssayTransformer`, `BasePublicationTransformer`, `CrossRefPublicationTransformer`, `PubMedPublicationTransformer` |
| 21 | YAML Configuration Resolution Chain | flowchart | Architecture | 7.94 | 8 | 9 | 8 | 8 | 6 | Цепочка резолюции конфигурации: _base.yaml → provider.yaml → entity.yaml → dq overrides → filter rules. Показывает наследование и мерж конфигов. Решает проблему "откуда берётся это значение?". Полезна при добавлении или дебаге конфигов. | `PipelineYamlConfig`, `ConfigLoader`, `DQConfigLoader`, `FilterConfigLoader`, `DQConfig`, `InputFilterConfig`, `SourceConfig` |
| 22 | Publication Composite — Merge All Sources | flowchart | DataFlow | 7.88 | 8 | 8 | 6 | 9 | 8 | Финальный merge 5 Silver таблиц в Gold: Seed + CrossRef + PubMed + OpenAlex + S2 → LEFT OUTER JOIN → conflict resolution → column ordering → Gold write. Показывает стратегии разрешения конфликтов. Критична для data quality в Gold. | `MergeService`, `ColumnOrderer`, `ColumnRenamer`, `EnricherDeduplicator`, `MergeConfig`, `CompositePublicationGoldSchema`, `FieldGroup` |
| 23 | Composite Pipeline Phase Lifecycle | stateDiagram | Lifecycle | 7.88 | 8 | 8 | 6 | 9 | 8 | State machine фаз composite pipeline: INIT → SEED → DEPS → ENRICHING → MERGING → GOLD_WRITING → DONE. С checkpointing на каждой фазе для resume. Показывает recovery points. Необходима для дебага зависших composite runs. | `CompositePipelineRunner`, `CompositeCheckpointState`, `CompositeCheckpointManager`, `FSMStateHelper`, `EnrichmentCoordinator`, `DependencyCoordinator` |
| 24 | CompositePipelineRunner Component | classDiagram | Component | 7.88 | 8 | 8 | 6 | 9 | 8 | Class diagram CompositePipelineRunner — самый сложный runner. Показывает все инжектируемые компоненты: coordinator, dependency_coordinator, merger, key_extractor. Референс для расширения composite functionality. | `CompositePipelineRunner`, `CompositeRuntimeConfig`, `EnrichmentCoordinator`, `DependencyCoordinator`, `MergeService`, `KeyExtractorService`, `CompositePreflightValidator`, `EnricherAggregator` |
| 25 | Exception Hierarchy Full Tree | classDiagram | Architecture | 7.88 | 7 | 9 | 9 | 7 | 8 | Полное дерево исключений: BioETLError → CriticalError, RecoverableError, DataQualityError → NetworkError, StorageError, ValidationError → 30+ конкретных ошибок. Справочник для catch/except блоков. Необходим каждому разработчику. | `BioETLError`, `CriticalError`, `RecoverableError`, `DataQualityError`, `NetworkError`, `StorageError`, `ValidationError`, `LockLostError`, `CircuitBreakerOpenError`, `RetryExhaustedError`, `SchemaViolationError` |
| 26 | Idempotent Write Pattern — Content Hash Deduplication | flowchart | Pattern | 7.81 | 9 | 8 | 7 | 9 | 4 | Показывает механизм idempotency: compute content_hash → query Silver по entity_id → same hash → skip, different → upsert. Гарантирует exactly-once семантику на уровне Silver. Критична для понимания data integrity гарантий. | `SilverWriter`, `BaseDeltaWriter`, `ETLRecordSchema`, `RecordProcessor`, `BaseTransformer` |
| 27 | Content Hash Calculation Pipeline | flowchart | DataFlow | 7.75 | 8 | 8 | 8 | 9 | 4 | Детальный алгоритм вычисления content_hash: exclude META_FIELDS → canonical JSON → sort_keys → round floats → NaN→null → sha256. Критичен для стабильности deduplication. Ошибка в хэше ломает идемпотентность всей системы. | `RecordProcessor`, `BaseTransformer`, `StdLibJsonEncoder`, `OrjsonEncoder`, `ETLRecordSchema` |
| 28 | BatchExecutor Internal Structure — 786 LOC Decomposition | classDiagram | Component | 7.75 | 8 | 8 | 8 | 8 | 6 | Class diagram BatchExecutor (786 LOC) — самый большой компонент application layer. Декомпозиция: _transformer, _writer, _metrics_recorder, _tracing_manager. Показывает delegation pattern. Необходима при рефакторинге и оптимизации. | `BatchExecutor`, `BatchTransformer`, `BatchWriter`, `BatchMetricsRecorder`, `BatchTracingManager`, `BatchResult`, `TransformResult` |
| 29 | Silver Merge/Upsert Decision Logic | flowchart | DataFlow | 7.75 | 8 | 8 | 8 | 9 | 4 | Decision tree для SilverWriter: check write_mode → MERGE (upsert by PK) / APPEND / DELETE. Показывает, когда какой режим используется. Ключевая для data engineers, работающих с Silver layer. | `SilverWriter`, `BaseDeltaWriter`, `SinkLayerConfig`, `PipelineYamlConfig` |
| 30 | At-Least-Once Delivery + Silver Deduplication | flowchart | Pattern | 7.75 | 9 | 8 | 6 | 9 | 5 | Показывает, как at-least-once delivery (retry → duplicate Bronze) превращается в exactly-once на Silver через content_hash merge. Фундаментальный паттерн data integrity проекта. Полезна для архитекторов данных. | `BronzeWriter`, `SilverWriter`, `RecordProcessor`, `BaseTransformer`, `ETLRecordSchema` |
| 31 | Infrastructure Adapter Inheritance Hierarchy | classDiagram | Architecture | 7.75 | 8 | 9 | 7 | 7 | 8 | Полное дерево наследования адаптеров: BaseHttpAdapter → ChemblAdapter, UniProtAdapter, CrossRefAdapter, OpenAlexAdapter, SemanticScholarAdapter; BaseSyncAdapter → PubChemAdapter. С миксинами HealthCheckMixin и PaginatedFetcherMixin. | `BaseHttpAdapter`, `BaseSyncAdapter`, `HealthCheckProviderMixin`, `PaginatedFetcherMixin`, `ChemblAdapter`, `PubChemAdapter`, `UniProtAdapter`, `PubMedAdapter`, `CrossRefAdapter`, `OpenAlexAdapter`, `SemanticScholarAdapter` |
| 32 | Layered Validation Strategy (5 Levels) | flowchart | Pattern | 7.69 | 8 | 9 | 6 | 9 | 5 | 5 уровней валидации: L1 Pandera schema → L2 Cross-field → L3 External verify → L4 Logical ranges → L5 Semantic. Показывает, какие проверки на каком уровне. Необходима при добавлении новых валидационных правил. | `PanderaSilverValidator`, `PanderaGoldValidator`, `ETLRecordSchema`, `ActivitySchema`, `ChEMBLActivityGoldSchema`, `DataQualityService` |
| 33 | UnifiedHTTPClient Component — Full Internal Architecture | classDiagram | Component | 7.69 | 8 | 8 | 7 | 9 | 5 | Class diagram UnifiedHTTPClient — сложный компонент с внутренним rate limiter (TokenBucket), circuit breaker и retry logic. Показывает полную архитектуру HTTP-инфраструктуры. Необходима при тюнинге сетевых параметров. | `UnifiedHTTPClient`, `TokenBucket`, `CircuitBreaker`, `ProviderHealthMonitor`, `AdapterMetrics`, `RetryingDataSourceDecorator` |
| 34 | PipelineRunner ↔ BatchExecutor Interaction | sequenceDiagram | Interaction | 7.69 | 7 | 8 | 9 | 8 | 6 | Sequence diagram взаимодействия Runner и Executor: iterate batches → extract → transform → validate → write → checkpoint. Показывает batch loop — самый часто выполняемый код. Полезна для performance profiling. | `PipelineRunner`, `BatchExecutor`, `BatchTransformer`, `BatchWriter`, `CheckpointManager`, `BatchResult` |
| 35 | RunType-Based Clear Policy Interaction | sequenceDiagram | Interaction | 7.69 | 9 | 8 | 7 | 8 | 5 | Sequence diagram: check run_type → REBUILD/BACKFILL: clear Silver+Gold → INCREMENTAL: skip. Enforcement ARCH-007 через MedallionLifecycleService. Критична для data safety — неправильная clear policy = потеря данных. | `PipelineRunner`, `MedallionLifecycleService`, `RunType`, `SilverWriter`, `GoldWriter`, `ClearResult` |
| 36 | Incremental Data Flow — Delta Update Path | flowchart | DataFlow | 7.69 | 8 | 8 | 9 | 7 | 6 | Конкретный data flow для INCREMENTAL: load watermark → fetch since watermark → Bronze append → Silver merge → Gold merge. Самый частый режим работы pipeline. Показывает минимальный path для обновления данных. | `PipelineRunner`, `CheckpointManager`, `BatchExecutor`, `BronzeWriter`, `SilverWriter`, `GoldWriter`, `RunType` |
| 37 | Three Pillars of Observability in BioETL | flowchart | Observability | 7.63 | 8 | 9 | 7 | 7 | 7 | Обзор observability: Logging (StructlogLogger→LoggerPort) + Metrics (PrometheusMetrics→MetricsPort) + Tracing (NoOpTracing→TracingPort). Показывает, как ADR-022 (NoOp Tracing) и ADR-019 (port enforcement) реализованы. Полезна SRE и DevOps. | `LoggerPort`, `MetricsPort`, `TracingPort`, `StructlogLogger`, `UnifiedLogger`, `PrometheusMetrics`, `NoOpTracing`, `PipelineObserver`, `BatchMetricsRecorder` |
| 38 | MergeService Component | classDiagram | Component | 7.63 | 8 | 8 | 6 | 9 | 6 | Class diagram MergeService — ключевой компонент composite pipeline. LEFT OUTER JOIN seed+enrichers, conflict resolution, column ordering, trash filtering. Сложная логика мерджа из 5 источников. | `MergeService`, `MergeConfig`, `ColumnOrderer`, `ColumnRenamer`, `EnricherDeduplicator`, `FieldGroup`, `FieldSpec` |
| 39 | ChEMBL Adapter — 14 Entity Types Supported | flowchart | Provider | 7.63 | 7 | 9 | 7 | 7 | 9 | Показывает самый большой адаптер проекта: ChemblAdapter с ChemblEntityMapper, маршрутизирующим 14 entity types на API endpoints. Необходима для понимания ChEMBL — основного data source проекта. | `ChemblAdapter`, `ChemblEntityMapper`, `ChemblHealthMixin`, `ChemblMetadataMixin`, `BaseHttpAdapter`, `ChemblActivityRecord`, `ChemblAssayRecord`, `ChemblMoleculeRecord` |
| 40 | Provider-Specific Transformer Class Hierarchy | classDiagram | Provider | 7.63 | 7 | 9 | 7 | 7 | 9 | Полное дерево 23+ трансформеров: BaseTransformer → BaseChemblTransformer → 14 ChEMBL; BasePublicationTransformer → 4 publication. Каждый с _transform_impl(). Справочник для создания новых трансформеров. | `BaseTransformer`, `BaseChemblTransformer`, `ActivityTransformer`, `MoleculeTransformer`, `AssayTransformer`, `TargetTransformer`, `BasePublicationTransformer`, `PubMedPublicationTransformer`, `CrossRefPublicationTransformer`, `OpenAlexPublicationTransformer`, `SemanticScholarPublicationTransformer`, `PubChemCompoundTransformer`, `UniProtProteinTransformer` |
| 41 | Graceful Shutdown Lifecycle | stateDiagram | Lifecycle | 7.63 | 8 | 8 | 6 | 9 | 6 | State machine graceful shutdown: RUNNING → SIGNAL(SIGTERM) → DRAINING → CHECKPOINT_SAVED → LOCK_RELEASED → EXIT(0). Показывает, как система корректно завершает работу без потери данных. Критична для production operations. | `ShutdownService`, `ShutdownSignal`, `ShutdownReason`, `PipelineRunner`, `CheckpointManager`, `LockManager`, `HeartbeatTask` |
| 42 | DQ Flag Routing Decision Tree | flowchart | DataFlow | 7.56 | 7 | 9 | 8 | 8 | 5 | Дерево решений маршрутизации DQ: all pass → clean; warning <5% → _dq_warn=true; error → quarantine; >20% → batch fail. Определяет судьбу каждой записи. Необходима для настройки DQ thresholds. | `DataQualityService`, `QuarantineManager`, `UnifiedQuarantine`, `QuarantineEntry`, `DQConfig`, `BatchWriter` |
| 43 | UnifiedHTTPClient ↔ RateLimiter ↔ CircuitBreaker Triple Interaction | sequenceDiagram | Interaction | 7.56 | 8 | 8 | 7 | 9 | 4 | Тройное взаимодействие: HTTPClient → RateLimiter.wait() → CircuitBreaker.before_call() → httpx.request() → record_result(). Показывает порядок resilience checks. Сложный flow, без диаграммы легко запутаться. | `UnifiedHTTPClient`, `TokenBucket`, `CircuitBreaker`, `ProviderHealthMonitor`, `CircuitBreakerState` |
| 44 | Backfill Data Flow — Full Reload Path | flowchart | DataFlow | 7.56 | 8 | 8 | 7 | 8 | 6 | Полный data flow для BACKFILL: acquire exclusive lock → clear Silver+Gold → full API fetch → Bronze → Silver → Gold. Деструктивная операция — показывает все safeguards. Необходима для операционных процедур. | `PipelineRunner`, `MedallionLifecycleService`, `RunType`, `LockManager`, `BatchExecutor`, `BronzeWriter`, `SilverWriter`, `GoldWriter` |
| 45 | Pipeline Config YAML Structure | flowchart | Configuration | 7.50 | 7 | 9 | 9 | 7 | 5 | Визуализация структуры YAML конфига: pipeline_name, provider, entity_type, source, sink{silver, gold}, dq_overrides. Самый часто редактируемый файл проекта. Решает проблему "какие поля доступны и что они означают". | `PipelineYamlConfig`, `SinkLayerConfig`, `SourceConfig`, `DQConfig`, `InputFilterConfig`, `SortByConfig`, `TransformConfig` |
| 46 | Exception Propagation Through Layers | flowchart | ErrorHandling | 7.44 | 7 | 8 | 7 | 8 | 7 | Показывает, как ошибки проходят через слои: Infrastructure → domain exception wrap → Application catch → classify → retry/quarantine/fail → CLI exit code. Решает проблему "почему CLI вернул код 2?". | `BioETLError`, `CriticalError`, `RecoverableError`, `ErrorService`, `ErrorCategory`, `PipelineRunner`, `ExitCode`, `BatchExecutor` |
| 47 | DataSourcePort Protocol — Complete Method Signatures | classDiagram | DomainModel | 7.44 | 8 | 9 | 8 | 7 | 4 | Полная сигнатура DataSourcePort — центрального протокола проекта. fetch() → AsyncIterator, health_check() → HealthStatus. Также FilterableDataSourcePort с filter_ids. Необходима для реализации новых data source адаптеров. | `DataSourcePort`, `FilterableDataSourcePort`, `HealthStatus`, `HealthCheckResult`, `BaseHttpAdapter`, `BaseSyncAdapter` |
| 48 | Run ID Correlation Across All Observability Channels | flowchart | Observability | 7.25 | 7 | 8 | 8 | 7 | 6 | Показывает, как run_id (UUID) привязывается к logger, metrics labels и trace attributes для корреляции. Решает проблему "как найти все логи/метрики одного запуска pipeline". Полезна SRE при incident investigation. | `PipelineRunner`, `PipelineObserver`, `StructlogLogger`, `PrometheusMetrics`, `BatchMetricsRecorder`, `BatchTracingManager` |
| 49 | Provider Comparison — Rate Limit Strategies | flowchart | Provider | 7.25 | 7 | 9 | 7 | 6 | 8 | Сравнительная таблица: ChEMBL (none) → PubChem (5/s) → UniProt (100/s) → PubMed (3-10/s) → CrossRef (50/s) → OpenAlex (10/s) → S2 (0.33/s). TokenBucket конфигурация для каждого. Справочник для настройки rate limits. | `TokenBucket`, `HttpClientFactory`, `ChemblAdapter`, `PubChemAdapter`, `UniProtAdapter`, `PubMedAdapter`, `CrossRefAdapter`, `OpenAlexAdapter`, `SemanticScholarAdapter` |
| 50 | Conflict Resolution Strategies Comparison | flowchart | Composite | 6.69 | 7 | 8 | 5 | 8 | 4 | Сравнение стратегий: seed_priority → enricher_priority → coalesce (first non-null) → explicit_rules (per-field). Используется в MergeService для composite pipeline. Решает проблему "какое значение попадёт в Gold при конфликте". | `MergeService`, `MergeConfig`, `EnricherDeduplicator`, `ColumnRenamer` |
