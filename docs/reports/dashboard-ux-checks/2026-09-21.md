# Grafana correction acceptance — 2026-09-21

Scope: GR-DB-CORR issues #10555–#10571, parent #10554.

Acceptance remains incomplete. The observations below cover the local preview
dashboards; they do not establish acceptance of every panel and pipeline.

## Observed browser behavior

- Trust and Overview were inspected at 1320×920 and 900×920. All seven
  navigation links were visible. Table wrapping and column allocation were
  improved. The Overview row-height and SELECT RUN width adjustments were
  verified in loaded previews at both sizes on 2026-09-22.
- Provider Health was inspected at 900×920. Status reasons remained visible.
  A follow-up at one fixed Prometheus instant confirmed seven restored provider
  states and their timestamps; the earlier mismatched observations were not
  reproduced in that consistent-time query.
- Saved-run trust and CURRENT readiness remain distinct. A successful saved
  run does not establish complete current telemetry.

## Evidence limitations

The original audit is retained under
`reports/observability/grafana/audit-20260921/` in the canonical checkout.
Local continuation evidence is under `reports/gr-db-corr/` in the worktree.
Render API responses included loading panels and an unexpected image width;
these images are not accepted as visual verification.

The remaining dashboard sections, pipeline combinations,
timezone transitions and exact per-run Gold version attribution have not yet
been fully verified. The full semantic and visual matrix is pending, and no
issue is qualified for closure by this report alone.

## Cancellation follow-up — 2026-09-22

Real `chembl_subcellular_fraction` execution with `--limit 1000` was interrupted
with Ctrl+C. Run `a11c5db6-66a4-5daa-9451-47cd69f63f4e` persisted `shutdown`,
completion `2026-09-22T03:51:00.021579+03:00`, and duration 46.771356 seconds.
The report-index API returned the same terminal state with aligned source
identity. Loaded Run Explorer showed `shutdown` and `completed`, alongside
the earlier budget-exceeded run `b45d7511-1707-5dc9-b4ad-979fffb5f3a8` as
`failed`. The historical unfinished entry was retained.

The all-tests run reached 73% before a reproducible Arrow snapshot timeout.
After disabling nested Arrow workers in snapshot counting, the isolated
snapshot regression and all 11 tests in its module passed. A complete rerun
is pending. Mypy passed for 2497 source files; these checks do not substitute
for the remaining live acceptance matrix.

## Regression and browser follow-up — 2026-09-22

Run Explorer was loaded at 900×920 and 1320×920 for the cancellation run above.
All seven navigation links were visible. The three persisted rows displayed
`shutdown` in orange, `failed` in red and historical `unfinished` in gray.
The table retained full identifiers in its cell data and report links.
Screenshots were inspected in the interactive browser; they were not saved as
standalone image artifacts. A refresh of the preview dashboards encountered
an HTTP timeout, so this observation does not certify every latest preview.

The filter-options endpoint had no shared forensic deadline. Its handler now
uses the bounded operation contract and responds with HTTP 504 on deadline
exhaustion. Regression tests verify that no late HTTP 200 is written after
the timeout; deployment and live-load acceptance remain pending.

The latest broad unit/integration attempt executed 29,758 tests with 10
failures, 91 skips and no collection errors. Failures included stale dashboard
window expectations, generated matrix drift, a visual empty-state description,
and a subprocess timing assertion under concurrent load. The isolated timing
check passed. Follow-up integration and architecture runs remain in progress;
the broad attempt is not a PASS. Type checking passed for 2,497 source files
before the final dependency cleanup. Debt budgets were not increased.


## Live deployment and selector follow-up — 2026-09-22

Image `4f8db68846007c3220ec42ff72e6394435f56532a102a1c0d05f546c3212bcdb`
was built from the correction worktree and deployed. Both health endpoints
returned HTTP 200. All seven isolated preview dashboard updates returned 200.
Run Explorer selected identity completion now renders as local
`2026-09-22 03:51:00`, matching the saved cancellation report. The table was
inspected at 1320×920 and 900×920; identifiers wrap and remain available.

Live filter-options still exposed a deadline: pipeline selection returned
504, while the exact pipeline run selector returned 200. Container profiling
measured 4.281 s for 326 run manifests, 0.645 s for 122 workflow manifests and
8.157 s for report supplementation. Independent catalog reads now overlap;
report reads narrow to selected pipeline owners. All 53 routing/selector
regressions passed. This latest optimization awaits rebuilt-image verification.
The architecture tail completed with nine failures; follow-up repairs and
artifact rebinds are in progress. No full test-suite or audit PASS is claimed.


## Full architecture follow-up — 2026-09-22

The complete architecture run executed 4,797 tests in 702.090 seconds, with
4 failures, 105 skips and no collection errors. The four failures concern
script reference line numbers, test inventory drift and two telemetry source
fingerprint checks after subsequent selector regressions were added. Their
canonical inventories have been refreshed; focused revalidation is pending.
All 92 selector/identity regression tests passed after eliminating full-ledger
reads for identity-only dimensions. Status filtering still reads the ledger.
Mypy passes for all 2,497 source modules.

The refreshed audit inventory retains all 262 elements and 27 pipeline
configurations. It currently contains 38,514 available/latest-context rows
and 11,967 distinct query jobs, including explicit range requests. The seven
preview models match source panels after normalizing the isolated UID prefix.
Unavailable scenario combinations and the original 59,474-row matrix remain
separate evidence to reconcile before acceptance.

## Reconciled live evidence — 2026-09-22

Checkpoint commit `e03895888e3` preserves the implementation before the final
follow-up. The installed image `f1b65d615610b7005093d9748b7bde59ab74418d08d67af36c1945d940005016`
returned pipeline options in 9.998 seconds and run options in 4.402 seconds,
both HTTP 200. Clean-source inventory checks passed. A subsequent Docker
restart had left Grafana in its documented Prometheus-only fallback because
the Ops identity probe timed out. Restarting Grafana after Ops readiness
restored `ops_http=ready`, `source_id_matched=1`, and `dashboard_profile=full`.
The preview Recent Runs table then displayed the same three records as the API.

The post-recovery collector executed 11,845 unique queries at
2026-09-22T03:19:26.962628+00:00: 11,812 succeeded, 31 had transport errors,
and two returned explicit HTTP-200 error envelopes. Serial rechecks retained
four latest-complete-run failures. These are not valid empty results. Catalog
reads now overlap and their elapsed time reduces the remaining discovery
budget; rebuilt-image verification is pending.

Evidence is in the worktree's ignored
`reports/gr-db-corr/acceptance-20260922-restored/`. Its
`coverage-reconciled.csv` preserves all 59,474 historical rows plus 38,514
current rows, for 97,988 evidence rows. These are observations from different
generations, not 97,988 unique accepted combinations. The 486 original missing
scenario groups remain accounted for. All 262 elements and 27 configured
pipelines are retained. Global query responses are shared evidence, not
individual run results. Thirty-four selected-run responses matched saved
execution states; this check does not establish domain or visual acceptance.
The evidence manifest records SHA-256 values for the inputs and outputs.

The full 35,026-test selection was interrupted after two architecture failures:
a 106-line checkpoint helper and a documentation inventory that omitted the
newly tracked report. Duplicate exception handling was consolidated without
changing error semantics; the documentation inventory was refreshed after
the commit. Targeted composite and first-window checks passed after restoring
the canonical minute-resolution date format. No full-suite PASS is claimed.

At 900×920, all seven navigation links are visible. Run Explorer and Overview
show the cancelled run as shutdown, with incomplete evidence kept explicit.
Trust's long reason text remained clipped by table wrapping; the follow-up
uses a reason count linked to the full checks panel, preserving raw detail in
the API. The count, final image, all collapsed sections, and complete visual
matrix still require acceptance. None of the 18 issues is closed by this
partial report.

## Post-merge verification and issue closeout — 2026-09-22

GitHub comparison confirms that checkpoint `368f7f1da591074fa66bef1de1aae52bc3a0661a`
is included in main `e45b142f9b6a041ae2cbcfde5e00c514b4897c65`. Issues
10561, 10565, 10566, 10556 and 10562 were closed with individual test and
persisted-run evidence. Together with the previously closed 10563 and 10564,
seven of the original eighteen issues are closed. The other eleven remain open.

The follow-up corrects Trust's reason-count link: panel 9418 now opens 9451,
whose Control Plane row displays every frozen reason from the selected report.
The raw reason code and verdict are preserved. The cancelled run
`a11c5db6-66a4-5daa-9451-47cd69f63f4e` displays three lineage reasons, confirmed
in the working browser at both 900×920 and 1320×920. This is scoped visual
evidence, not acceptance of every collapsed panel or every pipeline context.

Repeated live requests uncovered an additional event-loop blocker: the
long-lived server rehydrated report-backed metrics synchronously every thirty
seconds. Both initial and periodic rehydration now run in a worker thread.
The lifecycle regression asserts that neither call runs on the event-loop
thread; 44 lifecycle tests passed. On the rebuilt image, four previously
problematic latest-complete-run requests returned in 5.653, 9.478, 9.969 and
12.007 seconds. The last returned an explicit HTTP-200 `deadline_exceeded`
error row, not a successful or empty assessment. This does not establish that
every expensive lookup finishes successfully within the deadline.

Installed image
`sha256:7a542954793fa81907abc7538eec4a70b80dfa52f6f5ba541d6bcd0672cc64ea`
matches all 2,497 local Python modules byte-for-byte after CRLF normalization.
Proof is stored in `reports/gr-db-corr/runtime-all-source-proof.json`.
All seven isolated preview dashboard updates returned HTTP 200.

Scoped results: 181 Trust/layout checks passed and one browser capture test
was skipped; 180 classification/bootstrap/ChEMBL identity checks passed;
60 report/lifecycle checks and 80 durable-accounting/catalog checks passed;
29 code-budget/latest-complete checks passed. Ruff passed. Mypy passed for
all 2,497 modules including the final worker-thread call changes.

The subsequent complete-suite attempt stopped with 4,058 passed, 3 skipped
and one Windows/WSL wrapper failure. Its helper could fall back to an
unisolated Bash environment when the WSL prerequisite probe failed. It now
reports that unavailable prerequisite explicitly. The isolated wrapper suite
then returned 48 passed and one WSL-chmod-timeout skip. A complete final suite
PASS is still outstanding. The earlier interrupted parallel attempt also
reported a terminated worker and is not accepted as verification.

The broad governance refresh was rejected by automatic approval review.
Only the permitted targeted source/module inventories, scripts inventory,
documentation inventory and `refresh-ci-drift-families` were used. Existing
coverage provenance was retained; its 96.74 percent is not a new local
coverage measurement. Debt budgets were not increased. AI runtime sources
were not edited, so runtime mirror synchronization is not applicable.

The continuation check fixed two stale post-merge test expectations: the
cached scanner list and a permanently skipped, obsolete rollout test. The
latter now verifies that registry violations remain blocking after removal
of the rollout override. The focused architecture set passed 100 tests.

Live Incident Workspace inspection found that a table-wide `noValue`
message falsely labelled missing `instance` and `job` cells inside a firing
alert as an empty domain. The canonical correction now uses UNKNOWN for
missing table values and NOT PROVIDED for those optional alert labels.
The real BioETLDockerRuntimeProbeMissing row was rechecked in Grafana at
900 by 920: FIRING remained visible and both absent labels showed NOT
PROVIDED. The 63 dashboard checks passed; one optional capture test skipped.
All seven navigation links remained visible. Narrow-screen instructional
text still scrolls internally in Incident Workspace and Diagnostics; these
remain open design findings rather than accepted panels.
