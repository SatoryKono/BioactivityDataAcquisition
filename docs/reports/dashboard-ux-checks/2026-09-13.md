# Run Explorer UX check — 2026-09-13

Scope: local Run Explorer simplification requested through four browser comments.
This is an AI browser check of the working tree, not release acceptance.
Human usability remains NOT_MEASURED, N=0.

## Changes

- Removed Selected Run Details and Browse Workflow Runs with all nested panels.
- Inspect Recent Runs shows up to ten launches together without pagination.
  Compact single-line cells keep the table height bounded; complete values remain
  available through Inspect and the Run link.
- Inserted an explicit line break before Pipeline in the scope banner.
- Updated incoming run links to select the row without targeting deleted panel
  3022. The Report column still opens the exact row's report.

## Browser observations

Observed on local Grafana at `/d/bioetl-run-explorer-v1/6-run-explorer` with
Workflow=chembl_baseline, Pipeline=chembl_target, Run Type=backfill, selected run
`cda0fb24-1b2a-542a-8654-7b8a76eb96e1`, and Europe/Kiev time zone.

The rendered table contains ten rows, ordered newest first, from
2026-09-13 15:50 to 2026-09-04 00:06. The accessibility tree exposes all ten
Run and Report links. Pagination controls and both removed groups are absent.
The screenshot confirms the line break and compact row spacing. At the smaller
1265×712 browser viewport, page scrolling is required to see the bottom rows;
this check does not claim that ten rows fit every screen without page scrolling.

Run links preserve each row's identity and current time range without viewPanel.
Available-report and missing-report navigation were also exercised during the
preceding Report-column check; neither endpoint changed in this simplification.

## Validation boundary

Focused dashboard, navigation, readability, first-window, content-contract and
request-fixture checks are recorded under
`reports/quality/proof-or-stop/run-explorer-simplify-20260913/`.
The existing stale project-test-health documentation evidence remains a separate
repository-wide closeout blocker. No CI, merge, publication or human acceptance
is implied by this local browser check.
