# Grafana data integrity verification — 2026-09-07

Scope: issues #10163 and #10164; runtime panel 205 and Incident panel 2010.

Panel 205 evaluates the selected-window counter increase once at the window end.
Every scope recorded by the selected-window universe requires at least two
failed-counter samples. Missing or insufficient samples in any scope render
UNKNOWN. Scope-universe presence never manufactures a zero.

Incident 2010 uses one union of the three recorded ranking metrics. Grafana 12
therefore exposes one Value field before sorting and the five-row limit; Priority
is no longer lost between Value #A, Value #B, and Value #C.

Validation: operator-readability and first-window no-scroll contracts passed;
PromQL engine cases cover 15m/6h, older failures, resets, measured zero, missing,
single-sample, stale, foreign-pipeline, partial multi-scope, and complete
multi-scope series (ten new cases). Live requests, source references,
and Grafana 12 transformation replay are recorded in the data-integrity bundle.
This report does not claim screenshot or human visual acceptance.

The live evidence bundle remains local pending publication approval; it is not
an attachment to this change. Issue #10164 tracks its publication and outstanding
independent checks. Empty evidence never counts as numerical PASS.

Historical panel 205 requests at 2026-09-04 09:30 UTC matched the independent
counter calculation (measured zero) for both 15m and 6h. The initial 2026-09-07
window had no failed-counter samples and correctly returned UNKNOWN with the
candidate query. Positive failures and resets are covered by promtool cases.

The final coverage guard was also reconciled against five live requests: two
historical measured-zero matches and three absence matches, including a
multi-scope selection whose incremental scope has no qualifying counter.
Absence matches demonstrate UNKNOWN behavior and are not numerical PASS.

## Overview, Runtime and Incident follow-up

Scope: #10176, #10177, #10181, #10193 and #10194. The preceding data-integrity
verification remains a separate historical result.

### Operator behavior

- Overview shows routing urgency and the affected pipeline. Identical actions
  across run types are collapsed before the two-row limit; different objects
  remain distinct. Missing routing data remains UNKNOWN. Three state tracks
  use semantic mappings and outlined intervals; five domain comparisons use
  full-width tables with readable object and status columns.
- Runtime places blockers and their next diagnostic action on the first
  screen, including 900 by 768. Endpoint SCRAPING and baseline PRESENT are
  neutral evidence, accompanied by rule evaluation age. Complete telemetry
  remains UNVERIFIED: the two-series baseline cannot establish coverage of
  every required metric family. Both lower instruction panels expose their
  action and destination links without nested scrolling.
- Incident states GLOBAL CURRENT scope beside the ranking. Pipeline and Run
  ID selections do not filter the fleet-wide suspects or global domain
  details. Rank orders CRIT before WARN; equal severity means equal urgency.
  Confidence is UNVERIFIED, not a fabricated probability or established cause.
  The visible table contains Rank, Severity, Confidence, Object, Signal,
  Action and Domain. Raw labels remain available through Inspect.
- Inactive zero-valued suspects are excluded before ranking; an explicit
  telemetry-gap row survives as UNKNOWN. Impact and confidence guidance fits
  in one visible panel. Alert history reserves space for complete names,
  draws state intervals and retains FIRING/PENDING colors and legend without
  clipping state words into repeated FI/F fragments. Missing samples remain
  gaps.
- The canonical navigation generator reduces its band from four to three grid
  rows on all seven dashboards. Authored heading/body sizes remain 19/16 px.
  No overflow masking, debt budget, query budget or exception increase is used.

### Validation and evidence boundary

The local browser bundle is `reports/observability/ori-five/`. It contains
before/after screenshots, URLs, viewport dimensions, source JSON hashes,
screenshot hashes and panel geometry in per-pass `manifest.json` files.
These local files are not GitHub attachments. The browser substitutes the
candidate dashboard JSON in its own response only and uses the existing live
datasources. It does not replace provisioned dashboards or attest production
source identity; that deployment attestation remains #10165.

Scope: `chembl_assay`, run type All, provider `chembl`, selected run
`a72f6bc0-d3cb-5e9f-bd7d-00763ec963c8`, dark theme, Europe/Kiev, height 768,
widths 1366, 1000 and 900. Overview/Runtime use Last 12 hours; Incident alert
history uses Last 7 days. Browser zoom is unchanged and the navigation drawer
is closed consistently in the comparable before/after captures.

The accepted `verified-3d26` pass completed all 45 top/detail captures on
candidate `3d26de97ec1394fbfc3e9778239100c26341d0fd`. The source JSON hashes
below identify that captured candidate. Subsequent integration with main
`a4391596eaa322e8acee5e9f7fc3ff7059702589` requires a separate final-candidate
check; these historical images alone do not establish that result.
The 900 px Runtime frame contains the complete blocker panel and
neutral telemetry evidence. Incident has seven readable operator columns;
Impact/Confidence and both Runtime instructions have no nested scrollbars.
Alert names, FIRING/PENDING legend and outlined intervals are visible at
1000/1366 px without FI/F fragments.

The `global-3d26` pass adds 20 Overview captures at 900/1366 px with Pipeline
All. Separate DQ actions retain distinct pipeline identities. Domain tables
keep object and Status together across the available width; longer global
result sets retain ordinary vertical row scrolling. The `actions-3d26` pass
clicked all four Runtime handoff links and verified the destination UID and
pipeline/run/time parameters after Grafana normalized the URL slug.

The `navigation-3d26` pass adds 12 navigation captures for the other four
shipped dashboards at 1366/1000/900 px. All seven navigation bands measure
106 CSS px, preserve 16 px links and have no overflow. Those additional frames
validate navigation only, not the data completeness of the other dashboards.

Earlier `before`, `before-verified` and `before-nav` bundles preserve the
baseline. The intermediate `final-verified`, `seven-day-final` and
`incident-columns` bundles document development checks. Failed/partial passes
are not accepted evidence. Ops HTTP timeouts and an external Grafana restart
interrupted earlier attempts; the accepted primary pass followed recovery and
waited for both panel and variable queries to settle. No container restart,
provisioned-dashboard replacement or datasource configuration change was
performed by this task.

| Dashboard JSON | SHA-256 |
| --- | --- |
| `bioetl-runtime.json` | `80092e644718ca5207c6289420cb77faac6ad27c327461de216cbf209b08de5f` |
| `bioetl-overview-v2.json` | `a40b13f0bfcd07ba05d840d27e955ff882b432578a270c00d09acf42b6ba18fa` |
| `bioetl-incident-v1.json` | `d65cdf59e5cb49727af4c0d70f9ae1569e0240db1d5ae3e759e1b4f5bd40724a` |

Representative frames in `verified-3d26`:

| Frame | SHA-256 |
| --- | --- |
| `bioetl-runtime-900-top.png` | `863435bac2736d51dc17ac790c2452f7c6e321331b257f330d48f7a626fe4d40` |
| `bioetl-incident-v1-900-top.png` | `7184b3fda393dffcfb16151cb84cfe0472dcf788a29b35839a05a8e836fa526e` |
| `bioetl-incident-v1-1000-Track-Alert-State-History.png` | `a6cb450daf6fc7722c5aff4a415f22c91aa3175caa8587740a2a81a1b1f032d4` |
| `bioetl-runtime-1000-Review-Cross-Domain-Handoffs.png` | `f987b41cd4d311139aa718da107e2b1f2eb03712cf4219655465696cd0626e01` |

The merged source passed 878 related tests with eight explicit skips: five
retired-workflow cases, two POSIX bootstrap cases on Windows and the opt-in
live panel-fill test. The full merged promtool suite passed. Navigation,
scenes parity, first-window layout, readability, empty-state, filter/link,
query-budget and metric-inventory contracts are included in that validation.
The full repository suite runs in PR CI.

Canonical scripts, test governance, flaky-review, telemetry and metric-mention
inventories were refreshed after integration with main. Test telemetry uses
actual coverage/duration artifacts from PR run `34138599480`, whose other
failed contracts were repaired separately; that run is not claimed as an
overall CI pass. The audit registry digest was recomputed with its canonical
function after the remote-main baseline refresh; this does not claim a new
architecture audit. Runtime agent/skill sources were not edited. Codex–Junie
mirror parity, documentation links/drift and debt governance passed with
unchanged budgets. Persistent memory writes are skipped in read-only mode.

### Final verification after the table-stream merge

`merged-c42a` completed all nine first-screen captures at 1366/1000/900 px
after integrating main `a4391596eaa322e8acee5e9f7fc3ff7059702589`.
The rendered commit `c42a43fb801f1d61083d705eda8523340b034006` and its
message-only replacement `d5709c075842b97bce4be7a776b80aca60d8e58e` share
the exact Git tree `00f193014ceb977755472636af16ebd70a9059d7`.
`amend-identity.json` records that equality.

`merged-panel-identities.json` compares options, queries, links and geometry
with the accepted 45-frame pass: 30 of 31 affected panel/navigation objects
are identical. The sole change, Overview 9002, combines the two incoming
width declarations into one 110 px rule covering the original and display
field names. `status-d570` separately captures that panel at 900/1366 px;
both Input and Status are readable with no nested scrolling. The existing
200% width budget remains unchanged and passes.

All seven dashboard JSON objects match their committed Git sources.
`dashboard-git-identity.json` records raw-file, Git-blob and canonical JSON
hashes; canonical JSON hashes below ignore serialization/line-ending differences.

| Final dashboard | Canonical JSON SHA-256 |
| --- | --- |
| Runtime | `df52de9f2d0794a93fafb7bd7747d895a3a7aac082c39ec88ab4adc4331eab9a` |
| Overview | `80dd0f48e0e2994929565cc96b6fd68dedf9fb0b7ab292938e15a74b88366a9c` |
| Incident | `88c9fcc69da3bd725d10cca8ff953d2c698c87a79db3783482af9e29cb51d41e` |

Additional checks: `absolute-3d26` repeats Overview at 900/1366 px for
2026-09-06 15:15–15:26 Europe/Kiev. `global-action-clean` shows distinct
`chembl_target` and `chembl_publication` DQ actions at 900 px.
`tooltip-3d26` checks Incident with backfill selected: the native tooltip
shows the displayed interval start and duration alongside the full alert
name and FIRING state; the graph retains both interval boundaries and gaps.
These target panels are covered by the unchanged-object comparison above.

The merged dashboard/navigation/accounting test selection passed 899 tests
with the same eight explicit skips. The documentation links/drift checks and
light pretest guardrails passed. CI run `34143259598` produced coverage
(96.71%) and duration telemetry for `d5709c075842b97bce4be7a776b80aca60d8e58e`.
The canonical baseline now binds test-tree digest
`755d16956e1008675979b2b4518c12f2e41474cbeec55514ffa2e76f6cf3c08c`;
all eight telemetry-baseline and scripts-inventory checks passed. This records
the successful producer jobs, not an all-green conclusion for that CI run.

## Tables, Provider Health, DQ and accounting presentation

Scope: #10178, #10179, #10180, #10192 and #10183. Review copies use
`tables-five-*` UIDs in Grafana 12.0.0. The seven shipped JSON sources remain
canonical. Review-copy differences are limited to UID, title, database ID,
version and the Processed Records datasource pointing to the candidate HTTP
server on port 8015. That server reads the same canonical data directory and
the same selected-run ledger as the existing server on port 8000.

Browser context: dark theme; `chembl_assay`, `backfill`, run
`a72f6bc0-d3cb-5e9f-bd7d-00763ec963c8`; Last 12 hours unless noted. The
1366 by 768 viewport uses the docked menu; the 900 by 768 viewport hides it.

- Selected-run summary at 900 px shows success, OUT OF RANGE, local Started
  and Completed timestamps, Gold 983 and Excluded 17 before the complete,
  inspectable UUID. The same field contract is used in Runtime and Incident.
- Run Explorer reserves timestamp width, retains the compact selection marker
  and provides a direct report link for workflow runs. Runs at 15:20, 14:22
  and 14:19 are distinguishable without horizontal scrolling.
- Provider fleet details show UNKNOWN instead of code 3. Reason and source
  state wrap at word boundaries. The latency stat shows a readable UNKNOWN
  in its original three-grid-unit height and retains its seconds unit.
- DQ impact/freshness cards form two aligned columns with equal heights and
  font sizes at 900 and 1366 px. Missing current-status evidence produces an
  explicit Evidence unavailable (UNKNOWN) row. The OK branch instead names
  the absence of active reasons; query failures retain Grafana error state.
- The candidate HTTP response for the real selected ledger contains 100.0%,
  0.0%, 98.3% and 1.7%. In the live DQ table, Gold 983 / 98.3% and excluded
  17 / 1.7% have matching neutral quantity colors. Bronze/Silver/Gold label
  colors identify storage layers, not severity or an exclusion threshold.

Automated validation includes all dashboard/Grafana integration modules,
Processed Records unit tests, module inventory freshness, operator readability,
first-window containment and presentation contracts. The broad local pass ran
748 cases: 739 passed and 9 conditionally skipped; a staged/revision-bound
freshness check is required before submission because the unstaged diff is
outside that test's discovery contract.

The actual Prometheus engine also passed twelve expression checks: three
panel queries across explicit OK, WARN with a reason, explicit UNKNOWN, and
absent current-status series. Fixtures do not replace or modify live metrics.
The Run Explorer repeat at 1000 px showed full workflow timestamps, failed
and success labels, direct report URLs, and neutral Gold quantities.

Browser snapshots are retained in the implementation task. Server-side PNG
export failed in the renderer process (Chromium startup/render timeout), also
after restarting that renderer alone. These observations do not claim a
successful server-render bundle or deployment of the candidate backend to the
canonical port 8000. No font, technical-debt budget or threshold was relaxed.

Final review follow-up for #10178/#10183: the 900 px selected-run summary retains hidden `from_ms`/`to_ms` frame fields. Clicking Started exposes a Set range to run row link resolved to `from=1788696949943&to=1788697575718` for the acceptance run. The header uses Open Run Explorer because header links have no row context. Workflow report rows omit the unsupported selected marker; pipeline-run selection is retained. The operator guide now documents the same 1–3 decimal percentage rule as the backend. After merging main, the related dashboard/Grafana and accounting tests passed: 746 passed, 8 skipped.
