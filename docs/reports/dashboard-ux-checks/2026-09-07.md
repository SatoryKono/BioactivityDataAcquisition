# Grafana data integrity verification — 2026-09-07

Scope: issues #10163 and #10164; runtime panel 205 and Incident panel 2010.

Panel 205 evaluates the selected-window counter increase once at the window end.
Every scope recorded by the selected-window universe requires at least two
failed-counter samples. Missing or insufficient samples in any scope render
UNKNOWN. Scope-universe presence never manufactures a zero.

Incident 2010 uses one union of the three recorded ranking metrics. Grafana 12
therefore exposes one Value field before sorting and the five-row limit; Priority
is no longer lost between Value #A, Value #B, and Value #C.

Validation: operator-readability and first-window no-scroll contracts passed;
PromQL engine cases cover 15m/6h, older failures, resets, measured zero, missing,
single-sample, stale, foreign-pipeline, partial multi-scope, and complete
multi-scope series (ten new cases). Live requests, source references,
and Grafana 12 transformation replay are recorded in the data-integrity bundle.
This report does not claim screenshot or human visual acceptance.

The live evidence bundle remains local pending publication approval; it is not
an attachment to this change. Issue #10164 tracks its publication and outstanding
independent checks. Empty evidence never counts as numerical PASS.

Historical panel 205 requests at 2026-09-04 09:30 UTC matched the independent
counter calculation (measured zero) for both 15m and 6h. The initial 2026-09-07
window had no failed-counter samples and correctly returned UNKNOWN with the
candidate query. Positive failures and resets are covered by promtool cases.

The final coverage guard was also reconciled against five live requests: two
historical measured-zero matches and three absence matches, including a
multi-scope selection whose incremental scope has no qualifying counter.
Absence matches demonstrate UNKNOWN behavior and are not numerical PASS.

## Overview, Runtime and Incident follow-up

Scope: #10176, #10177, #10181, #10193 and #10194. The preceding data-integrity
verification remains a separate historical result.

### Operator behavior

- Overview shows routing urgency and the affected pipeline. Identical actions
  across run types are collapsed before the two-row limit; different objects
  remain distinct. Missing routing data remains UNKNOWN. Three state tracks
  use semantic mappings and outlined intervals; five domain comparisons use
  full-width tables with readable object and status columns.
- Runtime places blockers and their next diagnostic action on the first
  screen, including 900 by 768. Endpoint SCRAPING and baseline PRESENT are
  neutral evidence, accompanied by rule evaluation age. Complete telemetry
  remains UNVERIFIED: the two-series baseline cannot establish coverage of
  every required metric family. Both lower instruction panels expose their
  action and destination links without nested scrolling.
- Incident states GLOBAL CURRENT scope beside the ranking. Pipeline and Run
  ID selections do not filter the fleet-wide suspects or global domain
  details. Rank orders CRIT before WARN; equal severity means equal urgency.
  Confidence is UNVERIFIED, not a fabricated probability or established cause.
  The visible table contains Rank, Severity, Confidence, Object, Signal,
  Action and Domain. Raw labels remain available through Inspect.
- Inactive zero-valued suspects are excluded before ranking; an explicit
  telemetry-gap row survives as UNKNOWN. Impact and confidence guidance fits
  in one visible panel. Alert history reserves space for complete names,
  draws state intervals and retains FIRING/PENDING colors and legend without
  clipping state words into repeated FI/F fragments. Missing samples remain
  gaps.
- The canonical navigation generator reduces its band from four to three grid
  rows on all seven dashboards. Authored heading/body sizes remain 19/16 px.
  No overflow masking, debt budget, query budget or exception increase is used.

### Validation and evidence boundary

The local browser bundle is `reports/observability/ori-five/`. It contains
before/after screenshots, URLs, viewport dimensions, source JSON hashes,
screenshot hashes and panel geometry in per-pass `manifest.json` files.
These local files are not GitHub attachments. The browser substitutes the
candidate dashboard JSON in its own response only and uses the existing live
datasources. It does not replace provisioned dashboards or attest production
source identity; that deployment attestation remains #10165.

Scope: `chembl_assay`, run type All, provider `chembl`, selected run
`a72f6bc0-d3cb-5e9f-bd7d-00763ec963c8`, dark theme, Europe/Kiev, height 768,
widths 1366, 1000 and 900. Overview/Runtime use Last 12 hours; Incident alert
history uses Last 7 days. Browser zoom is unchanged and the navigation drawer
is closed consistently in the comparable before/after captures.

The pre-merge `final-verified` pass completed 45 top/detail captures; the
`seven-day-final` pass verified real alert history at 1000 and 1366 px.
The `incident-columns` pass subsequently verified the final seven-column
table at 900 px. Those passes are intermediate evidence, not a claim that
the final merged candidate has passed its browser retest. Final-candidate
render acceptance is pending while the shared Ops HTTP service times out;
unfinished queries and partial captures are rejected rather than counted as
successful renders.

The merged source passed 849 related tests with eight explicit skips: five
retired-workflow cases, two POSIX bootstrap cases on Windows and the opt-in
live panel-fill test. The full merged promtool suite passed. Navigation,
scenes parity, first-window layout, readability, empty-state, filter/link,
query-budget and metric-inventory contracts are included in that validation.
The full repository suite is delegated to PR CI. Canonical scripts and test
governance inventories were refreshed after integration with main. Runtime
agent/skill sources were not edited; Codex–Junie mirror parity, documentation
links and drift checks passed. Debt governance passed with unchanged budgets.
The follow-up CI repair checks passed (26 tests), and the documentation metric
inventory passed all 15 governance tests after canonical regeneration. Test
telemetry uses actual coverage/duration artifacts from PR run `34138599480`,
whose other failed contracts were repaired separately; that run is not claimed
as an overall CI pass. Persistent memory writes are skipped in read-only mode.
