# Dashboard UX check — 2026-08-11 visual-audit closeout

## Scope

- merged sequential multi-expand layout bands for Trust, Pipeline Diagnostics,
  and Data Quality from PRs #8577 and #8582;
- Incident Workspace panel `2006` (`Track Alert State History`) reduced from
  `24×14` to `24×8`;
- Incident downstream panels moved upward by six grid units without changing
  their size or order;
- DQ regression geometry synchronized with the shipped post-#8582 layout.

## Semantic contract review

- Incident panel `2006` keeps the same `ALERTS` range query, datasource,
  state mappings, fixed FIRING/PENDING colors, row labels, value labels,
  tooltip, and `maxDataPoints`.
- No Prometheus metric, selector, recording rule, SLI, SLO, alert rule,
  dashboard variable, datasource, or operator verdict changed.
- No `run_id`, `manifest_id`, payload hash, record ID, or other
  high-cardinality Prometheus label was introduced.
- Missing data remains explicit; this change does not convert missing evidence
  into a healthy state.

## Static verification

- dashboard JSON syntax and whitespace validation;
- Grafana render-first, surface, DS2, and configuration integration contracts;
- dashboard visual semantics and strict performance budgets;
- dashboard inventory, panel audit, title, PromQL-scope, and duplicate-query
  checks;
- regenerated ADR-053 JSON-to-Scenes parity ledger.

## Visual verification protocol

The immutable live capture is intentionally performed from the resulting
`main` commit, after this layout change merges. The required evidence is:

1. all seven dashboards at 1920×1080, dark theme, kiosk/full mode, 100% zoom,
   default variables and default row state;
2. default-row full-page and force-expanded full-page captures;
3. close-ups for Trust panels `9413–9417`, Overview current row `9030`, DQ
   panels `9101–9102`, and Incident panel `2006`;
4. immutable manifest with commit SHA, dashboard JSON SHA-256, exact PNG file
   set and hashes, theme/viewport/zoom/variables/time/row state, and terminal
   panel classifications;
5. explicit review of overlap, clipping, internal scroll, query errors,
   no-data semantics, state transitions, and operator-fact preservation.

The final RF-004 capture and human image review completed on 2026-08-11 after
PR #8636 restored DQ progressive disclosure. The accepted immutable baseline
is rooted at
`reports/observability/grafana/visual-baseline-20260811-rf004-closeout-f942f62/`
and records source commit `f942f62e8622e4e7175cbad46797003ac0ac45c4` with
a clean working tree. That commit contains the DQ correction from
`e84f741a88d00be535b43d6133dc779a7dc1a8a9`; later documentation-only commits
do not change `grafana/dashboards/**`.

## Visual acceptance result

**Accepted.** The default-row first fold, default-row full-page, and
force-expanded full-page images were reviewed for all seven dashboards. The
canonical full-set manifests contain exactly seven PNGs each, all dashboard
JSON hashes match the reviewed source, and the render preflight reports no
`RENDER_ERROR`.

- Trust: the real expanded Grafana render preserves the operator guidance,
  clearly exposes `UNKNOWN`/`INCOMPLETE` without false-green states, and shows
  panels `9413–9417` without overlap or clipping. The separate fixture HTML
  close-up matrix is not represented as a live Grafana render; it was used to
  verify the same table fields for populated, valid-empty/unknown,
  backend-error, HTTP 503, and Infinity `noValue` states.
- Overview: the current-state row, First Action, domain status, range evidence,
  diagnostics, and run context remain legible with explicit no-data semantics
  and no overlap, clipping, or internal scroll.
- Data Quality: panels `9101–9102`, range evidence, and expanded diagnostics
  remain legible. Current `UNKNOWN` and missing evidence are explicit even when
  historical/range data contains a 100% score.
- Incident: panel `2006` renders at `24x8` with FIRING/PENDING history visible
  and no internal scroll. Downstream panels remain aligned and non-overlapping.

The acceptance evidence and machine-readable status are stored beside the
ignored local baseline as `VISUAL_ACCEPTANCE.md`, `fact-recalculation.json`,
and `load-p95.json`. The three immutable capture occurrences and all earlier
baselines remain unmodified.

## Final RF-004 quantitative revalidation

- default-row first fold: 7/7 PNGs, 45 viewport-intersecting facts, manifest
  SHA-256 `6ee502087ca200c7ef5121e2e82fd2e68d7406eb082c2f70ae8bb73f7334fe47`;
- default-row full page: 7/7 PNGs, manifest SHA-256
  `0ca3870c0235839fc3eb174c0dd78fdac5b97286c2066f0994a5dd7ba3231162`;
- force-expanded full page: 7/7 PNGs and 197/197 unique non-row panels,
  manifest SHA-256
  `6de9acfa01575f51f21a7d723b173d2e8c9e184353f13da2357b9e0a5cac2191`;
- compatible-state panel mappings: zero duplicates; observed render and
  query/backend error states: zero;
- first-fold type-normalized counts: 19 text, 13 stat, 12 table, and one
  state-timeline fact; the evidence file also records facts per portfolio
  viewport megapixel;
- load p95 (`domcontentloaded` to first visible panel): 1349 ms versus the
  previous accepted 1433 ms; the +10% ceiling is 1576.3 ms;
- strict performance budgets: PASS; DQ default load is two PromQL queries and
  its longest first-screen expression is 130 characters;
- render preflight: PASS for Grafana health/auth, Playwright runtime,
  expanded-row capture, immutable manifests, PNG file sets, actual
  viewport/theme, and terminal panel states.

The controlled Trust state matrix remains at
`reports/observability/grafana/visual-baseline-20260811/trust-closeups-by-state/`:
26 fixture-rendered close-ups distinguish populated, valid-empty/unknown,
backend-error, service-unavailable, empty-row, and zero-failure states. These
fixtures are supporting contract evidence and are not described as live
Grafana captures.

## Acceptance target

- **PASS** — Incident panel `2006` renders at `24×8` with all visible alert
  rows and transitions preserved and no internal scroll;
- **PASS** — downstream Incident panels start at `y=32`, `y=36`, and `y=37`
  with no overlap;
- **PASS** — Trust, Overview, and DQ evidence contains no truncation or layout
  drift;
- **PASS** — all seven dashboards reach terminal render states without
  `RENDER_ERROR` or unjustified `QUERY_ERROR`.
