# Dashboard UX check — 2026-08-05 threshold semantics repair

## Scope

- threshold base colors in `bioetl-control-plane-v1.json`, `bioetl-dq-v2.json`,
  `bioetl-incident-v1.json`, `bioetl-overview-v2.json`,
  `bioetl-provider-health-v2.json`, and `bioetl-runtime.json`
- threshold color values only; dashboard UIDs, panel IDs, queries, variables,
  navigation, time ranges, and drilldowns are unchanged

## Contract review

- Threshold palettes now use the canonical named color `gray` for null/base
  steps while preserving existing null value mappings.
- The affected panels continue to distinguish absent evidence from healthy,
  warning, and critical values.
- No metric names, PromQL selectors, datasource assumptions, or operator
  workflow were changed.
- The shipped `0..6` navigation bus and `${__url_time_range}` handoffs remain
  unchanged.

## Verification

- JSON syntax validation
- dashboard visual-semantics integration test
- Grafana config integration test
- ADR-053 Scenes parity ledger regeneration and contract test

## Outcome

The localized threshold repair is suitable for the shipped dashboard surface;
no live Grafana service is required to validate this JSON-only semantic change.

## Browse Recent Runs Completed timestamp

### Scope

- `bioetl-run-explorer-v1.json`, panel `Browse Recent Runs` (`id=3010`)
- display formatting for the `completed_at` field only

### Contract review

- The `Completed` column now uses Grafana's canonical `dateTimeAsIso` unit.
- The Ops HTTP endpoint, returned timestamp value, run selection, sorting,
  navigation, time range, and empty-state behavior are unchanged.

### Verification

- dashboard JSON syntax validation
- Run Explorer/Grafana integration contracts
- ADR-053 Scenes parity ledger regeneration and contract check
