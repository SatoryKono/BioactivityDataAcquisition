# Grafana acceptance continuation — 2026-09-23

Run Explorer panel 3010: Grafana 12.0.0 applies the wrap option from hidden Workflow to every row cell. The canonical generator now disables that option. A regression checks defaults and every override; the generated dashboard and Scenes ledger were refreshed.

Verification: the broad run collected 821 tests with 812 passed, 9 skipped and no failures/errors. One skipped freshness check was rerun after committing and correctly rejected the old report date; this dated report addresses that failure. The other skips concern retired dashboards, POSIX-only checks and opt-in live panel fill. Canonical generator and Scenes parity checks passed. No application Python or AI runtime sources changed; module-coverage refresh and runtime mirror synchronization are not applicable.

Live data: 131 Prometheus requests returned no errors or warning/info notices. DQ 10/155, Runtime 209/210 and several provider branches still lack real observations. No missing value was replaced by zero or OK. Three historical report-absent runs have ledgers ending at execute_pipeline start, without terminal evidence; preflight zeros cannot establish processing totals.

Visual acceptance remains BLOCKED. Render API captures show loading rather than a populated table; a requested 900px render was actually 1000px. Browser control stopped because the current URL could not be determined reliably. No screenshot PASS, complete matrix acceptance or issue closure is claimed. Raw evidence remains local. Issues 10570, 10571 and 10554 remain open.


## Catalog-only Trust follow-up

A populated 900x920 Run Explorer render showed ten unwrapped rows and all seven navigation links, but no Trust column when every row came from the ledger without a report. Catalog rows lacked processing_status/trust_status entirely. The API now includes the observed processing state and explicit UNKNOWN trust; saved-report fields still take precedence when merged. Tests cover unfinished, manifest-only, failed, and saved-report rows.

Real partial DQ telemetry is now independently evidenced: in 7d query_range, the hard branch for two ChEMBL pipelines contains measured zero increases while the soft branch is absent; the combined total remains absent, not zero. Positive missing-event scenarios remain unverified. Renderer minimum width 1000 and early query give-up impeded captures; temporary 900px/45s settings produced one populated 900x920 capture. The 1320x920 capture still showed loading. Temporary runtime settings were restored. Full acceptance remains open.

## Rolling-window legend reconciliation

Read-only capture at Unix time 1790143131.6681554 covers all fourteen panels
listed in issue 10570 and all 27 entity pipeline configurations: 1,296 target /
pipeline / time-window contexts reduce to 606 distinct query_range requests.
All returned successful responses without Prometheus warning/info notices.
Live target definitions matched the local canonical definitions at capture.
Steps were explicitly 30s, 120s and 1800s for 1h, 12h and 7d; these are audit
resolutions, not a claim of exact Grafana interpolation at every viewport.
Provider and GLOBAL queries are shared evidence, not individual run outcomes.

A real health degradation counter exposed an additional aggregation defect:
with a fixed 1830s increase window and fixed time bounds, the legend sum was
82, 20 and 2 for query steps 30s, 120s and 1800s, respectively. Maximum window
increase remained 2. Summing overlapping windows is not an event total. The
canonical generator now replaces legend sum with lastNotNull for rate/increase
timeseries, preserves other reducers, and explains that Last is an observed
window. Queries, thresholds, missing-data values and historical evidence are
unchanged. DQ panel 10 and Provider panels 106/109/111/115/32 are affected.

The new regression failed on both affected dashboards before the fix. After
generation, 77 scoped metric/readability/first-window tests passed. Expanded
verification passed all 121 scoped tests after correcting two stale assertions
for the existing DQ action_scope label wrapper; the numeric expressions remain
checked exactly. All seven
generator checks, Scenes parity, Ruff and 15 isolated promtool counter/window/
histogram-generation cases passed. No application or AI runtime Python changed;
module-coverage rebind and runtime mirror synchronization are not applicable.

Browser control is available again. Loaded DQ partial-coverage and empty-state
panels were inspected at 1320x920 and 900x920. A separate Provider preview at
both dimensions shows the real degradation peak and Last instead of Total;
absent retry evidence still says TELEMETRY MISSING. Screenshots, dimensions,
source hashes, exact query bounds and raw responses remain local. This is
scoped evidence, not acceptance of all panels or all matrix combinations.
Live positive-event verification for absent metric families remains BLOCKED;
local promtool fixtures do not replace those observations. Issues 10570,
10571 and 10554 are not closed by this change.

Local evidence SHA-256: query capture `5c4960385b666da3fa945b0ab257066ec09092ff29e0daa484a832962bb3d739`;
overlap reproduction `147763351614145d5f8b2ee484ced129c324c96125bb54518ebcf10feed825eb`. The local manifest binds
eight before/after and DQ screenshots. DOM receipts confirm innerWidth /
innerHeight of 1320x920 and 900x920; returned images omit viewport edges
(1305x910 and 885x905 pixels). Image dimensions are recorded separately
and are not substituted for the browser viewport.

## Complete inventory delta

All seven dashboards, 262 elements, 27 entity configurations and 59,474 original
matrix rows remain accounted for. Twelve changed histogram targets affect
2,724 contexts; 990 deduplicated instant/query_range requests were recaptured
without errors or notices. Another 15,436 contexts have presentation changes;
41,314 definitions are unchanged. The matrix keeps semantic and UI acceptance
separate: these query receipts assign no new semantic or visual PASS.

The canonical report-bind check passed for chembl_assay, including Ops and the
host/container source marker. Main Ops recent catalog rows still omit the new
processing_status/trust_status fields, so a merged code fix does not yet prove
runtime acceptance. PR 10633 has an explicit GitHub billing-lock annotation;
required jobs did not execute. CI remains BLOCKED_EXTERNAL.

## Isolated measured-zero visibility

Provider panel 32 contained a real single zero, confirmed independently in
Grafana Inspect Data. With automatic points and a hidden legend, the chart
looked empty. The canonical generator now always draws its points and shows
the observed Last and Max values. No missing series is created or replaced.
A regression failed before the fix; all 121 scoped tests passed afterwards,
as did canonical generation, Scenes parity and Ruff checks.

Browser screenshots at 1320x920 and 900x920 show the isolated zero point and
readable chembl Last 0 / Max 0 legend. Inspector and after-fix screenshots
remain local. This confirms that measured-zero case, not positive trips or
every missing-event family. Full acceptance remains open.

## Visual follow-up implementation

[Browser evidence and implementation matrix for #10614–#10624](2026-09-23-vis-followups.md). Candidate verification is separate from the earlier production audit; CI and final closure remain blocked.
