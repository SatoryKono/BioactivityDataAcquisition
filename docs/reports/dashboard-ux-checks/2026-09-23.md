# Grafana acceptance continuation — 2026-09-23

Run Explorer panel 3010: Grafana 12.0.0 applies the wrap option from hidden Workflow to every row cell. The canonical generator now disables that option. A regression checks defaults and every override; the generated dashboard and Scenes ledger were refreshed.

Verification: the broad run collected 821 tests with 812 passed, 9 skipped and no failures/errors. One skipped freshness check was rerun after committing and correctly rejected the old report date; this dated report addresses that failure. The other skips concern retired dashboards, POSIX-only checks and opt-in live panel fill. Canonical generator and Scenes parity checks passed. No application Python or AI runtime sources changed; module-coverage refresh and runtime mirror synchronization are not applicable.

Live data: 131 Prometheus requests returned no errors or warning/info notices. DQ 10/155, Runtime 209/210 and several provider branches still lack real observations. No missing value was replaced by zero or OK. Three historical report-absent runs have ledgers ending at execute_pipeline start, without terminal evidence; preflight zeros cannot establish processing totals.

Visual acceptance remains BLOCKED. Render API captures show loading rather than a populated table; a requested 900px render was actually 1000px. Browser control stopped because the current URL could not be determined reliably. No screenshot PASS, complete matrix acceptance or issue closure is claimed. Raw evidence remains local. Issues 10570, 10571 and 10554 remain open.


## Catalog-only Trust follow-up

A populated 900x920 Run Explorer render showed ten unwrapped rows and all seven navigation links, but no Trust column when every row came from the ledger without a report. Catalog rows lacked processing_status/trust_status entirely. The API now includes the observed processing state and explicit UNKNOWN trust; saved-report fields still take precedence when merged. Tests cover unfinished, manifest-only, failed, and saved-report rows.

Real partial DQ telemetry is now independently evidenced: in 7d query_range, the hard branch for two ChEMBL pipelines contains measured zero increases while the soft branch is absent; the combined total remains absent, not zero. Positive missing-event scenarios remain unverified. Renderer minimum width 1000 and early query give-up impeded captures; temporary 900px/45s settings produced one populated 900x920 capture. The 1320x920 capture still showed loading. Temporary runtime settings were restored. Full acceptance remains open.

## Rolling-window legend reconciliation

Read-only capture at Unix time 1790143131.6681554 covers all fourteen panels
listed in issue 10570 and all 27 entity pipeline configurations: 1,296 target /
pipeline / time-window contexts reduce to 606 distinct query_range requests.
All returned successful responses without Prometheus warning/info notices.
Live target definitions matched the local canonical definitions at capture.
Steps were explicitly 30s, 120s and 1800s for 1h, 12h and 7d; these are audit
resolutions, not a claim of exact Grafana interpolation at every viewport.
Provider and GLOBAL queries are shared evidence, not individual run outcomes.

A real health degradation counter exposed an additional aggregation defect:
with a fixed 1830s increase window and fixed time bounds, the legend sum was
82, 20 and 2 for query steps 30s, 120s and 1800s, respectively. Maximum window
increase remained 2. Summing overlapping windows is not an event total. The
canonical generator now replaces legend sum with lastNotNull for rate/increase
timeseries, preserves other reducers, and explains that Last is an observed
window. Queries, thresholds, missing-data values and historical evidence are
unchanged. DQ panel 10 and Provider panels 106/109/111/115/32 are affected.

The new regression failed on both affected dashboards before the fix. After
generation, 77 scoped metric/readability/first-window tests passed. Expanded
verification passed all 121 scoped tests after correcting two stale assertions
for the existing DQ action_scope label wrapper; the numeric expressions remain
checked exactly. All seven
generator checks, Scenes parity, Ruff and 15 isolated promtool counter/window/
histogram-generation cases passed. No application or AI runtime Python changed;
module-coverage rebind and runtime mirror synchronization are not applicable.

Browser control is available again. Loaded DQ partial-coverage and empty-state
panels were inspected at 1320x920 and 900x920. A separate Provider preview at
both dimensions shows the real degradation peak and Last instead of Total;
absent retry evidence still says TELEMETRY MISSING. Screenshots, dimensions,
source hashes, exact query bounds and raw responses remain local. This is
scoped evidence, not acceptance of all panels or all matrix combinations.
Live positive-event verification for absent metric families remains BLOCKED;
local promtool fixtures do not replace those observations. Issues 10570,
10571 and 10554 are not closed by this change.

Local evidence SHA-256: query capture `5c4960385b666da3fa945b0ab257066ec09092ff29e0daa484a832962bb3d739`;
overlap reproduction `147763351614145d5f8b2ee484ced129c324c96125bb54518ebcf10feed825eb`. The local manifest binds
eight before/after and DQ screenshots. DOM receipts confirm innerWidth /
innerHeight of 1320x920 and 900x920; returned images omit viewport edges
(1305x910 and 885x905 pixels). Image dimensions are recorded separately
and are not substituted for the browser viewport.

## Complete inventory delta

All seven dashboards, 262 elements, 27 entity configurations and 59,474 original
matrix rows remain accounted for. Twelve changed histogram targets affect
2,724 contexts; 990 deduplicated instant/query_range requests were recaptured
without errors or notices. Another 15,436 contexts have presentation changes;
41,314 definitions are unchanged. The matrix keeps semantic and UI acceptance
separate: these query receipts assign no new semantic or visual PASS.

The canonical report-bind check passed for chembl_assay, including Ops and the
host/container source marker. Main Ops recent catalog rows still omit the new
processing_status/trust_status fields, so a merged code fix does not yet prove
runtime acceptance. PR 10633 has an explicit GitHub billing-lock annotation;
required jobs did not execute. CI remains BLOCKED_EXTERNAL.

## Isolated measured-zero visibility

Provider panel 32 contained a real single zero, confirmed independently in
Grafana Inspect Data. With automatic points and a hidden legend, the chart
looked empty. The canonical generator now always draws its points and shows
the observed Last and Max values. No missing series is created or replaced.
A regression failed before the fix; all 121 scoped tests passed afterwards,
as did canonical generation, Scenes parity and Ruff checks.

Browser screenshots at 1320x920 and 900x920 show the isolated zero point and
readable chembl Last 0 / Max 0 legend. Inspector and after-fix screenshots
remain local. This confirms that measured-zero case, not positive trips or
every missing-event family. Full acceptance remains open.

## Visual follow-up implementation

[Browser evidence and implementation matrix for #10614–#10624](2026-09-23-vis-followups.md). Candidate verification is separate from the earlier production audit; CI and final closure remain blocked.


## Acceptance continuation: compact Trust titles and typed selectors

The 900x920 browser reproduced clipped Trust KPI headers and the Infinity
variable error `at least one field expected for variable`. Compact KPI names
retain the full scope in descriptions. The control-plane selector now uses
backend parsing so Grafana receives typed `__text`/`__value` fields directly.
A successful empty items array becomes a visible `NO MATCHES` selector option;
a missing items envelope remains a query error. This does not emit telemetry
or turn missing measurements into zero/OK. Run IDs remain unchanged values.

A live isolated Infinity probe verified a populated response, an empty array,
and an error envelope. The loaded Trust preview was inspected at 1320x920
and 900x920; the compact titles fit and the pipeline options load. The initial
`Monitor Checkpoint` shortening still clipped at 900; `Track Checkpoint` was
then verified visibly. Raw screenshots and API bodies remain local.

This receipt is scoped to the above fixes. Full matrix acceptance and the
EPIC remain pending; query success alone is never a semantic or visual PASS.

## Acceptance continuation: detail KPI widths

Expanded Trust sections reproduced truncated failure names at 900x920;
the global checkpoint admin failure title also clipped at 1320x920.
Narrow cards now use eight grid columns and at least three rows, retaining
queries, thresholds and scope descriptions. The global admin failure header
is shortened with its checkpoint scope retained in the description. The
existing minimum height and scalar density gates remain enforced.
Loaded browser screenshots at both sizes confirm readable checkpoint,
manifest/ledger and lineage card titles. First-window geometry is unchanged.

The 31 scoped readability, first-window, provenance and navigation checks
passed, and canonical generation is stable. Separate full-matrix source
collection encountered explicit capacity/deadline errors during concurrent
requests; those responses remain failed evidence pending serial rechecks.
This visual receipt does not classify those requests as successful.

## Acceptance continuation: manifest catalog deadline

The complete 12,199-request sweep preserved 227 transport/domain failures
for serial rechecks. Several latest-complete-run calls still returned
`deadline_exceeded` when queried sequentially. In the deployed container,
enumerating and validating 472 manifest files took 13.886 seconds, exceeding
the endpoint's 12-second deadline before evidence evaluation could begin.
A read-only four-worker probe over the same catalog took 5.495 seconds.

The candidate uses four bounded file-read workers and retains the existing
manifest parser, run-index validation, deterministic sorting and exception
propagation. It adds no cross-request cache and changes no timeout or verdict.
A concurrency regression fails with one worker and passes with four; 43
manifest storage/index/inspection tests pass. Deployment and endpoint-level
acceptance are separate from these timings and remain to be verified.

The deployed candidate read 480 manifests in 2.225 seconds. Serial replay of
all 227 originally failed requests reduced failures from 77 to two, both in
latest-complete discovery. The remaining profile showed unnecessary archive
and lineage checks for shutdown/failed candidates. Discovery now reads the
terminal ledger first and skips those artifact checks for non-success runs.
Successful runs still require the same full Trust evaluation, using the same
immutable ledger snapshot. Ordinary Trust summaries retain all checks.

The 97 related service, HTTP and retention checks pass. Two older HTTP test
expectations were aligned with existing contracts: broad discovery scopes
return an explicit non-candidate reason, and unreadable checkpoint evidence
is UNKNOWN/INCOMPLETE with an UNAVAILABLE diagnostic. Neither is OK.
The production-source hash guard passed without skips after both commits.
On deployed source b3ce5bdcf2ce, all 227 previously failing requests passed
serial replay with no transport/domain error; the slowest took 9.601 seconds
against the unchanged 12-second budget. Original failed responses remain
preserved separately. This does not close the full semantic/visual matrix.

## Acceptance continuation: Overview at the required viewports

Expanded Overview panels were inspected at 1320x920 and 900x920, including
domain timelines, diagnostics, range counters, identity and saved evidence.
The narrow first-window title `Review Selected Run Domains` clipped; its
compact `Review Run Domains` replacement fits at both sizes while descriptions
and the selected-run row retain scope. The preview was rebuilt after merging
main 702f6baeb83c so the accepted Incident widths, Provider suspect height,
DQ signal width and first-window table formatting are preserved.

After integration, 784 dashboard checks pass and eight skip (five retired
dashboard cases, two POSIX bootstrap checks, one opt-in live fill check).
These contract results and layout screenshots do not establish every
pipeline/scenario semantic verdict. Full acceptance remains open.

## Acceptance continuation: live execution presentation

The source-response reconciliation covers 106 selected-run requests: 77
selection-required contexts, 24 frozen assessments, two legacy reports and
three runs without final reports. Frozen assessment verdicts, domain reasons
and execution states match their saved reports. A failed legacy report remains
ERROR; a successful legacy report without a snapshot remains INCOMPLETE.

The three live diagnostic responses exposed a projection defect: canonical
execution/heartbeat fields reported RUNNING/STALE while Grafana presentation
rows retained UNKNOWN/NOT EVALUATED. The handler now updates summary, Trust
and domain presentation together from the same ledger diagnostic. It avoids
embedding presentation collections inside summary rows. Missing checks remain
UNKNOWN and evidence remains INCOMPLETE; no freshness verdict is fabricated.
All 128 selected-run endpoint tests pass, including started, finished, failed
and shutdown regression cases. Deployed verification remains required.

The opt-in live fill pytest run exceeded its overall 300-second limit while
connecting; it is not counted as PASS. A separate bounded-per-request checker
is collecting a persistent receipt. Local response and screenshot artifacts
remain separate from published summaries and do not imply completed ACCEPT.

The separate live fill receipt completed: 185 queryable panels, zero skipped
and zero gateway/query errors (2026-09-23 10:10:58 UTC). All three diagnostic
projection rechecks matched after deployment of image source 52f1e417c68f.
These checks validate projection consistency, not independent proof that an
old ledger-only run still has a live process. After rebasing the fix onto
main fa6b4b7c024a, 128 endpoint and two inventory freshness checks passed.

Runtime panel 9102 clipped its Metrics Coverage title at 900x920. The canonical
compact title is now `Monitor Coverage`; loaded previews at 900x920 and
1320x920 retain all four first-window panels and all seven navigation links.
The related 169 contract/readability tests pass; queries, units and missing
evidence semantics are unchanged. The remaining Runtime detail sections and
the complete cross-pipeline semantic/UI matrix still require acceptance.

The expanded Runtime cause tables also clipped `transform` and `quarantined`
with an 80-pixel Stage column; panels 256 and 241 now reserve 130 pixels.
Four escalation card titles were compacted without changing scope or queries.
Loaded browser captures show complete Stage values and all four titles at
both required sizes. The broader 818-test dashboard suite finishes with 810
passed and eight explicit skips (five retired dashboard cases, two Windows
POSIX limitations, one opt-in live check covered by the separate receipt).
The initial broad run caught the stale inventory title contract; it was
corrected before the passing rerun, not suppressed.

Provider Health panel 9107 clipped the full provider identifier in its reason
table. The canonical widths now reserve 140 pixels for Provider, 70 for
Source and 90 for Status, leaving Reason flexible. Both loaded preview
viewports show the complete identifier and all four columns. The 131 related
readability, first-window, layout, semantics and provenance tests pass. This
is preview evidence, not acceptance of every provider or production deploy.

The full fleet table (9111) also preserves long provider names without a
last-letter wrap. Provider card 102 now uses `Inspect Health p95`; the full
health-check latency scope remains in its description. Both viewport
captures confirm the compact heading. The 131-test rerun passes.

The recursive dashboard suite covers 1223 cases: 1215 pass and eight remain
explicitly skipped. Its initial run found a four-pixel 200% width overflow,
two stale width expectations, an Overview panel-document title and a
UID-less generator fixture failure. Source width is 70 pixels, preserving
the containment budget; missing UID uses generic presentation handling.
All five failures were corrected and the complete suite rerun passed.
Ruff also passes. These local checks do not replace blocked GitHub Actions.

The source-bound 7a2d3373a620 replay completed 227 requests with two
latest-complete deadline errors; both targeted retries returned within budget,
so the intermittent errors remain recorded. A component profile showed a
candidate spending time on lineage before an UNKNOWN retention result.
Candidate discovery now rejects the first non-OK component and checks
retention before lineage. Full Trust diagnostics still evaluate every
component. All-OK discovery still requires every component. The 37 focused
and 57 broader evidence tests pass; runtime revalidation is pending.

DQ preview acceptance at 900x920 and 1320x920 found clipped score titles,
an internally scrolling coverage explanation, and a three-line pipeline name
in Gold outcomes. Canonical compact score titles, a four-row coverage panel
and 200/100/80-pixel Gold columns resolve these defects in loaded captures.
All DQ detail sections were inspected for this no-selected-run context;
source queries reconcile nonzero quarantine categories separately from
measured zero Gold-specific outcomes. Missing
Silver and threshold series remain UNKNOWN or TELEMETRY MISSING. This does
not establish every pipeline/run/time combination as PASS.

The DQ recursive suite has 1223 cases, no failures, and nine explicit skips.
The extra UX freshness skip was caused by Git ownership configuration in
that invocation; an exact-safe-directory rerun passes both freshness tests.
The other eight skips retain the documented platform/retired/opt-in reasons.
All seven canonical generator checks and documentation inventory check pass.
