id: EV-domain-config-resolver-now-owns-yaml-plus-dq-domain-bridge
pillar: pipeline-config-loader-ownership
source:
  type: code
  ref: "src/bioetl/infrastructure/config/domain_config_resolver.py:1-45; src/bioetl/infrastructure/config/_base.py:44-79"
  retrieved_at: 2026-03-20
claim: "The canonical YAML + DQ -> domain bridge now sits in `DomainConfigResolver`, and `get_pipeline_config()` already routes through that resolver instead of manually using `PipelineConfigLoader` as the main owner."
quote: "`DomainConfigResolver` is documented as the \"Canonical domain-config resolution flow\" and `get_pipeline_config()` composes `load_pipeline_config()` with `resolver.resolve(...)`."
confidence: 0.97
assumptions:
  - "The `get_pipeline_config()` convenience path is intended to represent the canonical public bridge for callers that want domain config directly."
notes: "This evidence narrows `PipelineConfigLoader` to a subordinate role in the end-to-end pipeline config story: it still participates in DQ resolution, but no longer defines the top-level canonical path."
tags:
  - rf-fs-004
  - domain-config
  - dq
  - ownership
  - bridge
