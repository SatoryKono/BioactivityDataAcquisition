id: EV-pipeline-config-loader-still-provides-tested-convenience-surface
pillar: pipeline-config-loader-ownership
source:
  type: code
  ref: "src/bioetl/infrastructure/config/pipeline_config_loader.py:48-108,110-203; tests/integration/config/test_dq_config_loading.py:83-114"
  retrieved_at: 2026-03-20
claim: "Despite losing canonical ownership, `PipelineConfigLoader` still provides a live convenience surface for uncached YAML loading, injected DQ/filter loader composition, DQ resolution, and cache clearing, and that surface is directly integration-tested."
quote: "`PipelineConfigLoader` still exposes `load_pipeline_config()`, `resolve_dq_config()`, and `clear_cache()`, and the integration tests exercise those methods directly."
confidence: 0.95
assumptions:
  - "The direct integration tests represent intentional retained usage rather than dead legacy coverage."
notes: "This is the main evidence against deleting or aggressively collapsing the class in the current wave. It still has operational value as an infrastructure convenience seam."
tags:
  - rf-fs-004
  - pipeline-config-loader
  - integration-tests
  - convenience
  - retained-seam
