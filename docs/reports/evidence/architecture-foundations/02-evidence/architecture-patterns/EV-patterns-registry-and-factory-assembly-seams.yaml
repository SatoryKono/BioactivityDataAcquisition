id: EV-patterns-registry-and-factory-assembly-seams
pillar: architecture-patterns
source:
  type: internal-doc
  ref: "docs/03-guides/registry-pattern.md:1-24; docs/03-guides/registry-pattern.md:82-126; README.md:455-466; src/bioetl/composition/entrypoints.py:1-11"
  retrieved_at: 2026-03-20
claim: "A registry/factory assembly pattern is an active part of the architecture: `PipelineRegistry`, `ProviderRegistry`, and `DataSourceFactory` are documented as canonical composition-layer surfaces for pipeline and provider assembly."
quote: "`ProviderRegistry` is the canonical class-based provider registry"
confidence: 0.91
assumptions:
  - "The registry-pattern guide reflects the currently supported public assembly surfaces."
  - "The public entrypoint layer in `composition` still delegates to these registry/factory seams rather than bypassing them wholesale."
notes: "This pattern operationalizes the composition layer by turning assembly into explicit, reusable, and testable registry/factory APIs."
tags:
  - patterns
  - registry
  - factory
  - provider-registry
  - data-source-factory
  - composition
