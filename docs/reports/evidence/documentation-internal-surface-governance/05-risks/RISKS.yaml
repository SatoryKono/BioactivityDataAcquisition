- id: RISK-documentation-internal-surface-governance-ai-surface-may-stay-one-click-too-deep
  risk: "Keeping the AI top-level surface link-only may preserve a small onboarding friction because readers must enter through the project hub rather than seeing AI docs directly in nav."
  linked_decision: DEC-documentation-internal-surface-governance-keep-ai-top-level-index-link-only-for-now
  severity: low
  likelihood: medium
  triggers:
    - "Contributors repeatedly miss the AI top-level index during onboarding."
    - "More AI clusters are added and the current project hub link stops being enough."
  mitigations:
    - "Revisit nav promotion if future onboarding evidence shows recurring confusion."
    - "Keep the AI entry point prominent in `docs/00-project/index.md`."

- id: RISK-documentation-internal-surface-governance-repo-only-surfaces-may-be-missed-by-published-docs-readers
  risk: "Treating `plans/**` and `reports/**` as repo-only may reduce discoverability for readers who expect every useful surface to exist in the published site."
  linked_decision: DEC-documentation-internal-surface-governance-treat-plans-and-reports-as-repo-only-reference-surfaces
  severity: medium
  likelihood: medium
  triggers:
    - "Contributors repeatedly fail to find planning or evidence material from the published docs hub."
    - "Published pages stop carrying repository-path references to the repo-only surfaces."
  mitigations:
    - "Keep repository-path entrypoints explicit in `docs/00-project/index.md` and related maps."
    - "Add curated summaries in published docs when a repo-only cluster becomes high-traffic."

- id: RISK-documentation-internal-surface-governance-guard-green-state-may-hide-future-reference-drift
  risk: "Relying on the current green guard baseline may become stale if repository-path references or experimental docs drift again after this refresh."
  linked_decision: DEC-documentation-internal-surface-governance-keep-doc-sync-enforcement-in-guard-layer
  severity: medium
  likelihood: medium
  triggers:
    - "A future docs change reintroduces path mismatches or outdated script examples."
    - "Strict MkDocs build or `check-drift` starts failing after a new refactor wave."
  mitigations:
    - "Continue running `check-links`, `check-drift`, and strict MkDocs build as the refresh ratchet."
    - "Prefer small targeted fixes when the guard layer detects new drift."

- id: RISK-documentation-internal-surface-governance-experimental-deployment-sync-may-regress
  risk: "The experimental deployment subtree may drift again if the helper script, manifests, and narrative docs evolve independently."
  linked_decision: DEC-documentation-internal-surface-governance-keep-doc-sync-enforcement-in-guard-layer
  severity: low
  likelihood: medium
  triggers:
    - "The helper script changes image/tag behavior without updating deployment docs."
    - "Deployment docs regain hard-coded image tags or stale registry instructions."
  mitigations:
    - "Keep deployment docs aligned to the helper-script override contract."
    - "Verify experimental deployment docs whenever `scripts/ops/deploy-bioetl.sh` changes."

- id: RISK-documentation-internal-surface-governance-mixed-language-friction-may-linger
  risk: "Deferring dedicated RU/EN normalization may leave some readers with avoidable friction when moving between internal docs with different language and tone."
  linked_decision: DEC-documentation-internal-surface-governance-defer-dedicated-ru-en-normalization-wave
  severity: low
  likelihood: medium
  triggers:
    - "Future contributors report confusion caused by mixed-language guidance."
    - "Internal docs accumulate more mixed-language wording in high-traffic pages."
  mitigations:
    - "Normalize wording opportunistically when touching high-traffic internal pages."
    - "Escalate to a dedicated cleanup wave only if readability friction becomes recurrent."
