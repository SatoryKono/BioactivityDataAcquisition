# Retired `library/audit/project/new2/`

Unregistered 14-card megaprompt tree (`prompt.audit.project.new2.*`) with
library-default `ALLOW_*=true` (ADR-060 D2). Not default paste.

Canonical id resolution: `compatibility/prompt.audit.project.new2.<domain>.md`
→ `generated/<domain>/audit-readonly.md`. Overlay SSOT: `overlays/<domain>.yaml`.
