# Retired `library/audit/project/new/`

Unregistered 10-card megaprompt tree superseded by ADR-060 overlays +
`generated/<domain>/`. Not default paste. Prefer:

```bash
python -m scripts.ai.prompts compile --domain docs --profile audit-readonly
```

Frozen evidence remains in `library/audit/project/materialized-v3/`.
