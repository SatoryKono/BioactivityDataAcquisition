# Dashboard UX Check 2026-05-19

Scope:
- `grafana/dashboards/bioetl-runtime.json`
- `grafana/dashboards/bioetl-workflow-overview.json`

What changed:
- widened first-screen `Runtime Telemetry Gap` from a one-column marker to a
  readable first-screen trust stat
- normalized `bioetl-workflow-overview` `$workflow` selector to
  single-select with Include All

Operator impact:
- runtime first-screen zero-value cards now keep a visibly readable datasource
  trust marker above fold
- workflow dashboard handoff preserves one coherent `$workflow` shell value
  instead of allowing multi-select ambiguity

Manual spot-check summary:
- first-screen runtime row still fits `Runtime Status`, `Runtime Blockers`,
  `Runtime Telemetry Gap` on one row without overlap
- workflow first screen still keeps `First Action` beside the selected-range
  evidence cards
- dashboard-to-dashboard links remain same-tab and preserve bounded handoff
  vars only
