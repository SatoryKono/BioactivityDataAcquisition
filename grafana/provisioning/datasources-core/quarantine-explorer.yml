apiVersion: 1

datasources:
  - name: Quarantine Explorer
    type: yesoreyeram-infinity-datasource
    uid: quarantine-explorer
    access: proxy
    url: ${BIOETL_QUARANTINE_EXPLORER_URL}
    editable: true
    jsonData:
      allowedHosts:
        - ${BIOETL_QUARANTINE_EXPLORER_URL}
      auth_method: none
