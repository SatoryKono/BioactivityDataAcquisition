apiVersion: 1

# Legacy relocation tombstone. Pushgateway is a metrics ingestion endpoint,
# not a Prometheus query API, so this local-only provisioning surface must stay
# inert even if it is mounted accidentally.
datasources: []
