# Graph Exports

Generated deterministic graph snapshots are written here by
`python -m memory.tooling.refresh_all --include-graph-export` or direct
`memory.graph.sync --export ...` calls.

These artifacts are derived memory outputs, not source-of-truth. Rebuild them
from canonical repository sources rather than editing them manually.
