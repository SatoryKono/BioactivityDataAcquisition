# Curated Archive

Archived curated notes live here when they should no longer remain active memory.

Use:

```bash
python -m memory.tooling.archive_note --source src/memory/curated/lessons/example.md --reason "Superseded by newer guidance"
```

Archived notes remain versioned for traceability but are excluded from active curated validation and duplicate checks.
