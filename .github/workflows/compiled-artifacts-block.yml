name: Block Compiled Python Artifacts
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.ai/**'
      - '.github/workflows/**'
      - 'LICENSE'
  push:
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.ai/**'
      - '.github/workflows/**'
      - 'LICENSE'

jobs:
  no-pyc-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python + uv
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.12"
          uv-extras: "dev"

      - name: Fail if *.pyc or __pycache__ exists
        run: |
          set -e
          FOUND=$(find . -type d \( -name .venv -o -name .git \) -prune -o \( -name "*.pyc" -o -name "__pycache__" \) -print | head -n 1 || true)
          if [ -n "$FOUND" ]; then
            echo "Found compiled artifacts: $FOUND"
            exit 1
          fi
          echo "No compiled artifacts found"

      - name: Validate root-level allowlist
        run: uv run python -m scripts.engineering.repo check-cleanliness
