# Disabled (#11012): anomalyco/opencode/github still installs from releases/latest
# and pipes a remote script to bash. Do not restore that action, id-token: write,
# or secret env until the installer is digest-pinned.
name: opencode-pr-review

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: OpenCode review disabled
        run: echo "OpenCode review is disabled until the installer is digest-pinned."
