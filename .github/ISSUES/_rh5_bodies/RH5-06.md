## Parent

_TBD_ (RH5-00)

## Problem

`skills-lock.json` is allowlisted at root (~3 repo references). It may be movable under a vendor/agent tree **only if** the skill installer / lock tooling requires that path — not assumed.

## REVIEW only

- [ ] Identify tooling that reads root `skills-lock.json` (docs, CI, skill installer)  
- [ ] If exact-root required → retain; document in review registry lane  
- [ ] If configurable path → optional move + allowlist shrink in a dedicated PR  
- [ ] Do not move speculatively  

## Acceptance (for a move PR, if ever)

- [ ] Consumer map complete  
- [ ] Allowlist + policy + registry updated together  
- [ ] skills-consistency / related CI green  

## Priority

P3 REVIEW — no unsolicited move.
