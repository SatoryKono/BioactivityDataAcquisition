## Parent

_TBD_ (RH5-00)

## Problem

Unauthorized root scripts reappeared after RH4 closeout (`_cr_*.py`, `_publish_*.py`). Agents and one-off publishers keep treating the repo root as a scratch pad.

## Scope

- [ ] Reinforce in agent/operator guidance (prefer existing surfaces):
  - `docs/00-project/governance/root-local-clutter-cleanup.md`
  - `AGENTS.md` / AI policy only if a short bullet is missing (no doc sprawl)
  - optional note in `.github/ISSUES/CREATION_GUIDE.md` or RH pack index
- [ ] Explicit ban: root `_cr_*.py`, `_tmp_*.py`, `_publish_*.py`, ad-hoc `test_*.py`
- [ ] Correct placement: `scripts/engineering/repo/**`, `scripts/ops/**`, `reports/**` for outputs
- [ ] Do not restore root script allowlist exceptions

## Acceptance

- [ ] Guidance searchable for “root scratch” / `_cr_` / allowlist rule  
- [ ] No allowlist growth  
- [ ] No new root tracked files from this PR  

## Priority

P2 — prevention after P0 fix.
