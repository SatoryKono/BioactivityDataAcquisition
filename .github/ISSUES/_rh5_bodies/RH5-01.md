## Parent

_TBD_ (RH5-00)

## Problem

TYPE root-allowlist violation: **3 tracked root Python files** are not in `.github/root-allowlist.txt`.

| Path | Notes |
| --- | --- |
| `_cr_one_scope.py` | CodeRabbit/scratch helper naming |
| `_cr_write_final.py` | same family |
| `_publish_arch_cr2_issues.py` | one-off publisher |

Evidence (2026-07-29):

- `git ls-files` (root-only) → 40 files  
- allowlist parse → 37 entries  
- unexpected = the three paths above  
- `git status` shows them as `D` (removed in working tree, still in git index)

Policy: root `.py` scratch/compatibility scripts are **forbidden** (`03-file-policy.md` §0; RH closed root script exceptions). Do **not** add these to the allowlist.

## Scope

- [ ] `git rm` (or confirm absent + remove from index) all three paths
- [ ] If any logic is still needed, **rehome under** `scripts/engineering/repo/**` or `scripts/ops/**` with catalog ownership — **not** root
- [ ] Verify tracked root file set == allowlist (37)
- [ ] Do not grow allowlist

## Out of scope

- Docker compose moves
- `.env*` handling
- Allowlist content edits (except no growth)

## Acceptance

- [ ] `git ls-files | awk -F/ 'NF==1' | wc -l` → 37  
- [ ] `git ls-files | awk -F/ 'NF==1'` is exact set of allowlist entries  
- [ ] `uv run python -m scripts.engineering.repo check-cleanliness` (tracked allowlist check) green  
- [ ] No new root `*.py` introduced in the same PR  

## Files

- git index: `_cr_one_scope.py`, `_cr_write_final.py`, `_publish_arch_cr2_issues.py`  
- optional rehome under `scripts/**` only if product still needs the publishers  

## Priority

**P0** — blocks root-hygiene CI / cleanliness strict mode.
