## Parent

_TBD_ (RH5-00)

## Problem

`configs/quality/root_hygiene_review_registry.yaml` records:

```yaml
current_live_root_baseline:
  tracked_root_audit_status: pass
  note: ... 37≡allowlist restored ...
```

After RH5-01, the live baseline must be re-verified and the note updated so future audits do not assume a stale green state while index drift exists.

## Scope

- [ ] After RH5-01 merge: re-run cleanliness + structure audit  
- [ ] Update `current_live_root_baseline` note with date, SHA, tracked count, allowlist count  
- [ ] Align any prose in `03-file-policy.md` §0 only if numbers/examples drift (no policy rewrite)  
- [ ] Keep `reports/quality/rh5-2026-07-29-issue-publish.json` closeout fields accurate  

## Acceptance

- [ ] Registry note reflects post-fix 37≡37 (or documents intentional shrink)  
- [ ] `check-root-review-registry` / unit tests green  
- [ ] `check-root-governance-docs` green if docs touched  

## Depends on

- RH5-01 complete
