## Summary
Conduct a narrow published-docs sweep for remaining normalization/control-plane references so the documentation boundary matches the current `application/services/control_plane/*` topology.

## Problem Statement
The main normalization plan has already been updated to the current control-plane layout, but the last hierarchical audit showed that the broader published/reference docs perimeter is larger than a single source-of-truth file. That leaves a low-priority risk of guidance drift outside the core plan.

## Evidence
- Canonical plan already corrected in [docs/05-engineering/normalization_plan_P0_P6.md](docs/05-engineering/normalization_plan_P0_P6.md).
- Current runtime seams live in:
  - `src/bioetl/application/services/control_plane/run_manifest_service.py`
  - `src/bioetl/application/services/control_plane/run_ledger_service.py`
- The latest normalization audit concluded that any remaining risk is now in published docs/governance hardening, not in runtime normalization behavior.

## Goal
Ensure the remaining published normalization/control-plane references are either:
- updated to the current topology, or
- explicitly marked as historical where older paths are intentionally preserved for recordkeeping.

## Scope
- Search normalization-related documentation, guides, evidence summaries, and published references for stale control-plane path guidance.
- Update active guidance to current `control_plane` paths.
- Preserve historical records only when clearly labeled as historical.
- Avoid changing runtime code or import topology.

## Detailed Implementation Plan
1. Search `docs/` for references to old control-plane service paths and pre-`control_plane` wording.
2. Classify each hit as either active guidance or historical evidence.
3. Update active guidance to current paths.
4. Add a short historical note where an old path must remain for evidence/history reasons.
5. Re-run targeted grep checks and manually review the changed docs.

## Acceptance Criteria
- Active normalization/control-plane docs no longer point to outdated service paths.
- Historical references, if kept, are clearly labeled as historical.
- No runtime code changes are required.

## Validation
- `rg "application/services/run_manifest_service.py|application/services/run_ledger_service.py" docs`
- Manual review of changed docs to confirm only active guidance was updated.

## Priority
Low. This is documentation hardening, not a runtime correctness fix.
