# Incident review follow-up, 24 September 2026

Result: PARTIAL. Issue #10982 acceptance remains BLOCKED by unresolved #10981 and exact-SHA CI. This report supersedes the earlier render binding for the changed dashboards; it does not reclassify historical missing evidence or claim full portfolio acceptance.

Implemented: Dashboard datasource mirrors no longer contain PromQL; summary/full titles and descriptions differ; inherited suspects retain TIME RANGE scope. Window is visible, headers fit narrow renders, and all table cells have a minimum width. The measurement table explicitly covers runtime non-validation backlog, not the separate BioETLStageBacklogActive alert window. Provider first-window summaries retain their three-row cap with full-fleet navigation below. Overview shares one HTTP envelope while taking trust from presentation_summary, never from the first domain verdict. Missing lineage reasons have shared operator aliases. Incident display vectors now execute in the canonical Prometheus fixture. Inventory, content, navigation, docs and Scenes contracts are synchronized without raising budgets.

Validation: 838 dashboard/Grafana integration tests passed, 8 skipped (retired workflow dashboard: 5; POSIX-only bootstrap: 2; opt-in live panel-fill: 1). Separate reason-alias tests passed. Ruff and canonical generation/Scenes parity checks passed. Canonical check-prometheus-rules --runner docker passed, including all 57 alerts and the added boundary/stale/missing-provider vectors. Module inventory source hash refreshed; the clean-source architecture guard is rechecked after commit. AI runtime mirrors are unchanged. Debt budgets are unchanged.

Browser evidence: fixed 2026-09-24 00:00–10:40 UTC interval, selected semanticscholar_publication run 4dc5e1d1-c3fc-5fb6-a02b-27fd9fe741e1. Overview visibly preserves Runtime OK alongside aggregate INCOMPLETE and No verified archive. Incident measurements show 600.195 s, 830.192 s and backlog 2 with windows and unverified observation freshness. Screenshots prove presentation only, not correctness of source data. followup-render-manifest.json records requested viewports, actual encoded image sizes and SHA-256. Main-page captures exclude scrollbar strips; the measurement captures are exactly 1011x920 and 1366x768. No complete first-screen containment, zoom, color-contrast or theme-parity claim is made.

Source binding: followup-source-bind.json records live resource versions, exact panel equality, canonical panel JSON SHA-256 (UTF-8, sorted keys, compact separators, ensure_ascii=False), and full dashboard SHA-256 with LF line endings. LF normalization makes the latter comparable with committed Git blobs. This is dashboard parity, not whole-container source identity.

Selector investigation: the prior persisted 504 is real. New independent container reads: 627 reports / 1.473 s; 552 run manifests / 1.418 s; 122 workflow manifests / 0.540 s. These isolated successful reads do not explain historical deadline exhaustion. No timeout increase, empty-success fallback or selector-fix claim was introduced. #10981 remains open; a ten-reload browser stress matrix and incident-correlated profiling remain required. #10982 remains open and no full PASS is claimed.
