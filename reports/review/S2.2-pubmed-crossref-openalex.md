# S2.2 Sector Review: application/pipelines/pubmed/ + crossref/ + openalex/

**Reviewer:** py-audit-bot (L3 Worker)
**Date:** 2026-02-26
**Files reviewed:** ~30 Python files

---

## Summary

**Status: PASS (9.5/10)**

All three publication provider pipelines are well-structured, following the `BasePublicationTransformer` Template Method pattern. DI compliance is strong. Extractors are properly separated into modules.

## Findings

### ARCH-001: Import Boundaries -- CLEAN
- All imports from `bioetl.domain.*` or `bioetl.application.*`
- No infrastructure, composition, or interfaces imports
- TYPE_CHECKING blocks contain only domain and application imports

### AP-001/DI-001: Hard-coded Constructor Dependencies
- **PubMed transformer** (`transformer.py:129-130`): Creates `AuthorExtractor()` and `DateExtractor()` directly
  - **Verdict: NOT a violation** -- these are stateless, application-layer strategy objects within the same package (`application/pipelines/pubmed/extractors/`). They have no I/O, no external dependencies, and are pure transformation logic. This falls under EXC-003/EXC-015 pattern.

### AP-002: structlog -- CLEAN
- No direct structlog imports

### NAME-001: Class Suffixes -- CLEAN
- `OpenAlexPublicationTransformer`, `CrossRefPublicationTransformer`, `SemanticScholarPublicationTransformer` -- correct `*Transformer` suffix
- `BaseFieldExtractor`, `AuthorExtractor`, `DateExtractor`, `MedlineDateParser` -- correct suffixes

### TYPE-001: Public Function Annotations
- `BaseFieldExtractor` docstring examples show `def extract(self, element):` and `def normalize(self, raw_value):` without type hints, but these are **docstring examples only** (lines 21-23), not actual code. The actual abstract methods at lines 30 and 42 have full type annotations. **NOT a violation.**

### Notes
- CrossRef transformer correctly validates DOI in `_pre_extract_validation()` before extraction
- PubMed uses XML parsing with cached root element for efficiency
- All three providers implement `_classify_publication_type()` for unified classification

## Issues Found: 0
