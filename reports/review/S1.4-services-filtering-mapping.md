# S1.4 Code Review: domain/services/ + domain/filtering/ + domain/mapping/

**Reviewer:** L3 Worker (S1.4)
**Date:** 2026-02-26
**Scope:** `src/bioetl/domain/services/`, `src/bioetl/domain/filtering/`, `src/bioetl/domain/mapping/`
**Files reviewed:** 32
**LOC:** 6,841

---

## Summary

Domain services are pure (no I/O), well-typed, and follow DDD patterns. The `DefaultDataNormalizationService` is a frozen dataclass with comprehensive text normalization pipeline. Filtering configs use attrs/dataclass patterns with proper validation. Mapping modules contain large but well-organized pure lookup tables for publication type classification and organism classification.

---

## Issues Found

### ARCH (Architecture)

| # | Rule | Severity | File | Description |
|---|------|----------|------|-------------|
| - | - | - | - | No violations found |

**ARCH-001:** No imports from infrastructure, application, composition, or interfaces. PASS.
**ARCH-002:** No I/O operations. Services use `hashlib`, `re`, `unicodedata`, `html.unescape` -- all pure computation. `calendar.monthrange` is imported locally in one method. PASS.

### AP (Anti-Patterns)

| # | Rule | Severity | File | Description |
|---|------|----------|------|-------------|
| - | - | - | - | No violations found |

### DI (Dependency Injection)

| # | Rule | Severity | File | Description |
|---|------|----------|------|-------------|
| - | - | - | - | No violations found |

**Assessment:** `DefaultDataNormalizationService` is a frozen dataclass with a `config` field using `default_factory=DataNormalizationConfig`. This follows EXC-002 (Optional Parameters with Defaults) -- a valid DI pattern for configuration value objects.

### NAME (Naming)

| # | Rule | Severity | File | Description |
|---|------|----------|------|-------------|
| 1 | NAME-001 | MEDIUM | `dq_metrics_calculator.py` | `DQMetricsInput` -- dataclass without suffix. Should be `DQMetricsInputSpec` or similar. |
| 2 | NAME-001 | MEDIUM | `normalization_service.py` | `NormalizationResult` -- follows `*Result` suffix convention. PASS. |
| 3 | NAME-001 | MEDIUM | `organism_classification_service.py` | `ClassificationStats` -- follows naming convention implicitly. |

**Assessment:** `DQMetricsInput` is a minor naming issue -- it is an input data container. `NormalizationResult` and `ClassificationStats` are acceptable (both are data containers with descriptive names). Low impact.

### TYPE (Type Annotations)

| # | Rule | Severity | File | Description |
|---|------|----------|------|-------------|
| 1 | TYPE-002 | LOW | `data_normalization_service.py` | `Any` in `normalize_to_string`, `_try_json_loads`, `_filter_json_authors`, `_parse_authors_from_list` -- handling heterogeneous API data. Justified by context. |
| 2 | TYPE-002 | LOW | `filtering/_base_filter_config.py` | `Any` in `_check_op_*` methods -- filter value type varies per operator. Justified. |

---

## Observations

1. **Large mapping files:** `publication_type_classification.py` (~1600 LOC) and `organism_classification.py` (~400 LOC) are large but contain pure lookup tables with clear structure. Not god objects (EXC-005).
2. **Service decomposition:** Author normalization is properly extracted into `AuthorNormalizationService` (SRP). `DefaultDataNormalizationService` inherits from it and orchestrates the full normalization pipeline.
3. **Filtering config:** Well-organized hierarchy with `BaseFilterConfig` -> `GoldFilterConfig`, `SilverFilterConfig`, `InputFilterConfig`.

---

## Score Calculation

| Category | Weight | Max | Deductions | Score |
|----------|--------|-----|------------|-------|
| Architecture (ARCH) | 30% | 10 | 0 | 10.0 |
| Anti-Patterns (AP) | 25% | 10 | 0 | 10.0 |
| DI Violations (DI) | 20% | 10 | 0 | 10.0 |
| Naming (NAME) | 10% | 10 | -0.5 (1 MEDIUM) | 9.5 |
| Types (TYPE) | 10% | 10 | 0 | 10.0 |
| Testing (TEST) | 5% | 10 | 0 | 10.0 |

**Weighted Score:** 0.30(10) + 0.25(10) + 0.20(10) + 0.10(9.5) + 0.10(10) + 0.05(10) = 9.95
**Status:** PASS
