Canonical kit destination is `reports/audit/tests/` (gitignored working reports).
This sandbox cannot write gitignored `/reports/*` paths, so the 2026-08-26
tests-system audit pair is persisted here. Move/copy to `reports/audit/tests/`
locally if you need the kit path. Do not treat this tracked `reports/review/`
copy as a new SSOT.
