# S2.3 Sector Review: application/pipelines/pubchem/ + semanticscholar/ + uniprot/

**Reviewer:** py-audit-bot (L3 Worker)
**Date:** 2026-02-26
**Files reviewed:** ~20 Python files

---

## Summary

**Status: PASS (9.25/10)**

All three provider pipelines follow the established patterns. One minor type annotation issue found.

## Findings

### ARCH-001: Import Boundaries -- CLEAN
- All imports from `bioetl.domain.*` or `bioetl.application.*`
- No infrastructure/composition/interfaces imports

### AP-001/DI-001: Hard-coded Constructor Dependencies -- CLEAN
- All transformers accept dependencies via optional constructor injection
- Null Object defaults (NoOpTracing, NoOpMetrics, etc.) are EXC-003

### AP-002: structlog -- CLEAN

### TYPE-001: Public Function Annotations
- **ISSUE (LOW):** `PubChemCompoundTransformer.__init__` (pubchem/transformer.py:53) is missing the `-> None` return type annotation on `__init__`. The closing `)` has no `-> None` unlike all other transformer `__init__` methods.

### NAME-001: Class Suffixes -- CLEAN
- `PubChemCompoundTransformer` -- correct `*Transformer` suffix
- `SemanticScholarPublicationTransformer` -- correct
- UniProt extractors: `TaxonomyExtractor`, `FeatureExtractor`, `CommentExtractor`, `CrossRefExtractor`, `GeneExtractor` -- all correct `*Extractor` suffix
- `ExtractorUtils` -- utility class, acceptable naming
- `IDMappingDataSource` -- located in `core/` not `pipelines/uniprot/`, proper naming

### Notes
- PubChem transformer uses domain validation helpers (`safe_float`, `safe_int`, `validate_molecular_weight`, `validate_non_negative`) -- clean separation
- SemanticScholar handles TLDR as abstract fallback
- UniProt extractors are well-organized with dedicated classes per concern

## Issues Found: 1

| # | Rule | Severity | File | Line | Description |
|---|------|----------|------|------|-------------|
| 1 | TYPE-001 | LOW | `application/pipelines/pubchem/transformer.py` | 53 | Missing `-> None` return type annotation on `__init__` |
