# S2.5 Sector Review: application/composite/ + services/ + observability/

**Reviewer:** py-audit-bot (L3 Worker)
**Date:** 2026-02-26
**Files reviewed:** ~35 Python files

---

## Summary

**Status: PASS (9.0/10)**

The composite pipeline runner, services, and observability layers are well-structured with clean architecture. One minor internal composition pattern noted in MergeService.

## Findings

### ARCH-001: Import Boundaries -- CLEAN
- All imports from `bioetl.domain.*` or `bioetl.application.*`
- `opentelemetry.trace.Span` imported under TYPE_CHECKING only -- EXC-001
- `MedallionLifecycleService.prepare_for_run()` imports `MedallionPolicy` from `bioetl.domain.medallion` inside method -- this is a domain import, perfectly valid per ARCH-001

### AP-001/DI-001: Hard-coded Constructor Dependencies -- ANALYSIS

**`MergeService.__init__` (merger.py:94-104):**
- Creates `EnricherDeduplicator(logger)`, `EnricherAggregator(logger)`, `ColumnRenamer(logger)`, `ColumnOrderer(logger, ...)` internally
- **Verdict: ACCEPTABLE** -- these are stateless, application-layer helper classes within the same `composite/` package. They receive the injected `logger` and are pure transformation utilities. Same pattern as PubMed extractors. Not external dependencies.

**`CompositePipelineRunner.__init__` (runner.py:197):**
- Creates `FSMStateHelper(...)` internally
- **Verdict: ACCEPTABLE** -- FSMStateHelper is a pure state machine helper within the same package, takes only state machine states as parameters. No I/O.

### AP-002: structlog -- CLEAN
- No direct structlog imports in any file

### DI-003: Service Locator -- CLEAN
- No ServiceLocator or Container.resolve patterns found

### DI-005: Factory in Business Logic -- CLEAN
- No Factory instantiation. `PipelineRunnerService` uses injected `runner_factory: RunnerFactoryPort` protocol

### NAME-001: Class Suffixes -- CLEAN
- Composite: `CompositePipelineRunner`, `MergeService`, `EnrichmentCoordinator`, `DependencyCoordinator`, `KeyExtractorService`, `CompositePreflightValidator`, `CompositeCheckpointManager`, `CompositeCheckpointState`, `FSMStateHelper`, `EnricherAggregator`, `EnricherDeduplicator`, `ColumnRenamer`, `ColumnOrderer`, `EnrichmentCrossValidator`
- Services: `HealthService`, `DataQualityService`, `PipelineRunnerService`, `MedallionLifecycleService`, `VacuumService`, `CheckpointService`, `ConfigService`, `ExportService`, `QuarantineService`, `BronzeCleanupService`, `ShutdownService`, `MetricsService`, `LockService`, `DQReportService`, `SilverDQAnalyzer`, `GoldDQAnalyzer`, `BronzeDQAnalyzer`
- Results/Data: `HealthResult`, `HealthCheckSummary`, `RunResult`, `RunOptions`, `ClearResult`, `VacuumResult`, `PrepareResult`, `PipelineInfo`, `SettingsInfo`, `ColumnInfo`, `TablePreview`, `TableInfo`, `ExportOptions`, `ExportResult`, `DQReportResult`, `DQReportContext`, `QuarantineRecord`, `CheckpointInfo`, `LockInfo`, `BronzeCleanupResult`, `MetricsServerStatus`, `StartResult`, `TableVacuumResult`, `VacuumAllResult`
- Observer: `PipelineObserver`, `LifecyclePhase`
- Errors: `PipelineNotFoundError`, `PipelineShutdownError`
- All follow NAME-001 conventions correctly

### TYPE-001: Public Function Annotations -- CLEAN
- All public methods in services have proper return type annotations
- `DataSourceFactoryPort` in health_service.py properly uses `@runtime_checkable` -- TYPE-004 compliant

### Additional Notes
- `PipelineRunnerService._execute_pipeline()` has a method-level import (`from bioetl.application.core.shutdown import PipelineShutdownError`) to avoid circular import -- acceptable pragmatic solution, well-commented
- `HealthService` properly uses `HealthCheckPort` runtime checkable protocol for adapter validation
- `PipelineObserver` properly suppresses `PipelineShutdownError` for clean exit

## Issues Found: 0
