[pytest]
testpaths = tests
pythonpath = src .
python_files = test_*.py
python_functions = test_*
addopts = -ra -q --strict-markers --strict-config
markers =
    unit: Unit tests (fast, no I/O)
    integration: Integration tests (may use I/O)
    slow: Slow tests
    network: Tests requiring outbound network access
