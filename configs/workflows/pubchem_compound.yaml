schema_version: "1.0.0"
workflow:
  name: pubchem_compound
  version: "1.0.0"
  defaults:
    run_options:
      run_type: backfill
      dry_run: false
      log_level: INFO
  steps:
    - kind: pipeline
      step_id: run_pubchem_compound
      pipeline_name: pubchem_compound
