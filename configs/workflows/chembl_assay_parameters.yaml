schema_version: "1.0.0"
workflow:
  name: chembl_assay_parameters
  version: "1.0.0"
  defaults:
    run_options:
      run_type: backfill
      dry_run: false
      log_level: INFO
  steps:
    - kind: pipeline
      step_id: run_chembl_assay_parameters
      pipeline_name: chembl_assay_parameters
