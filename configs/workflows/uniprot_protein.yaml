schema_version: "1.0.0"
workflow:
  name: uniprot_protein
  version: "1.0.0"
  defaults:
    run_options:
      run_type: backfill
      dry_run: false
      log_level: INFO
  steps:
    - kind: pipeline
      step_id: run_uniprot_protein
      pipeline_name: uniprot_protein
