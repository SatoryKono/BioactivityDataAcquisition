schema_version: 1
policy:
  default_gate_mode: block
  owner_diversification_sync:
    source: configs/quality/debt_scorecard.yaml#governance.owner_diversification
    starts_quarter: 2026-Q2
    review_on: "2026-06-30"
  required_fields:
    - value
    - owner
    - reason
    - classification
    - linked_rf
    - expires_on
    - removal_step
registries:
  file_size_limits:
    src/bioetl/composition/runtime_builders/run_manifest_builder.py:
      value: 363
      owner: "@bioetl-architecture"
      reason: "Run-manifest composition builder remains above the composition LOC budget while manifest creation and replay metric orchestration are split into smaller runtime-builder helpers."
      classification: "technical_debt"
      linked_rf: "RF-000"
      expires_on: "2026-09-30"
      removal_step: "Move manifest request construction and replay metric emission into focused composition runtime-builder modules, then remove this file-size exemption."
    src/bioetl/infrastructure/storage/silver/operations/metadata_operations.py:
      value: 822
      owner: "@bioetl-platform"
      reason: "Silver metadata operations still host canonical write, audit, DQ, and compatibility method binding paths during the storage operations split."
      classification: "technical_debt"
      linked_rf: "RF-000"
      expires_on: "2026-09-30"
      removal_step: "Extract metadata write/audit/finalization method bindings into focused infrastructure helpers, then remove this file-size exemption."
    src/bioetl/interfaces/cli/commands/_run_manifest_output_support.py:
      value: 421
      owner: "@bioetl-data-model"
      reason: "Run-manifest CLI output support is one line over the interfaces LOC budget while compact, diagnostics, and identity-graph renderers share private formatting helpers."
      classification: "technical_debt"
      linked_rf: "RF-000"
      expires_on: "2026-09-30"
      removal_step: "Move diagnostics or identity-graph rendering helpers into a sibling CLI output module, then remove this file-size exemption."
  function_complexity:
    {}
  function_length:
    {}
  class_size: {}
  class_method_count: {}
  god_object: {}
  domain_complexity:
    src/bioetl/domain/config/memory.py::MemoryConfig:
      value: 6
      owner: "@bioetl-platform"
      reason: "MemoryConfig uses Pydantic BaseModel for validation, leading to an apparent cyclomatic complexity of 6 in static analysis, though its core logic is simple and delegated to Pydantic's internal mechanisms."
      classification: "intentional_exception"
      linked_rf: "RF-000"
      expires_on: "2026-09-30"
      removal_step: "Re-evaluate when Pydantic BaseModel cyclomatic complexity is correctly measured or when a change in validation framework occurs."
