# S7 / #9603 — package cohesion budgets. Per-file LOC caps are unchanged.
# package_loc and module_count_per_package are shrink-only.
# domain/aggregates hold-flat at 8/8 until mixin consolidation fits ≤305 LOC (#10552).
schema_version: 1
owner: "@bioetl-architecture"
linked_issue: "9603"
packages:
  - path: src/bioetl/domain/aggregates
    max_modules: 8
    max_package_loc: 2100
    target_modules: 8
    hold_flat_issue: "10552"
  - path: src/bioetl/application/services/control_plane
    max_modules: 150
    max_package_loc: 20000
    max_helper_ratio: 0.399
    target_helper_ratio: 0.40
  - path: src/bioetl/composition
    # Shrink-only after shim collapse (#10551 / AUD-001 → #10595 / AUD-002, #11080):
    # live 279, composition_util = 279/295 ≈ 0.9458 ≤ 0.95. Cap held at 295.
    max_modules: 295
    max_package_loc: 40000
    linked_issue: "10595"
  - path: src/bioetl/infrastructure/quality
    max_modules: 40
    max_package_loc: 8000
