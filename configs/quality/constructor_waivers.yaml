# RF-017 Constructor Waivers
# ARCH-RES-02 / #6750: 3 -> 2. Shrink-only.
# CheckpointRuntimeService removed via CheckpointRuntimeIdentity bag.

PipelineObserver:
  max_args: 13
  owner: "@bioetl-observability"
  expiry: "2026-12-31"
  reason: "Observer currently aggregates multiple sinks and lifecycle reporters."

QuarantineEntry:
  max_args: 9
  owner: "@bioetl-domain"
  expiry: "2026-12-31"
  classification: intentional_exception
  linked_issue: "#6685"
  linked_adr: "ADR-051-quarantine-entry-aggregate-surface.md"
  reason: >-
    Domain aggregate intentionally exposes an explicit multi-field identity surface.
    See ADR-051; max_args frozen (no growth).
