# RF-017 Constructor Waivers
# ARCH-CONT-04 / #6760: 2 -> 1. Shrink-only.
# PipelineObserver removed via PipelineObserverIdentity bag.
# CheckpointRuntimeService removed via CheckpointRuntimeIdentity bag (ARCH-RES-02).

QuarantineEntry:
  max_args: 9
  owner: "@bioetl-domain"
  expiry: "2026-12-31"
  classification: intentional_exception
  linked_issue: "#6685"
  linked_adr: "ADR-051-quarantine-entry-aggregate-surface.md"
  reason: >-
    Domain aggregate intentionally exposes an explicit multi-field identity surface.
    See ADR-051; max_args frozen (no growth).
