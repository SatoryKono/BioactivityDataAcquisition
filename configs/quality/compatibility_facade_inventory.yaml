version: 1
policy_scope: compatibility_facades
tracked_docstring_prefixes:
- 'Backward-compatible '
- 'Compatibility '
- Compatibility-
- Deprecated compatibility
- Composition-level compatibility
- Pipeline factory compatibility-only facade
- Storage compatibility-only facade
transition_debt:
- path: src/bioetl/interfaces/cli/commands/_compat.py
  compatibility_role: Internal CLI compatibility shim helper that powers retained
    top-level command alias modules.
  canonical_target: bioetl.interfaces.cli.commands
  status: compat-shim
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: >-
    `src`: only retained top-level CLI public seam modules under
    `src/bioetl/interfaces/cli/commands/` may import `alias_module`; `tests`:
    dedicated CLI boundary coverage may exercise the helper indirectly through
    those public seam modules
  migration_path: >-
    Keep top-level CLI public seams as the sanctioned import targets; do not
    import `_compat.py` outside the retained command-seam modules.
  exit_criteria: >-
    `_compat.py` remains confined to the reviewed top-level command seams and
    can be removed once those seams no longer need helper-based alias forwarding.
- path: src/bioetl/composition/factories/storage/adapter.py
  compatibility_role: Deprecated compatibility shim for the storage bundle module
    after the canonical implementation moved to
    `bioetl.composition.factories.storage.bundle`.
  canonical_target: bioetl.composition.factories.storage.bundle
  status: deprecated-warn
  owner: bioetl.composition.factories.storage
  introduced_in: 2026-04 storage factory naming cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: true
  allowed_call_sites: '`src`: no direct imports; use `bioetl.composition.factories.storage.bundle`;
    `tests`: dedicated storage bootstrap governance may inspect the shim to verify
    the deprecation warning and removal horizon'
  migration_path: Import `StorageBundle` from `bioetl.composition.factories.storage.bundle`
    in all first-party code. Keep this module only as a warning compatibility seam
    until the 2026-09-30 compatibility review.
  exit_criteria: First-party `src` imports remain at zero, governance tests confirm
    the shim stays warning-only, and the module is removed after the compatibility
    review or explicit breaking-change window.
retained_entrypoints:
- path: src/bioetl/interfaces/cli/commands/run.py
  compatibility_role: Permanent public run command seam over the split internal owner
    module `bioetl.interfaces.cli.commands.domains.run.command`.
  canonical_target: bioetl.interfaces.cli.commands.run
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run` as the public
    seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/run/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.run`,
    while direct `bioetl.interfaces.cli.commands.domains.run.command` imports stay
    confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.run` for public CLI wiring and
    patch targets; do not import `bioetl.interfaces.cli.commands.domains.run.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/run_all.py
  compatibility_role: Permanent public run-all command seam over the split internal
    owner module `bioetl.interfaces.cli.commands.domains.run_all.command`.
  canonical_target: bioetl.interfaces.cli.commands.run_all
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run_all` as the
    public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/run_all/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.run_all`,
    while direct `bioetl.interfaces.cli.commands.domains.run_all.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.run_all` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.run_all.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/run_composite.py
  compatibility_role: Permanent public run-composite command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.composite.command`.
  canonical_target: bioetl.interfaces.cli.commands.run_composite
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run_composite` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/composite/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.run_composite`,
    while direct `bioetl.interfaces.cli.commands.domains.composite.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.run_composite` for public CLI
    wiring and patch targets; do not import `bioetl.interfaces.cli.commands.domains.composite.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/health.py
  compatibility_role: Permanent public health command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.health.command`.
  canonical_target: bioetl.interfaces.cli.commands.health
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.health` as the public
    seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/health/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.health`,
    while direct `bioetl.interfaces.cli.commands.domains.health.command` imports stay
    confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.health` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.health.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/diagnostics.py
  compatibility_role: Permanent public diagnostics command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.diagnostics.command`.
  canonical_target: bioetl.interfaces.cli.commands.diagnostics
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.diagnostics` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/diagnostics/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.diagnostics`,
    while direct `bioetl.interfaces.cli.commands.domains.diagnostics.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.diagnostics` for public CLI
    wiring and patch targets; do not import `bioetl.interfaces.cli.commands.domains.diagnostics.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/quarantine.py
  compatibility_role: Permanent public quarantine command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.quarantine.command`.
  canonical_target: bioetl.interfaces.cli.commands.quarantine
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.quarantine` as the
    public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/quarantine/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.quarantine`,
    while direct `bioetl.interfaces.cli.commands.domains.quarantine.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.quarantine` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.quarantine.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/maintenance.py
  compatibility_role: Permanent public maintenance command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.maintenance.command`.
  canonical_target: bioetl.interfaces.cli.commands.maintenance
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.maintenance` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/__init__.py`;
    `tests`: import coverage may target `bioetl.interfaces.cli.commands.maintenance`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.maintenance` for public CLI
    wiring; do not import `bioetl.interfaces.cli.commands.domains.maintenance.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/plan.py
  compatibility_role: Permanent public plan command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.maintenance.plan`.
  canonical_target: bioetl.interfaces.cli.commands.plan
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.plan` as the public
    seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.plan`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.plan` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.plan` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.maintenance.plan`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/archive.py
  compatibility_role: Permanent public archive command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.maintenance.archive`.
  canonical_target: bioetl.interfaces.cli.commands.archive
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.archive` only as
    the public seam for import/patch stability; direct internal-owner imports stay
    confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/command.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.archive`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.archive` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.archive` for public patch targets;
    do not import `bioetl.interfaces.cli.commands.domains.maintenance.archive` directly
    outside the owning maintenance package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning maintenance
    package plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/cleanup.py
  compatibility_role: Permanent public cleanup command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.maintenance.cleanup`.
  canonical_target: bioetl.interfaces.cli.commands.cleanup
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.cleanup` only as
    the public seam for import/patch stability; direct internal-owner imports stay
    confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/command.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.cleanup`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.cleanup` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.cleanup` for public patch targets;
    do not import `bioetl.interfaces.cli.commands.domains.maintenance.cleanup` directly
    outside the owning maintenance package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning maintenance
    package plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/vacuum.py
  compatibility_role: Permanent public vacuum command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.maintenance.vacuum`.
  canonical_target: bioetl.interfaces.cli.commands.vacuum
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.vacuum` only as
    the public seam for import/patch stability; direct internal-owner imports stay
    confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/command.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.vacuum`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.vacuum` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.vacuum` for public patch targets;
    do not import `bioetl.interfaces.cli.commands.domains.maintenance.vacuum` directly
    outside the owning maintenance package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning maintenance
    package plus dedicated boundary tests.
- path: src/bioetl/composition/entrypoints.py
  compatibility_role: Permanent public composition entrypoint that intentionally
    shields internal `_pipeline_execution`, `_resource_management`, and `_services`
    module paths.
  canonical_target: bioetl.composition.entrypoints
  status: public-entrypoint
  owner: bioetl.composition
  introduced_in: 2026-03 entrypoint freeze
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only explicit execution-focused public symbols from
    `__all__` may be imported from `bioetl.composition.entrypoints`; deprecated
    service/resource symbols resolved through `_LEGACY_SYMBOL_TARGETS` are external-compatibility
    only and are blocked in first-party source. `tests`: public entrypoint imports
    may appear in interface/composition boundary coverage, but direct internal-module
    patch targets stay confined to `tests/unit/composition/test_entrypoints.py`,
    `tests/unit/composition/test_resource_management.py`, and `tests/unit/composition/test_services_entrypoints.py`'
  migration_path: Use `bioetl.composition.entrypoints` as the sanctioned public
    seam; do not import `bioetl.composition._pipeline_execution`, `bioetl.composition._resource_management`,
    or `bioetl.composition._services` directly outside dedicated entrypoint-boundary
    coverage.
  exit_criteria: The public entrypoint remains sanctioned, deprecated service/resource
    symbol imports in first-party source stay at zero, internal implementation-module
    imports outside `composition/` stay at zero, and internal-module patch coverage
    remains confined to the dedicated entrypoint-boundary tests.
- path: src/bioetl/composition/__init__.py
  compatibility_role: Retained package-root composition facade that preserves
    import stability for registry discovery and sanctioned public submodule access
    while first-party `src/` callers migrate to owner-focused APIs.
  canonical_target: bioetl.composition
  status: retained-entrypoint
  owner: bioetl.composition
  introduced_in: 2026-04 TD-005 TD-006
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: true
  allowed_call_sites: '`src`: no direct imports; use `bioetl.composition.registry_api`,
    `execution_api`, `control_plane_api`, `health_api`, `maintenance_api`, `composite_api`,
    or `observability_api`; `tests`: dedicated compatibility coverage may import the
    package root to verify the frozen lazy-export budget'
  migration_path: Keep `bioetl.composition` only as a retained public compatibility
    seam for external import stability and discovery. New first-party `src/` imports
    must point at owner-focused composition APIs instead of the package root.
  exit_criteria: First-party `src/` imports of `bioetl.composition` stay at zero,
    the package-root export budget remains frozen, and any future surface change is
    reviewed through this inventory first.
- path: src/bioetl/composition/bootstrap/__init__.py
  compatibility_role: Retained runtime-focused bootstrap package facade that preserves
    historical import stability while owner-focused execution, composite, and observability
    APIs serve as the canonical first-party composition seams.
  canonical_target: bioetl.composition.bootstrap
  status: retained-entrypoint
  owner: bioetl.composition.bootstrap
  introduced_in: 2026-04 TD-001
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: true
  allowed_call_sites: '`src`: no direct imports; use `bioetl.composition.execution_api`,
    `composite_api`, or `observability_api`; `tests`: dedicated compatibility and
    runtime-boundary coverage may import the bootstrap package root'
  migration_path: Keep `bioetl.composition.bootstrap` as a retained compatibility
    seam for runtime-facing import stability, but route new first-party `src/` imports
    through owner-focused composition APIs instead of the bootstrap package root.
  exit_criteria: First-party `src/` imports of `bioetl.composition.bootstrap` stay
    at zero, the runtime-focused `__all__` budget remains frozen, and any new root-level
    export requires an explicit inventory review.
- path: src/bioetl/application/services/__init__.py
  compatibility_role: Retained application-services package-root facade that preserves
    legacy flat service imports while first-party runtime code uses owner submodules.
  canonical_target: bioetl.application.services.execution, bioetl.application.services.control_plane,
    bioetl.application.services.lineage, or the concrete service/model owner module
  status: retained-entrypoint
  owner: bioetl.application.services
  introduced_in: 2026-04 issue-3474 caller-zero gate
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: true
  allowed_call_sites: '`src`: no direct imports from `bioetl.application.services`;
    use semantic subpackages or concrete service/model owner modules; `tests`: temporary
    compatibility coverage may import the package root while migrating patch seams,
    but new production callers are blocked by architecture gates'
  migration_path: Keep `bioetl.application.services` as an external compatibility
    seam only. New first-party runtime code must import from canonical owner modules
    such as `bioetl.application.services.execution.pipeline_runner_models`,
    `bioetl.application.services.execution.pipeline_runner_service`, `export_models`,
    `export_service`, `vacuum_service`, or focused control-plane/lineage submodules.
  exit_criteria: First-party `src/` imports of `bioetl.application.services` stay
    at zero, remaining test-only package-root imports are migrated or intentionally
    retained as compatibility coverage, and any new root export requires inventory
    review before merge.
- path: src/bioetl/domain/composite/config.py
  compatibility_role: Permanent public entrypoint for composite config models that
    shields split config internals.
  canonical_target: bioetl.domain.composite.config
  status: public-entrypoint
  owner: bioetl.domain.composite
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: canonical entrypoint usage allowed; internal split modules
    stay inside `src/bioetl/domain/composite/`; `tests`: direct split-internal coverage
    stays confined to `tests/unit/domain/composite/test_composite_config_facade.py`
    and `tests/unit/domain/composite/test_composite_config_edge_cases.py`, while ordinary
    tests keep importing the root config entrypoint'
  migration_path: Use `bioetl.domain.composite.config` as the sanctioned public
    seam; do not import split `config_*` internals outside the owning package or
    the dedicated composite-config coverage tests.
  exit_criteria: The root config entrypoint remains the sanctioned public path and
    direct imports of split config internals stay confined to the owning package
    plus the dedicated composite-config coverage tests.
- path: src/bioetl/domain/value_objects/activity_values.py
  compatibility_role: Permanent public entrypoint for activity-related value objects
    that shields split concentration/type/pChEMBL modules.
  canonical_target: bioetl.domain.value_objects.activity_values
  status: public-entrypoint
  owner: bioetl.domain.value_objects
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: canonical entrypoint usage allowed; internal split modules
    stay inside `src/bioetl/domain/value_objects/`; `tests`: direct split-internal
    coverage stays confined to `tests/unit/domain/value_objects/test_value_object_facade_reexports.py`,
    while ordinary tests keep importing the public entrypoint'
  migration_path: Use `bioetl.domain.value_objects.activity_values` as the sanctioned
    public seam; do not import split value-object internals outside the owning package
    or the dedicated facade-coverage test.
  exit_criteria: The facade remains the sanctioned public path and direct imports
    of split activity-value internals stay confined to the owning package plus the
    dedicated facade-coverage test.
- path: src/bioetl/domain/value_objects/publication_field_groups.py
  compatibility_role: Permanent public entrypoint for publication field-group definitions
    that shields private split config/type modules.
  canonical_target: bioetl.domain.value_objects.publication_field_groups
  status: public-entrypoint
  owner: bioetl.domain.value_objects
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: canonical entrypoint usage allowed; private split modules
    stay inside `src/bioetl/domain/value_objects/`; `tests`: direct split-internal
    coverage stays confined to `tests/unit/domain/value_objects/test_value_object_facade_reexports.py`,
    while ordinary tests keep importing the public entrypoint'
  migration_path: Use `bioetl.domain.value_objects.publication_field_groups` as
    the sanctioned public seam; do not import split value-object internals outside
    the owning package or the dedicated facade-coverage test.
  exit_criteria: The facade remains the sanctioned public path and direct imports
    of private publication-field-group internals stay confined to the owning package
    plus the dedicated facade-coverage test.
- path: src/bioetl/application/composite/merger.py
  compatibility_role: Permanent public composite merge module that requires `MergeCollaboratorGroup`
    bundle; legacy per-collaborator keyword wiring removed in RF-009.2.
  canonical_target: bioetl.application.composite.merger
  status: public-entrypoint
  owner: bioetl.application.composite
  introduced_in: 2026-03 merge collaborator migration
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: all wiring passes `collaborators=MergeCollaboratorGroup(...)`;
    `tests`: `tests/unit/application/composite/test_merger.py`, `tests/unit/application/composite/merge_test_support.py`,
    `tests/unit/composition/bootstrap/runtime/test_composite_support_services_factory.py`'
  migration_path: Use `bioetl.application.composite.merger` as the sanctioned public
    seam with `collaborators=MergeCollaboratorGroup(...)`.
  exit_criteria: The merge module remains the sanctioned public seam and all composition
    paths use collaborator bundles with legacy keyword wiring fully removed.
- path: src/bioetl/infrastructure/adapters/pubmed/client.py
  compatibility_role: Permanent public client entrypoint that shields older `pubmed_client`
    imports while exporting the current adapter surface and public `create_pubmed_adapter`
    factory alias.
  canonical_target: bioetl.infrastructure.adapters.pubmed.client
  status: public-entrypoint
  owner: bioetl.infrastructure.adapters.pubmed
  introduced_in: 2026-03 pubmed entrypoint hardening
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: direct `bioetl.infrastructure.adapters.pubmed.client`
    imports stay confined to `src/bioetl/infrastructure/adapters/pubmed/__init__.py`;
    first-party code imports the provider package root; `tests`: `tests/unit/infrastructure/adapters/test_provider_entrypoints.py`,
    `tests/architecture/test_adapter_contracts.py`, `tests/architecture/test_retained_adapter_entrypoint_policy.py`'
  migration_path: Use the provider package root `bioetl.infrastructure.adapters.pubmed`
    in new first-party code; keep `bioetl.infrastructure.adapters.pubmed.client`
    as the sanctioned public adapter seam and do not import `bioetl.infrastructure.adapters.pubmed.pubmed_client`
    directly.
  exit_criteria: The public `client.py` seam remains sanctioned, direct `client.py`
    imports stay confined to the provider package root plus dedicated compatibility
    coverage, and private `_create_pubmed_adapter` remains unexported from the public
    entrypoint.
- path: src/bioetl/infrastructure/adapters/semanticscholar/client.py
  compatibility_role: Permanent public client entrypoint that shields older `adapter`
    imports.
  canonical_target: bioetl.infrastructure.adapters.semanticscholar.client
  status: public-entrypoint
  owner: bioetl.infrastructure.adapters.semanticscholar
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: direct `bioetl.infrastructure.adapters.semanticscholar.client`
    imports stay confined to `src/bioetl/infrastructure/adapters/semanticscholar/__init__.py`;
    first-party code imports the provider package root; `tests`: `tests/unit/infrastructure/adapters/test_provider_entrypoints.py`,
    `tests/architecture/test_adapter_contracts.py`, `tests/architecture/test_retained_adapter_entrypoint_policy.py`'
  migration_path: Use the provider package root `bioetl.infrastructure.adapters.semanticscholar`
    in new first-party code; keep `bioetl.infrastructure.adapters.semanticscholar.client`
    as the sanctioned public adapter seam and do not import `bioetl.infrastructure.adapters.semanticscholar.adapter`
    directly.
  exit_criteria: The public `client.py` seam remains sanctioned, direct `client.py`
    imports stay confined to the provider package root plus dedicated compatibility
    coverage, and legacy-path references remain reduced to that public seam.
measured_only_ratchet:
  max_total_modules: 0
  scoped_limits:
  - path_prefix: src/bioetl/application/services/
    max_modules: 0
  - path_prefix: src/bioetl/interfaces/cli/commands/
    max_modules: 0
measured_only_review_workflow:
  review_cadence: quarterly
  required_checks:
  - verify no first-party src imports remain
  - confirm measured-only docstring tracking stays aligned with the YAML allowlist
  - run targeted owner tests before deciding retain, promote, or remove
  - refresh the generated compatibility snapshot after lifecycle changes
  allowed_outcomes:
  - retain
  - promote
  - remove
  promotion_requires_curated_row: true
measured_only_modules: []
