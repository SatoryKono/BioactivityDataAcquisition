version: 1
policy_scope: compatibility_facades
tracked_docstring_prefixes:
- 'Backward-compatible '
- 'Compatibility '
- Compatibility-
- Deprecated compatibility
- Composition-level compatibility
- Pipeline factory compatibility-only facade
- Storage compatibility-only facade
transition_debt:
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_base.py
  compatibility_role: Compatibility shim for moved base wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.base
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.base` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_base` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_base_helpers.py
  compatibility_role: Compatibility shim for moved base-helper wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.helpers
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.helpers` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_base_helpers` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_checkpoint_projection.py
  compatibility_role: Compatibility shim for moved checkpoint-projection wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.checkpoint_projection
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.checkpoint_projection` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_checkpoint_projection` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/_run_manifest_diagnostics_checkpoint_projection.py
  compatibility_role: Compatibility shim for moved checkpoint-projection wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.checkpoint_projection
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.checkpoint_projection` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `_run_manifest_diagnostics_checkpoint_projection` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_finalization.py
  compatibility_role: Compatibility shim for moved finalization wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.finalization
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.finalization` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_finalization` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_replay.py
  compatibility_role: Compatibility shim for moved replay wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.replay
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.replay` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_replay` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_replay_projection.py
  compatibility_role: Compatibility shim for moved replay-projection wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.replay_projection
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.replay_projection` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_replay_projection` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_snapshot_support.py
  compatibility_role: Compatibility shim for moved snapshot-support wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.snapshot_support
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.snapshot_support` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_snapshot_support` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_source_refs.py
  compatibility_role: Compatibility shim for moved source-reference wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.source_refs
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.source_refs` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_source_refs` remain, except those added during this compatibility transition issue.
- path: src/bioetl/application/services/control_plane/run_manifest_diagnostics_artifact_support.py
  compatibility_role: Compatibility shim for moved artifact-support wrappers.
  canonical_target: bioetl.application.services.control_plane.manifest.diagnostics.artifact_support
  status: compat-shim
  owner: bioetl.application.services.control_plane
  introduced_in: 2026-05 compatibility surface cleanup
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only `bioetl.application.services.control_plane` and manifest callers may import this shim until deprecation review.'
  migration_path: Migrate all direct imports to `bioetl.application.services.control_plane.manifest.diagnostics.artifact_support` and keep this shim only for transition compatibility.
  exit_criteria: No first-party `src` imports of `run_manifest_diagnostics_artifact_support` remain, except those added during this compatibility transition issue.
retained_entrypoints:
- path: src/bioetl/interfaces/cli/commands/run.py
  compatibility_role: Permanent public run command seam over the retained domain
    helper modules under `bioetl.interfaces.cli.commands.domains.run`.
  canonical_target: bioetl.interfaces.cli.commands.run
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run` as the public
    seam; `src/bioetl/interfaces/cli/commands/domains/run/__init__.py` may re-export
    that public module for package compatibility only. `tests`: patch/import
    coverage may target `bioetl.interfaces.cli.commands.run`; retired wrapper module
    `bioetl.interfaces.cli.commands.domains.run.command` must stay absent'
  migration_path: Use `bioetl.interfaces.cli.commands.run` for public CLI wiring and
    patch targets; do not reintroduce `bioetl.interfaces.cli.commands.domains.run.command`
    as an internal owner wrapper.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, the domain package resolves directly to that public module, and the retired
    `domains.run.command` wrapper stays absent.
- path: src/bioetl/interfaces/cli/commands/run_all.py
  compatibility_role: Permanent public run-all command seam over the split internal
    owner module `bioetl.interfaces.cli.commands.domains.run_all.command`.
  canonical_target: bioetl.interfaces.cli.commands.run_all
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run_all` as the
    public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/run_all/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.run_all`,
    while direct `bioetl.interfaces.cli.commands.domains.run_all.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.run_all` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.run_all.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/run_composite.py
  compatibility_role: Permanent public run-composite command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.composite.command`.
  canonical_target: bioetl.interfaces.cli.commands.run_composite
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.run_composite` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/composite/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.run_composite`,
    while direct `bioetl.interfaces.cli.commands.domains.composite.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.run_composite` for public CLI
    wiring and patch targets; do not import `bioetl.interfaces.cli.commands.domains.composite.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/health.py
  compatibility_role: Permanent public health command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.health.command`.
  canonical_target: bioetl.interfaces.cli.commands.health
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.health` as the public
    seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/health/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.health`,
    while direct `bioetl.interfaces.cli.commands.domains.health.command` imports stay
    confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.health` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.health.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/diagnostics.py
  compatibility_role: Permanent public diagnostics command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.diagnostics.command`.
  canonical_target: bioetl.interfaces.cli.commands.diagnostics
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.diagnostics` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/diagnostics/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.diagnostics`,
    while direct `bioetl.interfaces.cli.commands.domains.diagnostics.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.diagnostics` for public CLI
    wiring and patch targets; do not import `bioetl.interfaces.cli.commands.domains.diagnostics.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/quarantine.py
  compatibility_role: Permanent public quarantine command seam over the internal owner
    module `bioetl.interfaces.cli.commands.domains.quarantine.command`.
  canonical_target: bioetl.interfaces.cli.commands.quarantine
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.quarantine` as the
    public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/quarantine/__init__.py`;
    `tests`: patch/import coverage may target `bioetl.interfaces.cli.commands.quarantine`,
    while direct `bioetl.interfaces.cli.commands.domains.quarantine.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.quarantine` for public CLI wiring
    and patch targets; do not import `bioetl.interfaces.cli.commands.domains.quarantine.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/interfaces/cli/commands/maintenance.py
  compatibility_role: Permanent public maintenance command seam over the internal
    owner module `bioetl.interfaces.cli.commands.domains.maintenance.command`.
  canonical_target: bioetl.interfaces.cli.commands.maintenance
  status: public-entrypoint
  owner: bioetl.interfaces.cli.commands
  introduced_in: 2026-03 RF-024
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use `bioetl.interfaces.cli.commands.maintenance` as
    the public seam; direct internal-owner imports stay confined to `src/bioetl/interfaces/cli/commands/domains/maintenance/__init__.py`;
    `tests`: import coverage may target `bioetl.interfaces.cli.commands.maintenance`,
    while direct `bioetl.interfaces.cli.commands.domains.maintenance.command` imports
    stay confined to dedicated boundary coverage'
  migration_path: Use `bioetl.interfaces.cli.commands.maintenance` for public CLI
    wiring; do not import `bioetl.interfaces.cli.commands.domains.maintenance.command`
    directly outside the owning package or dedicated boundary tests.
  exit_criteria: The top-level module remains the sanctioned public CLI patch/import
    seam, and direct internal-owner imports remain confined to the owning package
    plus dedicated boundary tests.
- path: src/bioetl/composition/entrypoints.py
  compatibility_role: Permanent public composition entrypoint that intentionally
    shields internal `_pipeline_execution`, `_resource_management`, and `_services`
    module paths.
  canonical_target: bioetl.composition.entrypoints
  status: public-entrypoint
  owner: bioetl.composition
  introduced_in: 2026-03 entrypoint freeze
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: only explicit execution-focused public symbols from
    `__all__` may be imported from `bioetl.composition.entrypoints`; removed
    service/resource compatibility symbols must stay absent from first-party source.
    `tests`: public entrypoint imports
    may appear in interface/composition boundary coverage, but direct internal-module
    patch targets stay confined to `tests/unit/composition/test_entrypoints.py`,
    `tests/unit/composition/test_resource_management.py`, and `tests/unit/composition/test_services_entrypoints.py`'
  migration_path: Use `bioetl.composition.entrypoints` as the sanctioned public
    seam; do not import `bioetl.composition._pipeline_execution`, `bioetl.composition._resource_management`,
    or `bioetl.composition._services` directly outside dedicated entrypoint-boundary
    coverage.
  exit_criteria: The public entrypoint remains sanctioned, removed service/resource
    symbol imports in first-party source stay at zero, internal implementation-module
    imports outside `composition/` stay at zero, and internal-module patch coverage
    remains confined to the dedicated entrypoint-boundary tests.
  public_export_contract:
    max_public_exports: 15
    lazy_export_table: _PUBLIC_SYMBOL_TARGETS
- path: src/bioetl/composition/health_api.py
  compatibility_role: Permanent public health composition facade that intentionally
    shields split services/bootstrap/resource seams while keeping a reviewed export
    budget.
  canonical_target: bioetl.composition.health_api
  status: public-entrypoint
  owner: bioetl.composition
  introduced_in: 2026-05 public export review
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: import only the explicit health/quarantine symbols
    exposed by `bioetl.composition.health_api`; do not bypass this facade to patch
    `_services`, bootstrap health wiring, or `_resource_management` outside the
    reviewed owner paths. `tests`: public facade imports may appear in CLI/composition
    coverage, while direct internal patch targets stay confined to dedicated boundary
    tests.'
  migration_path: Use `bioetl.composition.health_api` as the sanctioned health-facing
    composition seam; do not add new first-party imports from `bioetl.composition._services`,
    `bioetl.composition._resource_management`, or `bioetl.composition.bootstrap.cli.health`
    when the public facade export already exists.
  exit_criteria: The public health facade remains the sanctioned import seam, its
    reviewed export budget stays bounded, and internal owner modules remain hidden
    behind the facade for ordinary first-party callers.
  public_export_contract:
    max_public_exports: 8
    lazy_export_table: _PUBLIC_EXPORTS
- path: src/bioetl/composition/maintenance_api.py
  compatibility_role: Permanent public maintenance composition facade that intentionally
    shields maintenance/resource wiring behind a reviewed export budget.
  canonical_target: bioetl.composition.maintenance_api
  status: public-entrypoint
  owner: bioetl.composition
  introduced_in: 2026-05 public export review
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: import maintenance lifecycle/archive/cleanup/vacuum
    helpers only through `bioetl.composition.maintenance_api`; direct imports from
    `_resource_management` and `_services` stay confined to owner implementation
    paths. `tests`: public facade imports may appear in CLI/composition coverage,
    while direct internal patch targets stay confined to dedicated boundary tests.'
  migration_path: Use `bioetl.composition.maintenance_api` as the sanctioned maintenance-facing
    composition seam; do not add new first-party imports from `bioetl.composition._resource_management`
    or `bioetl.composition._services` when the public facade export already exists.
  exit_criteria: The public maintenance facade remains the sanctioned import seam,
    its reviewed export budget stays bounded, and internal owner modules remain hidden
    behind the facade for ordinary first-party callers.
  public_export_contract:
    max_public_exports: 8
    lazy_export_table: _PUBLIC_EXPORTS
- path: src/bioetl/infrastructure/config/__init__.py
  compatibility_role: Permanent public infrastructure config package-root convenience
    facade that intentionally shields split config loader APIs behind a reviewed
    export budget.
  canonical_target: bioetl.infrastructure.config
  status: public-entrypoint
  owner: bioetl.infrastructure.config
  introduced_in: legacy-pre-2026-05
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: use the package-root config facade only for reviewed
    convenience symbols and keep new imports bounded by the dedicated root-facade
    inventory. `tests`: public package-root imports may appear in runtime/config
    contract coverage, while deeper loader modules remain the owner path for focused
    implementation tests.'
  migration_path: Use `bioetl.infrastructure.config` only for the sanctioned convenience
    exports and prefer owner loader modules when no reviewed facade symbol is needed.
  exit_criteria: The package-root convenience facade remains the sanctioned public
    import seam, its reviewed export budget stays bounded, and symbol-level import
    growth remains governed by the dedicated root-facade inventory.
  public_export_contract:
    max_public_exports: 18
    parse_dunder_getattr_string_branches: true
- path: src/bioetl/domain/composite/config.py
  compatibility_role: Permanent public entrypoint for composite config models that
    shields split config internals.
  canonical_target: bioetl.domain.composite.config
  status: public-entrypoint
  owner: bioetl.domain.composite
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: canonical entrypoint usage allowed; internal split modules
    stay inside `src/bioetl/domain/composite/`; `tests`: direct split-internal coverage
    stays confined to `tests/unit/domain/composite/test_composite_config_facade.py`
    and `tests/unit/domain/composite/test_composite_config_edge_cases.py`, while ordinary
    tests keep importing the root config entrypoint'
  migration_path: Use `bioetl.domain.composite.config` as the sanctioned public
    seam; do not import split `config_*` internals outside the owning package or
    the dedicated composite-config coverage tests.
  exit_criteria: The root config entrypoint remains the sanctioned public path and
    direct imports of split config internals stay confined to the owning package
    plus the dedicated composite-config coverage tests.
- path: src/bioetl/domain/value_objects/activity_values.py
  compatibility_role: Permanent public entrypoint for activity-related value objects
    that shields split concentration/type/pChEMBL modules.
  canonical_target: bioetl.domain.value_objects.activity_values
  status: public-entrypoint
  owner: bioetl.domain.value_objects
  introduced_in: legacy-pre-2026-03
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: canonical entrypoint usage allowed; internal split modules
    stay inside `src/bioetl/domain/value_objects/`; `tests`: direct split-internal
    coverage stays confined to `tests/unit/domain/value_objects/test_value_object_facade_reexports.py`,
    while ordinary tests keep importing the public entrypoint'
  migration_path: Use `bioetl.domain.value_objects.activity_values` as the sanctioned
    public seam; do not import split value-object internals outside the owning package
    or the dedicated facade-coverage test.
  exit_criteria: The facade remains the sanctioned public path and direct imports
    of split activity-value internals stay confined to the owning package plus the
    dedicated facade-coverage test.
- path: src/bioetl/application/composite/merger.py
  compatibility_role: Permanent public composite merge module that requires `MergeCollaboratorGroup`
    bundle; legacy per-collaborator keyword wiring removed in RF-009.2.
  canonical_target: bioetl.application.composite.merger
  status: public-entrypoint
  owner: bioetl.application.composite
  introduced_in: 2026-03 merge collaborator migration
  review_date: '2026-09-30'
  external_breaking_change_required: true
  internal_callers_zero: false
  allowed_call_sites: '`src`: all wiring passes `collaborators=MergeCollaboratorGroup(...)`;
    `tests`: `tests/unit/application/composite/test_merger.py`, `tests/unit/application/composite/merge_test_support.py`,
    `tests/unit/composition/bootstrap/runtime/test_composite_support_services_factory.py`'
  migration_path: Use `bioetl.application.composite.merger` as the sanctioned public
    seam with `collaborators=MergeCollaboratorGroup(...)`.
  exit_criteria: The merge module remains the sanctioned public seam and all composition
    paths use collaborator bundles with only the canonical `order_service` ordering
    collaborator.
measured_only_ratchet:
  max_total_modules: 0
  scoped_limits:
  - path_prefix: src/bioetl/application/services/
    max_modules: 0
  - path_prefix: src/bioetl/interfaces/cli/commands/
    max_modules: 0
measured_only_review_workflow:
  review_cadence: quarterly
  required_checks:
  - verify no first-party src imports remain
  - confirm measured-only docstring tracking stays aligned with the YAML allowlist
  - run targeted owner tests before deciding retain, promote, or remove
  - refresh the generated compatibility snapshot after lifecycle changes
  allowed_outcomes:
  - retain
  - promote
  - remove
  promotion_requires_curated_row: true
retained_public_entrypoint_burden_policy:
  linked_issue: "#4575"
  blocking_metric: retained_public_entrypoint_burden
  first_party_growth_policy: fail_fast_on_growth
  tracked_facades:
  - bioetl.composition.entrypoints
  - bioetl.composition.health_api
  - bioetl.infrastructure.config
  - bioetl.application.services.control_plane
  rationale: Retained public entrypoints are stable external API, but first-party
    imports through those facades remain architectural burden and must be visible
    as blocking governance debt when the reviewed surface grows.
retained_entrypoint_burn_down_plan:
  review_date: '2026-09-30'
  linked_issue: '#4565'
  allowed_phases:
  - visibility
  - narrow-first-party-callers
  - enforce
  allowed_target_states:
  - retain_as_stable_public_api
  - narrow_first_party_callers
  rows:
  - path: src/bioetl/interfaces/cli/commands/run.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/run_all.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/run_composite.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/health.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/diagnostics.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/quarantine.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/interfaces/cli/commands/maintenance.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public CLI command seam; removal would be an external breaking change.
    completion_gate: Keep the top-level CLI command as the sanctioned patch/import seam and block new direct owner-module imports outside approved boundary coverage.
  - path: src/bioetl/composition/entrypoints.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public composition entrypoint; retained as stable integration API.
    completion_gate: Reduce first-party imports to the explicit execution-facing exports only and keep `_pipeline_execution`, `_resource_management`, and `_services` hidden behind the public seam.
  - path: src/bioetl/composition/health_api.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public composition health facade; retained as stable integration API.
    completion_gate: Route first-party health and quarantine call sites through the reviewed facade exports only and keep deeper composition owner modules as implementation details.
  - path: src/bioetl/composition/maintenance_api.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public composition maintenance facade; retained as stable integration API.
    completion_gate: Route first-party maintenance/archive/cleanup/vacuum call sites through the reviewed facade exports only and keep deeper composition owner modules as implementation details.
  - path: src/bioetl/infrastructure/config/__init__.py
    phase: narrow-first-party-callers
    target_state: narrow_first_party_callers
    rationale: Public package-root facade remains, but first-party imports must continue migrating to canonical modules.
    completion_gate: Hold the package-root facade as external convenience API while first-party `src/` imports continue ratcheting downward through the dedicated root-facade inventory.
  - path: src/bioetl/domain/composite/config.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public domain config model entrypoint; retained as stable integration API.
    completion_gate: Keep the root domain config entrypoint stable and block new imports of split config internals outside the owner package and dedicated boundary tests.
  - path: src/bioetl/domain/value_objects/activity_values.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public value-object entrypoint; retained as stable integration API.
    completion_gate: Keep the value-object facade stable and block new imports of split activity-value internals outside the owner package and dedicated boundary tests.
  - path: src/bioetl/application/composite/merger.py
    phase: enforce
    target_state: retain_as_stable_public_api
    rationale: Public composite merge module; retained while collaborator bundle API is canonical.
    completion_gate: Keep the sanctioned merge entrypoint stable and prevent first-party callers from bypassing the collaborator bundle/canonical merge surface.
measured_only_modules: []
