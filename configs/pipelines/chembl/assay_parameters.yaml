# configs/pipelines/chembl/assay_parameters.yaml
# ChEMBL Assay Parameters — inherits from _base.yaml.
# DQ & Filters: loaded via hierarchy (ADR-027/028/029).

pipeline_name: chembl_assay_parameters
provider: chembl
entity_type: assay_parameters
schema_file: ../../schemas/chembl/assay_parameters.yaml
description: "Extract experimental assay parameters from ChEMBL API"

business_primary_keys: ["assay_param_id"]

sink:
  silver:
    # Override: partition by type for efficient type-specific queries
    partition_by: ["type"]
