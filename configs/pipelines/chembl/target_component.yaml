# configs/pipelines/chembl/target_component.yaml
# ChEMBL Target Component — inherits from _base.yaml.
# DQ & Filters: loaded via hierarchy (ADR-027/028/029).

pipeline_name: chembl_target_component
provider: chembl
entity_type: target_component
description: "ChEMBL Target Components (protein sequences, etc.)"
schema_file: "../../schemas/chembl/target_component.yaml"

business_primary_keys: ["component_id"]

sink:
  silver:
    # Override: partition by organism for efficient organism-specific queries
    partition_by: ["organism"]
