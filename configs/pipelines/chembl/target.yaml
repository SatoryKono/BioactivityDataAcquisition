# configs/pipelines/chembl/target.yaml
# ChEMBL Target — inherits from _base.yaml.
# DQ & Filters: loaded via hierarchy (ADR-027/028/029).

pipeline_name: chembl_target
provider: chembl
entity_type: target
description: "Extract biological targets from ChEMBL API"

business_primary_keys: ["target_id"]

sink:
  silver:
    # Override: partition by target_type for efficient type-specific queries
    partition_by: ["target_type"]
