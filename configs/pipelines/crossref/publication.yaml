# configs/pipelines/crossref/publication.yaml
# CrossRef Publication — DOI enrichment with citation metadata.
# DQ & Filters: loaded via hierarchy (ADR-027/028/029).

pipeline_name: crossref_publication
provider: crossref
entity_type: publication
description: "Enrich publication records with CrossRef metadata via DOI resolution"

# Override: full_scan required — API doesn't support stable incremental cursors
loading_strategy: full_scan_only

business_primary_keys: ["doi"]
