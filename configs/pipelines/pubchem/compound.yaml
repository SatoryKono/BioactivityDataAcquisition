# configs/pipelines/pubchem/compound.yaml
# PubChem Compound — inherits from _base.yaml.
# DQ & Filters: loaded via hierarchy (ADR-027/028/029).

pipeline_name: pubchem_compound
provider: pubchem
entity_type: compound
description: "Pipeline for ingesting PubChem compounds"
schema_file: "../../schemas/pubchem/compound.yaml"

business_primary_keys: ["molecule_id"]

sink:
  silver:
    # Override: partition by batch_date for efficient date-range queries
    partition_by: ["batch_date"]
